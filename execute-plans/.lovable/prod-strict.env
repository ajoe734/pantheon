# Pantheon Lovable main deployment - BFF-CONSOL-023 strict cutover target.
# This env is for the Lovable main/dev frontend deployment only.
# Pantheon currently has no separate production BFF tier; this intentionally
# targets the existing dev BFF HTTPS endpoint. Do not apply this to
# staging-live without the staging-live publish gate and operator approval.

VITE_BFF_MODE=live
VITE_BFF_BASE_URL=https://pantheon-lupin-dev-bff.34.81.75.241.sslip.io
VITE_BFF_FALLBACK=strict
VITE_BFF_REAL_WRITES=false
