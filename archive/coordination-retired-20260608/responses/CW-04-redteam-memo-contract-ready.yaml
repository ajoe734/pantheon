feature_id: CW-04-redteam-memo
type: contract-ready
source_repo: ajoe734/pantheon
screen: redteam-memo
status: live
published_at: "2026-04-22T16:59:57Z"
task: CW-04-REDTEAM-MEMO-001
description: >-
  CW-04 Red-team Memo contract, live memo list/detail routes, and the
  module-local frontend handoff bundle are now aligned. Frontend may implement
  the memo list/detail surfaces against the published mapping object, evidence
  links, staleness semantics, and governance-handoff authority without
  deriving workflow from raw consultation data.
artifacts:
  bff_contract: docs/bff/CW-04-redteam-memo.md
  screen_spec: docs/screens/CW-04-redteam-memo.md
  example_payload: docs/examples/CW-04-redteam-memo.json
  lovable_ui_task: .coordination/responses/CW-04-redteam-memo-lovable-ui-task.yaml
  frontend_change_spec: docs/pantheon-handoffs/CW-04-redteam-memo/FRONTEND_CHANGE_SPEC.md
endpoints:
  - GET /api/v1/consult/memos
  - GET /api/v1/consult/memos/{memo_id}
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
dependencies:
  - CW-01-FOUNDATION-001
  - CW-02-TRANSCRIPT-001
readiness_gate: >-
  BFF route family confirmed live and the module-local frontend handoff bundle
  was published at 2026-04-22T16:59:57Z. Frontend may begin production UI
  against GET /api/v1/consult/memos and GET /api/v1/consult/memos/{memo_id}.
  If a live payload diverges from this synced contract during implementation,
  stop and emit the canonical CW-04 bff-gap handoff instead of mocking or
  inferring missing memo state.
front_actions_required:
  - fetch memo list from GET /api/v1/consult/memos with the published status, page_token, and page_size semantics only
  - fetch memo detail from GET /api/v1/consult/memos/{memo_id} only
  - navigate to detail using route_href from list rows; do not construct memo URLs from ids
  - render the mapping panel from session_to_memo_mapping exactly as supplied
  - resolve evidence navigation only from evidence_refs[].link; do not construct links from id or artifact_ref
  - render freshness from meta.staleness and surface health from meta.surfaces.redteam_memo.state without conflating them
  - show the governance CTA only when allowedActions.canInitiateGovernanceReview is true
  - keep recommendations[] as plain ordered strings; do not add severity or workflow columns
  - emit a bff-gap handoff if any required field is absent
acceptance_met:
  contract_ready_published: true
  bff_memo_list_route_live: true
  bff_memo_detail_route_live: true
  frontend_change_spec_published: true
  lovable_ui_task_published: true
  front_repo_receives_handoff_bundle: true
