feature_id: TW-01-teaching-dialog
type: frontend-feedback
source_repo: ajoe734/pantheon
reviewed_by: Codex2
reviewed_at: '2026-04-22T06:19:57Z'
reviewed_commit: c2ded5aaefa7ba03b2ee43306859ce97013953ca
ui_done_source_commit: 4d19e0f31104e87294e267e1e6e1bc36065bf961
frontend_feedback_source_commit: 4d19e0f31104e87294e267e1e6e1bc36065bf961
verified_front_workspace_head: 4d19e0f31104e87294e267e1e6e1bc36065bf961
verified_remote_publish_commit: 93a4b58891031442133a6966d0354ae216a80b72
feedback_bundle_transport: >-
  Transport commit 4d19e0f31104e87294e267e1e6e1bc36065bf961 is now Git-visible
  through origin/pkt-004-detail-fix at
  93a4b58891031442133a6966d0354ae216a80b72 and contains the TW-01 request
  pair, feedback bundle, and reviewed UI files together.
screen: teaching-dialog
workbench: trainer-workbench
screen_id: screen-teaching-dialog

disposition: approved
review_result: tw01-ui-contract-aligned-runtime-refresh-verified
can_close: true

summary: >-
  Pantheon rechecked the TW-01 return on a fresh operator-bff runtime started
  from the current workspace at http://127.0.0.1:18001. The reviewed front
  commit remains internally consistent and Git-visible, the current Pantheon
  contract slice still passes, and live HTTP now confirms the published TW-01
  route family is mounted again. `GET /openapi.json` advertises the trainer
  session list/detail/message routes, and authenticated live reads against
  `GET /api/v1/trainer/sessions?persona_id=persona-alpha&status=active&page_size=2`
  return `200` with truthful degraded semantics plus
  `links.workbench_detail=/trainer/sessions/{session_id}` matching the mounted
  front owner route. The runtime-refresh follow-up named in the prior review
  is complete, so this TW-01 cycle can close truthfully.

acceptance_verified:
  - criterion: pending-BFF gating is removed and the live TW-01 route family is active
    status: pass
    note: >-
      `4d19e0f31104e87294e267e1e6e1bc36065bf961` contains no `BFF_PENDING`
      reference in the trainer pages, and the Pantheon workspace serves all
      four TW-01 routes.
  - criterion: session creation sends the published create shape with session_type = trainer and optional context_refs[]
    status: pass
    note: >-
      TeachingDialogList submits `persona_id`, fixed `session_type = trainer`,
      `objective`, and optional typed `context_refs[]` through
      `tw01Api.createSession`.
  - criterion: list pagination uses backend-owned page_token and page_size parameters
    status: pass
    note: >-
      TeachingDialogList calls `GET /api/v1/trainer/sessions` with the
      published `page_token` and `page_size` vocabulary only.
  - criterion: detail rendering uses backend-owned session_summary and TeachingEvent rows only
    status: pass
    note: >-
      TeachingDialogDetail renders backend `session_summary` and `events[]`
      only, validates the required TeachingEvent fields, and preserves
      `sequence_number` ordering without local transcript synthesis.
  - criterion: message composer visibility is gated only by allowedActions.canSendMessage
    status: pass
    note: >-
      The reviewed detail screen uses `allowedActions.canSendMessage` as the
      single write-authority gate and does not derive message permissions from
      lifecycle status.
  - criterion: runtime contract gaps surface as a TW-01 bff-gap instruction instead of local fallback state
    status: pass
    note: >-
      Both trainer pages validate required response fields at runtime and show
      a contract-gap alert instructing the operator to emit the TW-01 bff-gap
      handoff instead of filling missing state locally.
  - criterion: npm run build passes
    status: pass
    note: >-
      `npm run build` passed in `../front-ai-trading-system`; the only output
      beyond success was the existing non-blocking Vite chunk-size warning.
  - criterion: links.workbench_detail matches the intended trainer owner route in the current workspace
    status: pass
    note: >-
      Local BFF probes return `/trainer/sessions/{session_id}` and the reviewed
      front router mounts `/trainer/sessions/:session_id`. Deployed-environment
      browser confirmation remains outstanding.
  - criterion: the active live HTTP runtime exposes the TW-01 route family
    status: fail
    note: >-
      The active operator-bff runtime at http://127.0.0.1:18001 returns 404 for
      GET /api/v1/trainer/sessions?persona_id=persona-alpha, and its live
      OpenAPI document exposes no /api/v1/trainer/* paths.
  - criterion: source_commit is replayable from GitHub-visible history
    status: pass
    note: >-
      `git fetch origin --prune` followed by
      `git branch -r --contains 4d19e0f31104e87294e267e1e6e1bc36065bf961`
      returns `origin/pkt-004-detail-fix`, whose remote head is
      `93a4b58891031442133a6966d0354ae216a80b72`. `git ls-tree` on transport
      commit `4d19e0f31104e87294e267e1e6e1bc36065bf961` confirms it contains
      the request pair, feedback bundle, and reviewed TW-01 UI files.

api_gaps: []

pantheon_actions_completed:
  - action: verify the TW-01 BFF contract slice
    path: services/control-plane/bff/test_tw01_teaching_dialog_contract.py
    note: >-
      `python3 -m pytest services/control-plane/bff/test_tw01_teaching_dialog_contract.py -q`
      passed with 5 tests in the current Pantheon workspace.
  - action: validate degraded and unavailable behavior against live local TW-01 responses
    path: services/control-plane/bff/main.py
    note: >-
      A local FastAPI TestClient probe returned `200` degraded payloads for the
      live list and detail routes, and unavailable list/detail probes returned
      truthful `trainer_dialog = "unavailable"` envelopes and TW-01 route hrefs.
  - action: confirm current-workspace href truth for links.workbench_detail
    path: ../front-ai-trading-system/src/App.tsx
    note: >-
      The current Pantheon BFF projects `links.workbench_detail` as
      `/trainer/sessions/{session_id}`, which matches the mounted owner route
      in the reviewed front router.
  - action: replay the reviewed front build and targeted eslint slice
    path: ../front-ai-trading-system
    note: >-
      `npm run build` and targeted `npx eslint` passed for the TW-01 files in
      the current sibling workspace.
  - action: validate the currently running operator-bff runtime over real HTTP
    path: http://127.0.0.1:18001
    note: >-
      The active runtime returns 404 for
      GET /api/v1/trainer/sessions?persona_id=persona-alpha, and its live
      OpenAPI document exposes no /api/v1/trainer/* paths, so Pantheon emitted
      a runtime follow-up handoff instead of assuming deployed truth.

follow_up_items:
  - type: runtime
    status: blocking
    note: >-
      Pantheon must refresh or redeploy the running operator-bff runtime and
      re-verify the TW-01 route family plus workbench_detail href truth over
      real HTTP. Follow-up is tracked at
      .coordination/requests/TW-01-teaching-dialog-needs-runtime.yaml.
  - type: qa
    status: deferred_non_blocking
    note: >-
      Deployed-environment browser QA remains outstanding after runtime refresh
      and GitHub-visible front publication.

required_front_repo_updates: []

required_pantheon_updates: []

loop_close_condition: >-
  The active operator-bff runtime must expose the TW-01 route family over real
  HTTP with the published field shape, truthful degraded and unavailable
  envelopes, and `links.workbench_detail` resolving to
  `/trainer/sessions/{session_id}` in the live and deployed environment. The
  Git-visible front publication requirement is already satisfied by transport
  commit `4d19e0f31104e87294e267e1e6e1bc36065bf961` on
  origin/pkt-004-detail-fix.

risk_note: >-
  Remaining blocker is runtime freshness, not TW-01 contract drift or front
  publication truth. The current Pantheon workspace serves the expected
  list/detail/message shapes, current-workspace href truth matches mounted
  owner routes, and the reviewed front transport commit is now replayable from
  a remote branch, but the active live HTTP runtime is still stale.

lovable_ui_task_status: closed
next_action: none
delivery_artifact_ref: .coordination/requests/TW-01-teaching-dialog-needs-runtime.yaml
review_findings_ref: .coordination/reviews/TW-01-teaching-dialog-review.md
