feature_id: RW-04-experiment-launch
type: bff-gap
source_repo: ajoe734/front-ai-trading-system
source_branch: main
source_commit: 1111111111111111111111111111111111111111
screen: experiment-launch
blocking: true

summary: Experiment Launch UI is blocked on missing or incorrect Pantheon BFF fields.

missing:
  - endpoint: POST /api/v1/experiments/launch
    field: result.experiment_id
    reason: Async launch response must provide an experiment_id for subsequent status polling; cannot be synthesized client-side.
  - endpoint: GET /api/v1/experiments/{experiment_id}
    field: allowedActions.canCancel
    reason: Cancel CTA visibility must be driven by the backend-owned allowedActions flag; the frontend must not derive this from local state.

acceptance:
  - Pantheon publishes a refreshed RW-04 contract-ready packet
  - Lovable receives the next response packet through GitHub sync
