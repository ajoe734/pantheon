feature_id: PKT-003-post-incident-review
type: bff-gap
source_repo: ajoe734/front-ai-trading-system
source_branch: main
source_commit: 0000000000000000000000000000000000000000
screen: post-incident-review-console
blocking: true

summary: >
  Post-Incident Review Console list panel is blocked on one missing field.
  The incident list item projection (_project_incident_home_item) returns
  opened_at but omits resolved_at. The composed detail view endpoint
  (GET /api/v1/operator/post-incident-review/{incident_id}) is contract-ready.

missing:
  - endpoint: "GET /api/v1/incidents"
    field: "items[].resolved_at"
    reason: >
      _project_incident_home_item() in services/control-plane/bff/main.py (line 446)
      returns opened_at (aliased from created_at) but does not include resolved_at.
      The resolved_at field is present in the IncidentCase dataclass and in the seed
      data — it is simply not projected. The list panel requires resolved_at per row
      to display when each resolved incident was closed.
    fix: >
      Add resolved_at to _project_incident_home_item():
        "resolved_at": incident.get("resolved_at")

not_missing:
  - endpoint: "GET /api/v1/operator/post-incident-review/{incident_id}"
    note: >
      All required fields are present: data.incident.* (incident_id, title, status,
      artifact_id, artifact_version, runtime_id, trace_id), data.postmortem,
      data.evolution_decisions[], data.lineage_edges[], data.telemetry_performance
      (with summary.total_pnl/max_drawdown/sharpe_ratio), meta.snapshot_at, and all
      four meta.surfaces keys (postmortem, evolution_decisions, lineage,
      telemetry_performance). Contract-ready.
  - endpoint: "GET /api/v1/postmortems"
    note: >
      Navigation-only endpoint. No blocking gap for this screen.

acceptance:
  - BFF adds resolved_at to the incident list item projection
  - Pantheon publishes a refreshed contract-ready packet for this screen
  - Lovable receives the next response packet through GitHub sync

contract_lock: docs/pantheon-delivery/PKT-003-post-incident-review/CONTRACT_LOCK.md
delivery_note: docs/pantheon-delivery/PKT-003-post-incident-review/DELIVERY_NOTE.md

resolved: true
resolved_at: "2026-04-17T04:00:00Z"
resolved_by: Claude
resolution: >-
  _project_incident_home_item() in services/control-plane/bff/main.py already
  includes resolved_at: incident.get("resolved_at"). The field is present in the
  IncidentCase dataclass and seed data. Contract-ready packet refreshed and Lovable
  UI task published. Lovable prompt updated to resume implementation cycle.
resolution_artifacts:
  - .coordination/responses/PKT-003-post-incident-review-contract-ready.yaml
  - .coordination/responses/PKT-003-post-incident-review-backend-delivery.yaml
  - .coordination/responses/PKT-003-post-incident-review-lovable-ui-task.yaml
