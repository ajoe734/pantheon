# P2-TRL-RUNTIME-DATA-ACTIVATION-001 Evidence

Generated by:

```bash
python3 services/learning/trl/activation_smoke.py --enable-activation-ready --backend real --output-dir /tmp/pantheon/learning/trl/runtime-data-activation-real
```

Summary:

- FB-002 bounded governed fixture: 240 events, 240 preference pairs, 3 strategy families, 12 operators, approve/edit/reject all present.
- Data gates: preflight required gates open for FB-002 volume and preference-pair volume.
- Real TRL backend attempt: explicit dependency/config failure, `ModuleNotFoundError: No module named 'trl'`; `silent_stub_fallback=false`.
- Handoff artifacts: model artifact bundle, registry entry, evaluator packet, and candidate packet copied into this directory.
- Governance boundary: no registry write, no order routing, no deployment stage promotion, no raw secrets.

The JSON files preserve the command output. Paths inside `manifest.json` and
`activation_evidence_summary.json` point to the original `/tmp` smoke output;
the same files were copied here for durable review.
