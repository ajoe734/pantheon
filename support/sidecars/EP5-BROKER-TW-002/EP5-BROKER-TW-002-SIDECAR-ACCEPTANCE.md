# EP5-BROKER-TW-002 Acceptance Packet

**Sidecar kind:** `acceptance_packet`
**Sidecar task:** `EP5-BROKER-TW-002-SIDECAR-ACCEPTANCE`
**Helper parent:** `EP5-BROKER-TW-002` — Run Shioaji sandbox place/cancel/readback/reconcile smoke and assemble canary readiness packet
**Parent owner:** `Codex2`
**Parent reviewer:** `Claude`
**Prepared by:** `Gemini`
**Reviewer:** `Codex2`
**Date:** `2026-05-12`
**Status:** `in_progress`

> Scope constraint: support artifact only. This packet does not modify canonical truth, L1 policy files, runtime implementation, registry state, or governance semantics. It packages the acceptance surface for the `EP5-BROKER-TW-002` broker activation slice so the assigned reviewer can validate the smoke results and readiness packet without re-scanning the full task history.

---

## 1. Purpose

This sidecar packet gives `Claude` a compact acceptance surface for the open parent task `EP5-BROKER-TW-002`:

1. Restate the parent acceptance criteria against the current Shioaji sandbox smoke evidence.
2. Map the concrete dependencies and the output artifacts required for canary readiness.
3. Summarize the sandbox smoke constraints to ensure "fail-closed" live safety is preserved.
4. Provide a reviewer handoff checklist for formalizing the activation proof.

---

## 2. Parent Acceptance Criteria Checklist

From active `ai-status.json`:

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Sandbox place/cancel/readback/reconcile smoke runs end-to-end | **IN_PROGRESS** | `services/broker/shioaji/sandbox_smoke.py` is implemented and supports `--mock-api` and real simulation SDK paths. |
| 2 | Evidence captures request/response payload + order IDs + status transitions + reconcile diff | **IN_PROGRESS** | `run_smoke` function in `sandbox_smoke.py` structures this data into a comprehensive results dictionary. |
| 3 | `scripts/run_ep5_canary_readiness.py` consumes evidence and emits human-gate packet | **PENDING** | The smoke harness must first produce the output bundle for consumption by the readiness script. |
| 4 | Live path stays `SHIOAJI_LIVE_DISABLED` throughout; zero real-capital write | **SOURCE-READY** | `ShioajiBrokerAdapter` enforces this invariant; `sandbox_smoke.py` explicitly checks for `live_reject.error_code == "SHIOAJI_LIVE_DISABLED"`. |
| 5 | `BROKER_SHIOAJI_SANDBOX_ENABLED` toggle explicit; default-false still enforced | **SOURCE-READY** | `sandbox_smoke.py` reads this from env and records it in the evidence bundle. |
| 6 | `pytest services/broker/shioaji/` stays green | **PENDING** | Verification required after implementation completion. |
| 7 | No registry admission / paper / canary / live / capital binding performed | **INVARIANT** | Per `PAPER_CANARY_LIVE_POLICY.md` §2.1.1, sandbox smoke is a prerequisite that must not mutate production state. |

**Overall verdict:** The parent task is moving through implementation. This sidecar confirms the path to acceptance via evidence collection and readiness packet generation.

---

## 3. Dependency Map

### Upstream dependencies

| Dependency | Status | Relevance to `EP5-BROKER-TW-002` |
|---|---|---|
| Shioaji Simulation SDK / Sandbox Credentials | External | Required for real sandbox smoke; `--mock-api` provides a fallback for environment-limited runs. |
| `PAPER_CANARY_LIVE_POLICY.md` (L1) | Canonical | Defines the boundary between sandbox smoke (allowed) and canary/live (fail-closed). |

### Loop dependency chain

```text
EP5-BROKER-TW-002 (Implementation)
  -> services/broker/shioaji/sandbox_smoke.py --output-dir <DIR>
      -> Evidence bundle (JSON files)
          -> scripts/run_ep5_canary_readiness.py
              -> Canary Readiness Packet (Human-gate)
                  -> Reviewer Approval
                      -> Parent Closeout
```

### Expected artifacts to be produced by parent

| Expected artifact | Role |
|---|---|
| `support/ep5-broker-tw/sandbox-smoke-evidence.md` | Human-readable summary of the smoke test results. |
| `sandbox-smoke-output/summary.json` | Machine-readable evidence for the readiness script. |
| `sandbox-smoke-output/reconciliation.json` | Detailed diff between request and readback status. |
| Canary Readiness Packet | The final packet for human-gate approval (generated by `run_ep5_canary_readiness.py`). |

---

## 4. Sandbox Smoke Constraints & Safety

Per `PAPER_CANARY_LIVE_POLICY.md` and `services/broker/shioaji/sandbox_smoke.py`:

### 4.1 Safety Invariants
- **Live Rejected:** `adapter.reject_live_order()` must be called to prove the live path is disabled.
- **No Real Capital:** The evidence bundle must explicitly confirm `real_capital_used: False` and `production_live_order_submitted: False`.
- **Sandbox Boundary:** All orders must be marked with `deployment_stage: "sandbox"`.

### 4.2 Evidence Collection
- **Full Trace:** Mock API calls are captured in `sdk_trace` when `--mock-api` is used.
- **Readback Sync:** Reconcile must check status transitions (e.g., `submitted` -> `cancelled`).
- **Audit Ready:** Every generated artifact includes the `TASK_ID` and a `generated_at` timestamp.

---

## 5. Reviewer-Facing Acceptance Surface

### 5.1 What the reviewer should confirm

1. **Verify Evidence Bundle:** Confirm that the JSON artifacts produced by `sandbox_smoke.py` contain matched `observed` vs `expected` fields in `reconciliation.json`.
2. **Verify Live Rejection:** Confirm the evidence shows a rejected live-order attempt with `SHIOAJI_LIVE_DISABLED`.
3. **Verify Readiness Script Output:** Confirm that `scripts/run_ep5_canary_readiness.py` successfully consumed the bundle and produced a valid readiness packet.
4. **Clean Workspace:** Confirm `pytest services/broker/shioaji/` passes and no unrelated files were touched.

---

## 6. Sidecar Scope Declaration

- No canonical L1 or L2 document was modified by this sidecar.
- No runtime, registry, or governance implementation was modified by this sidecar.
- The only artifact produced by this slice is this support packet.
- Parent execution, review approval, and closeout remain owned by the `EP5-BROKER-TW-002` parent task lifecycle.

---

## 7. Reviewer Handoff Notes

**Reviewer:** `Codex2`

**Next steps**
1. Once the parent task implementation is complete and evidence is generated, verify this packet against the produced artifacts.
2. If the mapping and checklist are accurate, use the approval command below.

**Approval command**
```bash
AI_NAME=Codex2 python3 scripts/ai_status.py approve EP5-BROKER-TW-002-SIDECAR-ACCEPTANCE "Acceptance packet and dependency map verified; sandbox smoke evidence mapping and readiness packet flow are correctly structured."
```
