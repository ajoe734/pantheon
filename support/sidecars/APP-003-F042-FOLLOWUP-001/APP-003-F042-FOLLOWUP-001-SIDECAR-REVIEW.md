# APP-003-F042-FOLLOWUP-001 Review Packet

**Sidecar kind:** `review_packet`
**Sidecar task:** `APP-003-F042-FOLLOWUP-001-SIDECAR-REVIEW`
**Helper parent:** `APP-003-F042-FOLLOWUP-001` — Finish F-042 promotion-review frontend follow-up
**Parent owner:** `Codex`
**Parent reviewer:** `Codex2`
**Prepared by:** `Codex`
**Reviewer:** `Codex2`
**Date:** `2026-04-24`
**Status:** `complete`
**Revalidated:** `2026-04-24T09:05:07Z` against the current front-repo `HEAD`
`139081f0e4d516494819003bd95968ecb9b86c99`

> Scope constraint: support artifact only. This packet does not edit canonical
> truth or front-repo coordination files. It records the post-fix review read
> after rechecking the substantive reopen claims against the checked-in front
> implementation.

## 1. Review Verdict

The prior branch-`HEAD` wording blocker is cleared, and the reopened
substantive blocker no longer holds up under code inspection.

The current F-042 feedback bundle should now be reviewed as a closeout-ready
packet anchored to the reviewed checked-in F-042 surface at
`d306f850a8e04982862405e4435855bf11e008e4`.

## 2. Verification Surface

- `AI_COLLABORATION_GUIDE.md`
- `.orchestrator/task-briefs/app_003_f042_followup_001.md`
- `ai-status.json`
- `../front-ai-trading-system/.coordination/requests/F-042-frontend-feedback.yaml`
- `../front-ai-trading-system/docs/pantheon-feedback/F-042/API_GAP_REQUESTS.json`
- `../front-ai-trading-system/docs/pantheon-feedback/F-042/LOVABLE_CHANGE_FEEDBACK.md`
- `../front-ai-trading-system/docs/pantheon-feedback/F-042/QA_STATUS.md`
- `../front-ai-trading-system/docs/pantheon-feedback/F-042/UI_DECISIONS.md`
- `git -C ../front-ai-trading-system rev-parse HEAD d306f850a8e04982862405e4435855bf11e008e4`
- `git -C ../front-ai-trading-system diff --name-only d306f85..139081f -- src/App.tsx src/auth/AuthProvider.tsx src/pages/promotion/PromotionReview.tsx src/pages/promotion/types.ts src/lib/bffClient.ts`

## 3. Findings

### 3.1 The reviewed commit anchor is now truthful

The current feedback bundle records the F-042 source commit as:

- `d306f850a8e04982862405e4435855bf11e008e4`

That object resolves in the front repo, and the mirrored F-042 feedback docs
use the same full hash while describing it as the reviewed checked-in F-042
surface for this cycle rather than branch `HEAD`.

The local branch `HEAD` is still:

- `139081f0e4d516494819003bd95968ecb9b86c99` (`Publish governance and drilldown UI handoffs`)

There are still no committed F-042 file changes between `d306f85` and
`139081f`, so anchoring the packet to `d306f85...` remains correct.

### 3.2 The old reopen claims are no longer substantively accurate

What the checked-in code now shows:

- `src/App.tsx` mounts both `/promotion-review/:planId` and
  `/promotion-review`, so the route is no longer query-only.
- `src/pages/promotion/PromotionReview.tsx` prefers the route param, still
  accepts the legacy `?plan=` fallback, and renders decision metadata from
  `data.approval_decision`.
- `src/auth/AuthProvider.tsx` synchronizes `pantheon_operator_token` on refresh,
  sign-in, and sign-out.
- `src/lib/bffClient.ts` reads that same token for authenticated requests.
- `src/pages/promotion/PromotionReview.tsx` tolerates nullable
  `latestRun.progress` by showing an explicit unavailable state.

So the old reopen rationale about route binding, approval-decision rendering,
and auth-token bridging is stale on this reviewed surface.

### 3.3 No new Pantheon BFF gap is justified

`API_GAP_REQUESTS.json` still correctly remains `no_open_gaps`.

The present task is not a new backend-gap handoff. It is a closeout review of a
checked-in front surface that now matches the published contract closely enough
for normal review flow.

## 4. Required Owner Fixes

The owner fixes required for this cycle are now complete in the checked-in
feedback bundle.

If the owner later wants to claim closeout on a newer local F-042 surface, they
would still need a newer checked-in publication and a repointed evidence
bundle. That is not required for this review cycle.

## 5. Reviewer Recommendation

Recommended parent-task disposition:

1. Review `APP-003-F042-FOLLOWUP-001` as a closeout-ready packet, not a reopen.
2. Do not reject it again for the prior branch-`HEAD` wording issue.
3. Do not open a new Pantheon BFF gap from this packet alone.
4. If the reviewer agrees the refreshed evidence is truthful, advance the
   parent task through the normal review flow.

*Generated by Codex as a sidecar `review_packet` helper for
`APP-003-F042-FOLLOWUP-001`. This file is a support artifact and does not
modify canonical truth.*
