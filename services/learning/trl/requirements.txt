# TRL (Transformers for Reinforcement Learning) — governed preference-learning path
# OSS-NEXT-002: version pin for Pantheon activation baseline
# Package isolation rule: never merge into shared requirements; use this container only.
# Compatibility verified against: DSPy v2.4.5, imitation v1.0.1, MLflow 3.10.1, pyqlib 0.9.6
# Production DPO training requires: transformers, accelerate, peft (listed below).
# CI / smoke runs use StubDPOBackend — no TRL install required.

trl>=0.8.0,<0.10.0
transformers>=4.36.0,<5.0.0
accelerate>=0.26.0,<1.0.0
peft>=0.7.0,<1.0.0
datasets>=2.16.0,<4.0.0
torch>=2.1.0,<3.0.0
sentencepiece>=0.1.99
