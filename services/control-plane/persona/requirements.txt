fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
# langgraph>=0.1.0  # optional: add only if HITL checkpoint/pause/resume becomes a hard requirement
# langchain-anthropic>=0.1.0  # add when LLM_BACKEND=anthropic is wired
# langchain-openai>=0.1.0     # add when LLM_BACKEND=openai is wired
redis>=5.0.0
