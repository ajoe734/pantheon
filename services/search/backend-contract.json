{
  "contract_version": "pantheon.search-backend-contract.v2",
  "task_id": "SIMPLIFY-RETRIEVAL-ACCEPTANCE-CLOSURE-001",
  "accepted": true,
  "chosen_backend": "postgres_pgvector",
  "delivery_state": "accepted_single_production_backend",
  "activation_allowed": true,
  "reason": "PostgreSQL (native FTS + pgvector) selected per Requirement 3: achieves equivalent quality with Qdrant baseline (Recall@10 = 1.0, nDCG@10 >= 0.99), p95 latency under 1s, zero external inference, and eliminates dedicated vector database operational overhead (zero extra container, backup, patch, and RAM cost).",
  "product_runtime": {
    "retained_ranker_base": "5d15c7b821a846d24b76ce4f653f990482da0518",
    "source": "origin/dev at recovery; exact base verified in task evidence",
    "search": "PostgresRetrievalBackend integrated via SearchGateway facade; durable-only isolation preserved when durable_index_only=true.",
    "memory": "Unified institutional and persona semantic retrieval wired to selected PostgresRetrievalBackend with fallback to local store.",
    "negative_memory": "Unified negative-memory hybrid retrieval wired to selected PostgresRetrievalBackend.",
    "retained_additive_checks": [
      "tenant comparison",
      "as-of cutoff narrowing"
    ]
  },
  "candidate_modules": [
    "services/search/pg_retrieval.py",
    "services/search/local_embeddings.py",
    "services/memory/search_retrieval.py"
  ],
  "candidate_status": "PostgreSQL pgvector backend accepted as sole production backend; Qdrant benchmarked as comparative evaluation baseline and retained for regression verification.",
  "dependencies_and_models": "Pinned model intfloat/multilingual-e5-large (dimension 1024, revision 66076b8dc6e367337e3e90e6fb309fb0f3addaf6) via FastEmbed ONNX local execution with zero external inference.",
  "infrastructure_consumer": {
    "task": "OSS-INFRA-PROFILES-001",
    "instruction": "Consume accepted postgres_pgvector backend contract. No Qdrant dedicated daemon required in root Compose."
  },
  "validation_entrypoint": "services/search/evaluation/run_retrieval_eval.py",
  "validation_exit_status": 0,
  "rebuild_rollback": "Idempotent schema migration via services/search/sql/retrieval_index.sql; soft-delete tombstones and checkpoint recovery verified."
}
