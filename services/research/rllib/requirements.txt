# RLlib + Ray Tune research container dependencies
# Status: version-pinned deferred-prep scaffold; repo-local adapters and smoke paths exist
# Activation gate: explicit prep env/CLI gates are required; RL path approval is still required
# before production training/eval, registry writes, or paper/live activation
# See: services/learning/rl/PATH_DEFINITION.md §1
# See: services/learning/DEFERRED_OSS_ACTIVATION_MAP.md §4

--extra-index-url https://download.pytorch.org/whl/cpu
ray[rllib]==2.55.1
ray[tune]==2.55.1
torch==2.13.0+cpu
gymnasium==1.2.2
pydantic>=2.6.3
redis>=5.0.0
