OPS-GITHUB-CANONICAL-REVIEW-ENFORCEMENT-001 validation
Captured: 2026-08-09
Head verified: 5f8d467d5fee1aec269b54352c4087275482897b

Post-mainline composition:

The task branch composes `origin/dev`
`f3c3b830d9756a1a2b2bf40a42b8e086e615a469` at composition merge
`40d3216ff2fdb32d83b4b763e5315a2b6552bf23`, with no conflicts. Anchor
`5f8d467d5fee1aec269b54352c4087275482897b` corrects the required-context
publisher collision and is the code head verified below.

The historical second forward composition followed the first,
`9e165a1d7edf751bc3b519e749ccf601eb231c57` over `origin/dev`
`003688bd7402d051986c07f1769285925af24e1b`, cleared the `CONFLICTING`
mergeable state on PR #4303. Its sole conflict was the leading comment block
of `.github/workflows/canonical-review-gate.yml`; both comment-only sides were
retained and no executable workflow key, permission, trigger, or step changed.

The second composition was needed because `origin/dev` advanced 20 commits
(`003688bd7` -> `34e1f494a`) within two minutes of the exact-head approval of
`e8843e3d706bd30ff4aa45926678ab369903c015`, so
`scripts/git/auto_integrator.py` returned `rebase_required`: the approved head
no longer contained `dev` and the approval would not have covered what
actually merged. The refresh is a forward merge, not a rebase, so this
evidence manifest stays in the PR diff. It reported no conflicts, and

    git log 003688bd7402d051986c07f1769285925af24e1b..34e1f494a251f6c2292a6675baa0ed65fdab7bb5 -- <six task artifacts>

is empty: no commit in that range touched any file this task owns.

The generic gate retains dev's git-native exact-head review-proof
implementation. The trusted-base, read-only attestation audit stays isolated
at `.github/workflows/canonical-review-attestation-audit.yml`; it cannot
invoke the generic status writer or publish the external-App-pinned reviewer
attestation.

Read-only live regression evidence:

- PR-head check-run 92604198652 used name `Pantheon canonical review gate`
  and remained failure on c9d3485247c7fe2f9d7f69b49ba55806382782d6.
- Later commit status 51876975707 used the same context and was success at the
  same exact head.
- Workflow-dispatch run 31245097563 was success but its check-run was attached
  to dev, while PR #4303 remained mergeable_state=blocked.
- The publisher job is now `Publish canonical review status`; only its emitted
  commit status uses the required context name. Expected checker exit 1 no
  longer fails the publisher job, while an absent required status still
  blocks.

Focused commands:

    python3 -m pytest scripts/git/test_canonical_review_check.py
    python3 -m pytest scripts/git/test_canonical_review_gate_workflow.py
    python3 -m pytest scripts/git/test_task_review_merge_gate.py
    python3 -m pytest scripts/git/test_github_review_bridge.py
    PYTHONPATH=.orchestrator python3 -m pytest .orchestrator/test_github_bus.py

PASS - 194 tests (plus 2 subtests): 38 canonical-review-check, 23 generic-gate
workflow, 94 task-review-merge-gate, 11 github-review-bridge, and 28
GitHub-bus tests. The github-review-bridge suite was omitted from the earlier
182-test aggregate; it is included here.

Static checks:

- `python3 -m py_compile scripts/git/canonical_review_check.py
  scripts/git/canonical_review_gate_ci.py`: PASS.
- JSON parse of all five task evidence manifests: PASS.
- YAML safe-load of both canonical-review workflows post-merge: PASS.
- `git diff --check origin/dev...HEAD`: PASS.
- `actionlint`: not run because no `actionlint` executable is available in
  this worker environment.

CLI-level machine-plan checks (synthetic fixtures only, no live API call):

- `protection-plan` with a dedicated external App canary (id 987654, slug
  `pantheon-canonical-reviewer`, owner `pantheon-governance`, completed
  success): exit 0, schema `pantheon.canonical-review-protection-plan/v2`,
  issuer bound to the canary check-run id, head sha, and payload digest;
  activation ordered auto-merge disable -> admin enforcement -> required-check
  update, with the exact inverse rollback emitted.
- `protection-plan` with the generic `github-actions` canary (App id 15368):
  exit 1, `issuer_provenance_invalid`, "the generic github-actions App cannot
  be the canonical issuer".
- `verify-protection` against the plan-conformant post-activation readback:
  `all_entrypoints_blocked_without_exact_approval: true`.
- `verify-protection` after dropping the pre-existing
  `Pantheon canonical review gate` check: readback fails
  (`all_entrypoints_blocked_without_exact_approval: false`).
- `verify-protection` with a same-name attestation context re-pinned to App id
  15368: readback fails
  (`all_entrypoints_blocked_without_exact_approval: false`).

Focused assertions covered:

1. Exact-head signed attestations fail closed for missing, rejected, stale,
   expired, self-owned, forged, cross-PR, cross-repository, and key-mismatch
   envelopes.
2. A canary from generic `github-actions` App id 15368, a same-name Actions
   required check, or an App owned by the shared repository account cannot
   satisfy the dedicated external issuer plan.
3. The attestation audit rejects non-`dev|master` bases before comment fetch
   or checkout and uses the captured immutable base SHA with persisted
   credentials disabled.
4. The attestation audit has no checks/status write capability and cannot call
   `canonical_review_gate_ci.py`; the distinct generic lifecycle gate remains
   present under its own context.
5. Plan-bound readback rejects changed App identity, strict drift, missing
   pre-existing checks, admin bypass, or enabled auto-merge.
6. The generic required status is resolved entirely from the exact-head
   `pantheon-review/approve/<head-sha>` tag. It fails closed on a fresh head
   before independent approval instead of reading unavailable runner-local
   task state.
7. The publisher check-run name is distinct from the required commit-status
   context, and checker exit 1 is treated as an expected fail-closed status
   publication rather than a second same-name failed GitHub result.

Not run:

- No dedicated external GitHub App was created, installed, or used; that
  requires Human/Ops and a credential boundary outside shared owner runtime.
- No live branch protection, repository auto-merge setting, key, merge, or
  auto-merge mutation was authorized or performed.
- The refreshed composition head has not yet been independently approved. Its
  generic canonical-review status is correctly expected to fail closed until
  the current reviewer `Claude` approves that exact pushed head through
  the governed command, which creates the exact-head review-proof tag. The
  superseded head `e8843e3d706bd30ff4aa45926678ab369903c015` did carry a valid
  Antigravity approval, proof tag, and green required status; it was retired
  only by dev movement, not by any review finding.
