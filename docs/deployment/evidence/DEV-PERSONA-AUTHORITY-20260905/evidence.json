{
  "schema_version": "pantheon.review-evidence.v1",
  "task_id": "DEV-PERSONA-AUTHORITY-20260905",
  "title": "Implement tenant-bound authoritative Persona training targets",
  "recorded_at": "2026-09-05T05:10:00Z",
  "owner": "Claude",
  "reviewer": "Antigravity",
  "target_repo": "pantheon",
  "task_class": "functional",
  "summary_zh": "Root review R2（/tmp/dev-persona-authority-root-review-r2-20260905.md，對照 head d37a588b9a4d92dccf399ee20817380f38fdc1cc）指出前一版仍有三個 owner-boundary 缺陷：(1) 對未曾提交過 training target 的 Persona，任何呼叫者宣稱的 tenant_id 都會取得看似 authoritative 的 generation-0 view；(2) commit() 只驗證 evaluation_proof 內部與 request 本身宣稱欄位是否一致，從未把 expected_precondition_digest/expected_precondition_record_ref 拿去跟實際目前持久化的 owner state 比對；(3) _apply_to_persona_owner 只寫入 digest/ref 等 receipt metadata，沒有寫入真正的 candidate/control_state 內容，且 target store 先 CAS 成 status=committed 才呼叫這個獨立的 Persona patch，若該 patch crash/失敗，readback 會回報已 committed 但實際上從未真的套用，且 replay 分支會直接回傳成功而不修復。此版本只在 services/persona/write_owner.py 修正這三項，並更新/新增 services/persona/test_training_target_owner.py 內的 focused tests 覆蓋，不動 services/training-session/persona_target.py（凍結 client validator）、不動 Governance owner（DOMAIN-WRITERS-001 範圍）。",
  "artifacts": {
    "implementation_paths": [
      "services/persona/write_owner.py"
    ],
    "test_paths": [
      "services/persona/test_training_target_owner.py"
    ],
    "evidence_manifest": "docs/deployment/evidence/DEV-PERSONA-AUTHORITY-20260905/evidence.json"
  },
  "scope": {
    "owned": [
      "Root review R2 finding 1 (fabricated tenant on first read): _virtual_initial_record now derives tenant_id from the Persona's own real, governed `owner` field (set only through the persona.admin-gated create/patch path) instead of the caller-asserted X-Tenant-Id header. A new _load_owner_bound_persona(persona_id, tenant_id) helper is called by both read() and commit() before any precondition view (persisted or virtual) is ever returned, and raises TrainingTargetTenantMismatch (403) for any tenant that is not the Persona's actual owner. A caller can no longer obtain an authoritative-looking precondition for a tenant it does not genuinely own, even for a never-committed Persona.",
      "Root review R2 finding 2 (precondition never checked against real state): commit() now computes actual_precondition = the real persisted record, or the real virtual generation-0 view derived from the actual owner-bound Persona when none exists, independently re-derives its canonical digest with the same _canonical_digest the read path would compute, and requires request.expected_precondition_digest and request.expected_precondition_record_ref to exactly equal that real computed digest/ref before any further verification runs. A self-consistent but invented precondition (internally matching the caller's own proof) is now rejected as TrainingTargetProofInvalid (422) unless it genuinely matches the durable owner state.",
      "Root review R2 finding 3 (false terminal commit before application, no real applied values, no order safety): the target-store CAS on a fresh commit now writes status='applying' (not 'committed') first. Only after _apply_to_persona_owner has durably written the real Persona record's metadata -- including the actual candidate and control_state content (training_target_candidate / training_target_control_state), not just their digests -- does a new _finalize_committed step CAS the target record's status to the terminal 'committed'. If the process crashes or _apply_to_persona_owner fails between those two steps, the record durably stays 'applying': read() then serves a non-committed status that the frozen client's persona pre-readback/terminal-readback validators reject (fail closed, no false success), and a replay of the identical generation+binding+Idempotency-Key now detects the non-committed status and re-runs _apply_to_persona_owner (idempotently -- a no-op if it already landed) followed by _finalize_committed, recovering instead of returning a false terminal receipt. _apply_to_persona_owner is also made order-safe against concurrent/out-of-order generations: it first reads the Persona's own current training_target_generation from its metadata and is a no-op if that generation is already >= the generation being applied, so a lower generation's apply that runs after a higher generation already landed can never regress it."
    ],
    "not_changing": [
      "services/training-session/persona_target.py -- the frozen client validator is exercised as-is via its public read_persona_target_precondition/commit_persona_target functions, loaded standalone exactly as its own test suite does; not one line of it is edited",
      "The Governance owner service and its approval-decision contract (DOMAIN-WRITERS-001 scope) -- served from the same in-memory _FakeGovernanceApprovalVerifier test double as before",
      "The existing generic Persona lifecycle/CRUD fields and vocabulary (PersonaBody.status/lifecycle_state) -- training-target status stays a distinct {active, applying, committed} vocabulary on the separate training-target resource",
      "execute-plans FE PR #738 (head dbdb2f4ac621075e47f67f9b567b94f0cb06e6e2) and Pantheon PR #5593 (head 48e32ff7427294be633b05bff66a110f91592c96) -- untouched",
      "No live credentials, no dev/prod deployment or mutation -- everything runs against a temporary AtomicJsonRecordStore under pytest's tmp_path"
    ],
    "known_gap": [
      "Deployment wiring (persona-svc live URLs, Governance approval verifier base URL/service token) and hosted/dev acceptance remain explicitly out of scope per the task brief -- root GPT-6 Astra owns integration validation and dev deployment. This delivery only proves the Persona-owner contract itself, end to end, against the exact frozen validator and against direct owner-boundary attacks that bypass that client.",
      "The tenant identity for a Persona's training-target authority is now bound to the existing PersonaBody.owner field (the only genuine, governed tenant-shaped field this data model currently has). If a future canonical decision introduces a dedicated first-class tenant_id field on Persona distinct from `owner`, this binding will need a follow-up migration; until then, `owner` is the actual existing owner persistence this task's acceptance criteria requires reusing rather than fabricating."
    ]
  },
  "acceptance_mapping": {
    "training_authority_readback_write_readback_without_weakening_validator": "test_precondition_read_commit_and_restart_readback exercises the real frozen persona_target.read_persona_target_precondition and commit_persona_target functions (unmodified) end to end, including terminal-readback digest/generation/controller_record_ref checks and the newly-asserted real applied candidate/control_state content on the Persona record",
    "tenant_isolation_generation_cas_idempotency_conflict": "PersistentPersonaTrainingTargetOwner.commit(): owner-field-bound tenant authority (_load_owner_bound_persona) + compare_and_set on generation + real-state precondition digest/ref CAS + binding-equality check at the already-committed generation",
    "cross_tenant_denial": "test_cross_tenant_read_and_write_are_denied and the rewritten test_first_reader_cannot_hijack_tenant_via_read (any non-owner tenant is now rejected with 403 on the very first read/commit attempt, not merely denied a durable binding)",
    "stale_generation": "test_stale_generation_commit_is_rejected (409 after two real commits advance the generation)",
    "mismatched_or_expired_approval": "test_commit_rejects_unissued_approval for the owner's independent approval verification; the untouched frozen suite services/training-session/tests/test_persona_target.py covers the client-side expiry/identity checks",
    "semantic_idempotency_conflict": "test_semantic_idempotency_conflict_rejects_reused_generation_new_payload and test_replay_with_swapped_content_same_claimed_digests_is_rejected",
    "actual_policy_control_change_and_restart_readback": "test_precondition_read_commit_and_restart_readback now additionally asserts persona.metadata['training_target_candidate']/['training_target_control_state'] equal the real committed candidate/control_state after a simulated process restart, not just their digests",
    "root_review_r2_precondition_must_match_actual_owner_state": "test_commit_rejects_wrong_precondition_digest continues to cover an internally-inconsistent proof; the new real-state CAS check in commit() is exercised by every passing commit test (each one's expected_precondition_digest/ref must now equal the server's own independently computed value or the commit is rejected) and by test_commit_recovers_after_crash_between_durability_and_application's second (recovery) attempt reusing the same real binding",
    "root_review_r2_atomic_recoverable_applied_state": "test_commit_recovers_after_crash_between_durability_and_application: a commit whose _apply_to_persona_owner is monkeypatched to fail once leaves the target record durably 'applying' (proven via a fresh GET returning status='applying' and the Persona's own metadata still lacking the new generation); replaying the identical generation/binding/Idempotency-Key repairs it (applies then finalizes) and returns a terminal 'committed' replay",
    "root_review_r2_concurrent_generation_ordering": "test_apply_ignores_out_of_order_lower_generation: directly invoking _apply_to_persona_owner with a stale (already-superseded) generation's content after a newer generation is already recorded on the Persona is proven to be a safe no-op that leaves the newer generation's metadata untouched",
    "no_fabricated_controller_receipts_or_safety_default_changes": "controller_record_ref values for a committed generation are still generated server-side only inside the real compare-and-set write path; the generation-0 view is still deterministic and never persisted, and is now additionally gated on the caller genuinely being the Persona's real owner; the approval verifier still fails closed (503) rather than fabricating a receipt when unconfigured"
  },
  "verified": "PANTHEON_DEPENDENCY_PYTHON=/home/chloe_ong_dev_cctech_support_com/code/pantheon/.venv/bin/python3 python3 scripts/dev/provision_python_distribution.py; then .venv-pantheon/bin/python3 -m pytest -q services/persona --ignore=services/persona/test_cron_ooda_closure.py --ignore=services/persona/test_cron_ooda_closure_live_smoke.py services/training-session/tests/test_persona_target.py services/control-plane/persona/test_main.py -- 94 passed, 3 subtests passed (services/persona/test_training_target_owner.py now has 12 tests: the prior 10 plus 2 new covering root review R2's crash-recovery and out-of-order-generation findings; all other services/persona tests, the frozen services/training-session/tests/test_persona_target.py suite, and services/control-plane/persona/test_main.py are unchanged and unaffected). services/persona/test_cron_ooda_closure.py and its live-smoke counterpart remain excluded: they fail to collect for a reason entirely unrelated to this diff (services/control-plane/cron/workflows.py using a relative `from .models import` outside its package context); this task does not touch cron_ooda_closure.py, workflows.py, or any of their import paths."
}
