# L12-CTRL-001 — Harden canonical loop-controller truth

Wave 1, lane `loop-controller`, owner `Claude`, reviewer `Codex2`; depends on `L12-FLEET-001` and external overlap owner `PPL-ALLOC-009`.

Outcome: one tenant-safe, concurrency-safe controller contract for all twelve loops, including fenced lease renewal and truthful desired/controller/downstream projection.

Scope: `services/loop-control`, the controller schema, BFF loop inventory/read model, contract tests, and this task's evidence directory.

Acceptance: no lost concurrent fields; stale writers rejected; normal writes renew leases; desired presence and downstream actual state are authoritative; authenticated tenant/environment scope is enforced.

Proof: Postgres concurrency, lease fencing, tenant isolation, and reusable all-field BFF/controller conformance tests. The full machine contract is canonical in `tasks.json`.
