# L12-SRC-001 — Reconcile supervised source due state

Wave 1, lane `source`, owner `Codex2`, reviewer `Codex`; depends on `L12-FLEET-001`.

Outcome: turn Persona source requirements into connector/schedule desired state while preserving deny-by-default egress, allowlists, and rate limits.

Scope: `services/source_ingestion` and this task's evidence directory.

Acceptance: automatic idempotent desired-state creation; safe duplicate/two-worker behavior; missed-tick recovery; isolated provider failures; truthful SourceHealth categories.

Proof: concurrency, restart, provider-failure matrix, policy denial, and a bounded real-provider dev drill. The full machine contract is canonical in `tasks.json`.
