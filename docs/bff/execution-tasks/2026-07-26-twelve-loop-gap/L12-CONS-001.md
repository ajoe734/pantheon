# L12-CONS-001 — Deploy the Consultation executor

Wave 1, lane `consultation`, owner `Codex`, reviewer `Codex2`; depends on `L12-FLEET-001`.

Outcome: a supervised durable committee/red-team executor using real qualified contributions, tenant authority, leased work, bounded blocking, and unique handoff.

Scope: `services/consultation` and this task's evidence directory.

Acceptance: real provider contribution; durable claim lease; bounded blocked/DLQ states; one memo and handoff under concurrency; acknowledged recovery after crash.

Proof: real service-boundary, multi-executor race, phase crash recovery, auth/tenant, and DLQ replay tests. The full machine contract is canonical in `tasks.json`.
