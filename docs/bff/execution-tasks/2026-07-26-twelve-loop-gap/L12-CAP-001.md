# L12-CAP-001 — Make paper execution lossless and isolated

Wave 2, lane `capital`, owner `Antigravity`, reviewer `Claude`; depends on `L12-DEP-001` and `L12-TEL-001`.

Outcome: replace destructive queue pop with claim/ack visibility, requeue or DLQ every execution error, enforce binding/runtime/pool scope, and lease the reconciler leader.

Scope: Lean runtime execution, Capital services, and this task's evidence directory.

Acceptance: accepted signals survive crashes; errors remain recoverable; unscoped signals are rejected; exactly one reconciler owns each worker set; live capital stays disabled.

Proof: Redis crash-before-ack, execution-error retry/DLQ, two-reconciler lease, and six-binding restart/isolation drill. The full machine contract is canonical in `tasks.json`.
