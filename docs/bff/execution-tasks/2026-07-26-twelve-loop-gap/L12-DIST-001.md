# L12-DIST-001 — Make distillation transactional

Wave 2, lane `source`, owner `Claude`, reviewer `Antigravity`; depends on `L12-SRC-001`.

Outcome: replace unsafe JSONL with versioned source digests, durable inbox/outbox, leases, retries, and DLQ; Registry failure must be truthful.

Scope: `services/source_ingestion` and this task's evidence directory.

Acceptance: one job per committed normalized version; revised content is distinct; failure records controller degradation; immutable approvals never mutate; crash replay reaches one terminal draft.

Proof: real source-to-registry flow, two-worker revision case, Registry outage/replay, and approved-artifact immutability. The full machine contract is canonical in `tasks.json`.
