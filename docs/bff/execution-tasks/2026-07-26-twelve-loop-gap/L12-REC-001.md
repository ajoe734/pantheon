# L12-REC-001 — Harden reconciliation scheduling and recovery

Wave 1, lane `reconciliation`, owner `Codex`, reviewer `Codex2`; depends on `L12-FLEET-001`.

Outcome: finish scheduled reconciliation within a measured SLA using durable tenant-scoped records, reports, worker state, and leased ownership.

Scope: `services/reconciliation-drift` and this task's evidence directory.

Acceptance: bounded scheduler latency; no duplicate logical window with two schedulers/consumers; corrupt state fails closed; restart, retry, and replay do not duplicate incidents.

Proof: two-worker lease, timeout recovery, corrupt-state restart, tenant/auth, and incident-correlation tests. The full machine contract is canonical in `tasks.json`.
