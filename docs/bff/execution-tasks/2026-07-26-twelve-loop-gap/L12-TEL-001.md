# L12-TEL-001 — Make telemetry acknowledgement durable

Wave 1, lane `telemetry`, owner `Antigravity`, reviewer `Codex`; depends on `L12-FLEET-001`.

Outcome: eliminate the 202-before-durable-write loss window, preserve lifecycle trace identity across heartbeats, and make ingest/replay tenant-authorized.

Scope: `services/telemetry` and `docs/deployment/evidence/twelve-loop-gap/L12-TEL-001`.

Acceptance: success only after durable tenant-scoped ingest; heartbeats cannot erase identity; restart before flush loses no accepted event; read and DLQ replay enforce authority.

Proof: crash-before-flush, lifecycle/heartbeat regression, auth/tenant negatives, and current six-runtime summary readback. The full machine contract is canonical in `tasks.json`.
