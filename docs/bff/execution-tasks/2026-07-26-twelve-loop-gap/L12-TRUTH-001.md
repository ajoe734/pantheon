# L12-TRUTH-001 — Integrate twelve-loop operator truth

Wave 3, lane `operator-truth`, owner `Claude`, reviewer `Antigravity`; depends on `L12-CTRL-001`, `L12-MANIFEST-001`, and external overlap owner `PPL-ALLOC-009`.

Outcome: every loop writes a current canonical record and BFF exposes desired, controller, success/failure, actual state, and provenance without promoting unproved maturity.

Scope: loop catalog, BFF inventory/health models and tests, and this task's evidence directory.

Acceptance: all twelve tenant records; stale/absent/contradicted stays degraded; catalog fields match runtime; maturity stops at implemented until hosted proof is admitted.

Proof: all-loop conformance, stale/contradiction negatives, tenant BFF readback, and catalog/runtime exactness. The full machine contract is canonical in `tasks.json`.
