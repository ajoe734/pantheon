# L12-BFF-001 — Make BFF health monitoring durable

Wave 2, lane `bff-health`, owner `Antigravity`, reviewer `Claude`; depends on `L12-CTRL-001`, `L12-TEL-001`, and external overlap owner `PPL-ALLOC-009`.

Outcome: authoritative infrastructure telemetry, interval and error-rate triggers, complete target registry, durable replicated state, retry/DLQ, and incident recovery without a fake binding.

Scope: BFF downstream health monitor/main/contracts and this task's evidence directory.

Acceptance: valid infrastructure admission; complete probes; restart/replica dedupe; shared event IDs through telemetry and incidents; recovery resolves or updates the incident.

Proof: real admission, error-rate trigger, two-replica restart, and target stop/recovery drill. The full machine contract is canonical in `tasks.json`.
