# L12-DEP-001 — Lease and authenticate Deployment

Wave 1, lane `deployment`, owner `Codex2`, reviewer `Codex`; depends on `L12-FLEET-001`.

Outcome: tenant-authenticated mutations plus transactional outbox claim/ack, compensation, and truthful idle recovery for the Deployment saga.

Scope: `services/deployment`, `services/promotion`, and this task's evidence directory.

Acceptance: two consumers never own one event; crash after RuntimeBinding side effect creates no duplicate; retry/DLQ/compensation are visible; correlation survives every stage.

Proof: two-consumer, side-effect-before-ack crash, auth/tenant negative, idle recovery, and terminal downstream readback. The full machine contract is canonical in `tasks.json`.
