# SA/SD: verified same-delivery archive resurrection recovery

Task: OPS-ARCHIVE-RESURRECTION-CONTRACT-001
Operator approval: 2026-09-06, explicit agreement to extending the existing TaskStore import/reconciliation contract for proven role-only stale resurrection. The operator requested ordinary supervisor execution, not direct chatbox implementation.
Evidence: accompanying RECONCILIATION.md; immutable source files must be copied into the repository by this task and their original byte digests recorded.

## SA: cause, scope and authority

PPL-ALLOC-007 has a completed immutable generation-1 archive and exact merged source delivery, but an active generation-2 row with the same scoped contract. A genuinely audited 2026-09-05 role reassignment says evidence recovery, not new product development. The current same-delivery recovery rejects unequal generations; the separate replacement-delivery collision action is intentionally inapplicable. Merely deleting a generation comparison, modifying the archive or marking the active generation done would erase a real authority boundary.

The evidence also has historical reviewer drift and a resurrected row. Verify the complete authoritative sequence, including how the active row re-entered the store and every relevant role/generation transition; one terminal-quota message or identical task names is not enough. If a necessary source journal/audit link cannot be authenticated, report its exact missing identifier and keep that target unchanged. Do not invent a path across an incomplete chain.

Repair the existing recovery contract and the actual re-entry boundary. Keep one V2 TaskStore, one immutable task archive, existing audited reassignment verification, existing local status CLI, and the existing transaction/outbox/receipt pipeline. Pure proof logic should have one authoritative implementation shared by preflight and application. Do not add another reconcile command family, queue, recurring cron, event journal, terminal registry, source of truth or generic framework. Existing strict default generation fencing remains for genuine new work and unproved collisions. No compatibility endpoints or parallel old/new policy implementations.

This task is functional development tooling: source, isolated fixtures/process tests, exact-head review and repository delivery. It authorizes no real MFA assertion, credential creation/rotation, product API writes, hosted acceptance, deployment to a product VM, live trading or capital action. Task state maintenance uses qualified local Human/Ops; never hand-edit task or queue JSON.

## SD: proof and transaction contract

1. Re-audit current dev and qualified runtime before implementation. Locate all actual entry points capable of importing/reassigning/recovering terminal IDs. The current seed command already emits only empty genesis and refuses nonempty or ambiguous journals; preserve it and do not introduce projection-based recovery. Distinguish current defect from historical retired import behavior; do not build a new import system to fix obsolete code.
2. Build on `validate_merged_done_evidence`, `_validated_same_delivery_archive_recovery`, existing authenticated reassignment helpers, archive snapshot validation and TaskStore transition guards. Compare exact task ID, repository identity, merged delivery/review binding, accepted contract/provenance, artifacts, acceptance, dependencies/tracks, class and any current privileged execution policy. Resolve real branch ancestry in the correct repository; historical paths are not current roots.
3. For cross-generation stale resurrection, require authenticated, ordered and complete import/role/generation lineage from the archived completed delivery to the exact current row; reject gaps, forks, forged/unaudited events, ambiguous chronology, intervening changed scope, changed delivery, genuine reopen/new work or invalidation. Mere owner reassignment normally creates a new generation and never generally licenses reuse of old approval. Model precisely the narrow proven stale-row retirement exception, not a generation-agnostic done operation. Preserve strict same-generation behavior and distinct-replacement behavior where those existing contracts actually apply.
4. Preflight must bind the current task/generation/contract/state revision, archive bytes/digest, real delivery evidence and audit-head/proof range. Under the existing authoritative lock/transaction, revalidate these bindings and lack of active worker/lease/reserved or queued launch for the exact target. Any concurrent reassign, new scope, grant/claim, archive or proof change must abort before effects. Fail closed on unresolved attempts; do not kill arbitrary workers as a workaround.
5. Retire only the proven stale active row through the existing outbox/readback/receipt ordering. Retain the exact original archive bytes and original completed terminal fact/generation, with an append-only recovery event describing the retired active row digest/generation and complete proof. Never fabricate a generation-2 completion, reviewer attestation, new merge or replacement task. Existing consumers must resolve the original terminal identity consistently without a new registry or a blanket generation reset.
6. The operation must be idempotent and crash-safe before/after authoritative commit, archive readback, receipt and derived-view refresh. A retried operation with the same proof converges; changed proof or scope cannot reuse a success. Verify archived bytes and terminal identity after restart and preserve fail-closed state on any uncertain partial result. Do not delete audit history or rebuild canonical truth from ai-status.json.
7. Prevent the same completed ID from being resurrected again at the actual supported admission/recovery/reassignment boundary, using existing archive/terminal identities and stores. New genuine work must not be silently discarded as historical duplication. Guard privileged task policies and pending authorization introduced by OPS-PRIVILEGED-TASK-EXECUTION-AUTH-001; reconciliation cannot acquire or clear execution authority as a side effect.
8. Extend existing redacted local CLI/status explanation and runbook to show eligibility, exact proof rejection, retired stale generation versus original completed generation, and actual recovery receipt. Do not merge the existing source-signing and MFA trust domains or demand a product login for task maintenance.

## Scope and serialization

This is the only new owner of the narrow archive-resurrection repair. It does not replace or reimplement OPS-PRIVILEGED-TASK-EXECUTION-AUTH-001, OPS-LEGACY-REVIEW-RETIRE-001, prior merged archive reconciliation work, or any BFF/FE product task. It depends on OPS-LEGACY-REVIEW-RETIRE-001 and OPS-PRIVILEGED-TASK-EXECUTION-AUTH-001 so shared local-status, dispatch and review boundaries are not edited concurrently. Refresh current dev after both predecessors are genuinely accepted; preserve their trust and single-writer architecture.

Use only the packet's exact artifact grants. If source inspection proves another current entry point must change, propose the smallest governed artifact-contract addition and inspect its concurrent owner first. No broad `.orchestrator/**`, `scripts/**`, BFF or FE edit grants. Shared root is dirty and live: use the prepared clean task worktree and never stage root runtime/projection/archive changes.

## Validation and evidence

Required positive fixture: original completed g1 archive, real fixture git merge and immutable review evidence, complete authenticated recovery/role chain to otherwise identical stale g2, no active attempt. Real local CLI and existing TaskStore/outbox must retire only stale g2, preserve archive bytes and original g1 terminal identity, and resolve dependencies consistently. Synthetic identities/keys stay in isolated fixtures and are never live trust material.

Required negatives: missing/untrusted/forked/reordered lineage; ambiguous import; changed acceptance/artifacts/dependencies/class/repository/policy; real new-generation work; wrong merge/branch/review/repository; corrupt archive; unauthenticated operator; stale state/proof digest; active worker/lease/queued launch; privileged pending/granted task drift. Each must have zero unauthorized task retirement, terminal fact, archive change and worker launch. Test two real isolated processes racing recovery versus assign/claim and crash/retry/restart through actual persistence, not only a mock function returning an error string. Cover ordinary archive completion and the existing same-generation/replacement flows without weakening their guards.

Record exact commands, exits, counts, skips, immutable head/base, manifest blob, PR/check/review/merge and source hashes. Evidence must distinguish isolated proof, source acceptance, qualified tooling-runtime acceptance and actual live target reconciliation. Do not call sequential in-memory tests cross-process proof. Missing required acceptance cannot be relabeled a passing residual risk.

## Delivery and bounded live reconciliation handoff

Commit source, this plan, the factual diagnosis and exact evidence through the repository workflow; retain an independent reviewer and exact-head checks. No direct tooling-delivery bypass was requested. If configured runtime promotion is coordinator-owned, provide the exact qualified promotion handoff and mark the live portion pending rather than pretending source merge is runtime acceptance.

After accepted source and qualified tooling runtime, the coordinator may use only the existing extended Human/Ops command to reconcile PPL-ALLOC-007 if the full real proof passes. Recheck all target evidence and no active attempts immediately before the action. Preserve archive SHA256 `cf95cc79c44a027f42046f83572029d07563a207fa1cc66a4685b755f7ce4c78` or stop to explain an independently authenticated change. Record exact canonical and archive readback, recovery event/receipt, dependency resolution and zero unintended launches. If full real lineage cannot be proved, preserve the hold and state exactly the unavailable authority/evidence; never fabricate a successful live recovery.

PPL-ALLOC-009 remains hosted-closeout evidence only, temporarily Human/Ops-owned, with no BFF/FE edit grants. Do not restore a runnable owner or execute hosted work merely because PPL-ALLOC-007 becomes resolved. Migration to typed pending authorization and any real execution grant belong to the existing execution-authorization contract, preserving all acceptance. Keep all three original future hosted IDs and all twelve-loop/Management/Agora/exact-pair/rollback requirements unchanged.

Report this dependency-serialized repair to the existing SYS-SIMPLIFY-IMPLEMENTATION-CLOSURE-001 owner as additional factual closeout evidence, not a second global join or fake dependency. No claim of complete product readiness or complete catalog deduplication until the original required work actually passes.
