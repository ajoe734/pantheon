# SA/SD — one authoritative worker responsibility-transition classification

Task: OPS-REVIEW-HANDOFF-RECOVERY-CONTRACT-001

Operator decision: 2026-09-06, the current chatbox reply「同意」to the proposed single supervisor/worker termination-classification repair, serialized after the existing Archive work. This is normal supervisor/auto-worker implementation, not a direct chatbox implementation bypass. The decision does not approve unrelated Retrieval owner/schema changes, OpenClaw cron consolidation, credential operations, or hosted execution.

## SA: reproduced structural failure

The accepted live runtime is e9d1a1e50b4f098db6e62c1c4e247f1f40f36827; current dev was independently fetched at a9557a6002e8170eb92415cc61e9d6a584cc610f. A legitimate exact reviewer `reopen` transfers responsibility back to the owner. Active lease classification recognizes that lifecycle event and can stop the reviewer process. The runner truthfully records SIGTERM / exit 143. Post-exit classification then ignores `reopen` and/or accepts only exit 0. Generic lost-lease recovery consequently fences and increments generation, removes the generation-bound requeue intent, and overwrites current reviewer instructions before recognizing that responsibility had already moved.

The review findings remain in canonical handoffs/history. This is an inconsistent transition-classification and presentation contract, not missing storage. It also must not be treated as an automatic approval: a completed reviewer attempt can have a negative product decision and an in-progress task.

Most recent confirmed example: Registry reviewer run codex-20260906T151222Z-c4a97359 rejected exact 3f7e9551 at 15:17:07 UTC, g19. Exact reopen termination at 15:17:18 was followed by lost-lease-bd992815747a5b9fae23aa6b62c745394f0199ff589570452e587e901617aee2 at 15:17:30, g20; recovery released the obsolete reviewer fence and replaced next. See RECHECK.md for source/event/runner evidence and limits.

## Ownership and boundaries

One new narrow task owns this defect. OPS-LEGACY-REVIEW-RETIRE-001 is completed and archived; do not reopen it or claim its legacy-consumer retirement repaired this classifier. OPS-ARCHIVE-RESURRECTION-CONTRACT-001 owns a different completed-archive/stale-row proof problem and is still under review/repair. This task depends on it so shared supervisor, status, tests and documentation are not edited concurrently. Rebase on current dev after its accepted delivery; retain its proof and authorization checks.

Keep one V2 TaskStore and its existing transaction, canonical lifecycle/audit events, review decision/requeue intent, worker recovery receipts, queue and runner markers. Do not add a service, daemon, cron, journal, parallel state registry, compatibility alias, repair loop, or second approval path. Do not fix only next-string preservation or blanket-map exit 143 to success. Source edits are limited to the packet's exact grants in the prepared clean worker worktree, never the dirty shared root. An additional proven source requirement needs the existing governed artifact-contract flow and overlap check.

## SD: single classification contract

1. Re-audit every caller of the existing active-lease, exited-worker, startup-reconciliation and lost-lease classification. Inventory actual imports/callers before removing contradictory branches. Use one authoritative validated responsibility-transition result in the existing implementation, rather than copying policy into each poll/recovery path. Prefer the existing supervisor/rewrite worker-recovery boundaries; this is not a general framework or wholesale supervisor rewrite.
2. Separate process outcome from responsibility outcome and task acceptance. Preserve the truthful runner exit/signal. A valid reviewer reopen completes that exact review attempt, retains the rejection and in_progress owner responsibility, and does not approve/complete the task or issue a new source delivery.
3. Require authentic committed lifecycle evidence tied to exact task, generation, run/queue identity, PID start ticks/process generation, dispatched role, configured actor and delivery/review identity as applicable. Validate chronology and the latest relevant transition; stale, forged, replayed, superseded or mismatched evidence cannot complete another worker or bypass a newer claim. Current status or owner names alone are insufficient. Preserve distinct configured identities and live authentication rules.
4. Decide a legitimate exact responsibility transfer before creating a generic lost-lease fence. Apply this consistently to normal polling, supervisor restart/boot reconciliation and recovery replay. Existing review_decision_intent reservation/external-write/finalize fencing remains authoritative; an uncommitted or partially finalized decision is not a completed transfer.
5. Revalidate state, event, generation and exact attempt inside the existing lock/CAS boundary immediately before mutation. Preserve existing lock order and no blocking/network calls under canonical locks. A concurrent reassign, new review, claim, scope change, terminal transition or proof drift must produce the existing safe retry/refusal, never delete new work. Real process loss, crash or progress/lease expiry without valid transfer must still fence and recover under the original contract.
6. Reuse canonical handoffs and review_requeue_intent for current actionable reviewer findings. Recovery bookkeeping must not erase them or cause a stale rejection/head to replace a newer one. Preserve exact provenance and any independently authorized current clarification. Do not create another findings store or recurring note-restoration job. Prove what the successor owner actually receives, not merely that history retains text.
7. Existing pending/resolved recovery receipts created by the old classifier remain auditable. Safely converge only a provably obsolete exact-attempt fence through existing receipt/replay operations, without blanket removal, generation reset, archive edits, approval carry, or killing unrelated processes. This is not an archive resurrection exception; reuse the predecessor's strict accepted contract.
8. Privileged pending authorization, execution resources, exact attempt grants, sandbox barriers and reviewer independence are unchanged. Recovery cannot mint, clear, inherit or reacquire execution authority. Functional isolated tooling tests need no product login or real MFA credential. No hosted/product/production/capital actions are authorized by this packet.

## Required acceptance matrix

- Reproduce at least two recorded legitimate reopen sequences, including Registry g19 at 15:17, against the old classifier and demonstrate the corrected outcomes against the candidate. Preserve recorded negative review and runner exit; no unnecessary generation increment, lost-lease receipt or lost/replayed duplicate owner dispatch.
- Run a real isolated process/CLI/TaskStore/outbox flow: reviewer commits a genuine fixture reopen, exact reviewer runner is stopped or exits, supervisor polling and restart converge, and the existing owner queue/work packet receives the latest findings once. An in-memory call that returns the expected string is not process proof. Fixture identities/keys belong only to the disposable fixture, never live trust configuration.
- Exercise normal owner handoff, reviewer approval, reviewer rejection/reopen, done, owner blocker/clarification and role reassignment. Only proven authorized transitions release responsibility; no blanket success for statuses or exit codes. Keep full strict negatives for wrong actor, role, task generation, PID/start ticks, run/queue identity, stale event/head, altered digest, absent event and forged authority.
- Exercise true crash, progress timeout, lease expiry, exit 143 without a committed matching transfer, missing process with ambiguous evidence, and pending review-decision intent. Existing loss fencing, resource reservation and replay protections must still work.
- Use real two-process races for reopen/finalize versus generic recovery/claim/reassign, and crash windows before/after canonical commit, outbox, runner stop and queue receipt. Show retry/restart idempotency, no duplicate source writer, no stale-generation command admission and no archive or privileged authority mutation. Record which cases are real processes and which are unit tests.
- Run focused supervisor/recovery/runner/local-status tests plus predecessor-sensitive archive, TaskStore, dispatch and execution-authorization regressions. Record exact commands, terminal exit/counts/skips/timeouts, base/head, evidence manifest blob, source/document digests and required checks. A defect-reproduction script exiting 0 is not a passing candidate test; a timeout or skipped required case is not acceptance.
- Retire conflicting classification implementations and obsolete tests/docs together; show remaining callers all use the same authoritative policy. No new/old parallel API or fallback policy for a first release.

## Delivery, activation and closeout

Copy this immutable plan and RECHECK.md into the declared repository documentation, preserving original byte hashes in evidence.json; update the existing first-release INDEX and local tooling runbook. Deliver scoped source, tests and docs through validated commit, branch push, genuine independent exact-head review, required checks and dev merge. Do not fabricate review or proof tags. Register findings with the existing SYS-SIMPLIFY-IMPLEMENTATION-CLOSURE-001 owner; do not create a second global join or claim a note is a canonical dependency change.

Provide the coordinator an exact qualified tooling-runtime promotion handoff. Runtime acceptance must verify immutable command SHA, dedicated Python, canonical roots, existing watchdog and single integrator cron bindings, auth/permission barriers, exact worker identities and no unintended launches. Preserve current workers through the supported promotion path. Use a new isolated post-promotion replay/probe and actual qualified dispatch evidence without deliberately rejecting real product work. Distinguish source merged, tooling activated and verified behavior from product readiness. If a boundary remains unproved, retain that specific hold; do not call all 12 loops, Management or Agora complete.
