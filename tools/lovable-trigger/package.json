{
  "name": "pantheon-lovable-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Local browser trigger that sends prompt files into an existing Lovable project chat using a persistent Playwright session.",
  "type": "module",
  "scripts": {
    "bootstrap": "node send_prompt.mjs bootstrap",
    "send": "node send_prompt.mjs send",
    "batch": "node send_prompt.mjs batch"
  },
  "dependencies": {
    "playwright-core": "^1.55.0"
  }
}
