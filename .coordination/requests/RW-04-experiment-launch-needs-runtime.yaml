feature_id: RW-04-experiment-launch
type: needs-runtime
source_repo: pantheon
target_repo: ajoe734/front-ai-trading-system
screen: experiment-launch
status: completed
worker_kind: runtime-worker
requested_by: front-sync-worker
created_at: "2026-04-21T04:44:06Z"
resolved_at: "2026-04-21T16:50:45Z"
resolved_by: Codex
summary: >-
  The active operator-bff runtime was revalidated over live HTTP and now
  publishes the full RW-04 route family again. `/openapi.json` on
  http://127.0.0.1:18001 advertises GET /api/v1/experiments,
  POST /api/v1/experiments/launch,
  GET /api/v1/experiments/{experiment_id}, and
  POST /api/v1/experiments/{experiment_id}/cancel. Authenticated live probes
  return truthful degraded list/detail envelopes plus queued, running,
  completed, failed, canceled, and OBJECT_NOT_FOUND behavior, and a
  workspace-backed HTTP probe launched with BFF_READ_SURFACE_STATE=unavailable
  returns truthful unavailable list/detail envelopes for the same routes.
next_step: >-
  Hand control back to Pantheon review / front-sync so RW-04 can close from
  current live-route truth. Remaining follow-up is front publication from one
  immutable Git-visible commit plus non-blocking deployed browser QA.
verification:
  runtime_url: http://127.0.0.1:18001
  unavailable_probe_url: http://127.0.0.1:18012
  commands:
    - pytest RW-04 contract slice
    - curl live OpenAPI on 18001 for the four RW-04 methods
    - curl live degraded list on 18001 under operator auth
    - curl live degraded detail on 18001 for completed, running, failed, and missing experiment ids
    - curl live launch on 18001 under operator auth and cancel the queued experiment
    - launch workspace-backed uvicorn on 18012 with BFF_READ_SURFACE_STATE=unavailable
    - curl HTTP unavailable list on 18012 under operator auth
    - curl HTTP unavailable detail on 18012 under operator auth
  results:
    - pytest returned 21 passed
    - live OpenAPI on 18001 advertises get/post on list/launch/detail/cancel for RW-04
    - live list probe on 18001 returned 200 with experiment_history degraded and seeded completed/running/failed rows
    - live detail probe on 18001 returned 200 completed exp-20260419-012 with canCancel false, 200 running exp-20260418-009 with canCancel true, 200 failed exp-20260417-004 with failure.reason_code MISSING_DATA_PARTITION, and 404 OBJECT_NOT_FOUND for does-not-exist
    - live launch on 18001 returned queued exp-20260421-004, cancel returned status canceled with canCancel false, and follow-up detail/list probes confirmed the terminal canceled state
    - unavailable list probe on 18012 returned 200 with ids [], total 0, and experiment_history unavailable
    - unavailable detail probe on 18012 returned 200 with experiment_status unavailable while preserving the backend-owned run snapshot
