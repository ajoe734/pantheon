feature_id: PKT-002-incident-detail
type: contract-ready
source_repo: ajoe734/pantheon
screen: incident-detail
status: published
published_at: "2026-04-14T13:30:00Z"
description: "PKT-002 Incident Detail contract-ready handoff. BFF contract, example payload, screen spec, and Lovable task packet are all published."
artifacts:
  bff_contract: docs/bff/PKT-002-incident-detail.md
  screen_spec: docs/screens/PKT-002-incident-detail.md
  example_payload: docs/examples/PKT-002-incident-detail.json
  lovable_ui_task: .coordination/responses/PKT-002-incident-detail-lovable-ui-task.yaml
endpoints:
  - GET /api/v1/operator/incident-response/{incident_id}
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
front_actions_required:
  - build the Incident Detail composed view panel from GET /api/v1/operator/incident-response/{incident_id}
  - use only the existing BFF client
  - do not add raw fetch calls in component files
  - do not invent fields beyond this handoff packet
  - render all CTAs from backend-shaped allowedActions only
  - render each degraded surface with explicit named copy; never show a generic empty state
  - display the degradation banner when any meta.surfaces entry is degraded or unavailable
acceptance_met:
  - contract_ready_published: true
  - lovable_ui_task_published: true
  - front_repo_receives_handoff_bundle: true
