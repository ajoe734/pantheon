feature_id: TW-04-teaching-replay
type: contract-ready
source_repo: ajoe734/pantheon
screen: teaching-replay
status: live
published_at: "2026-04-20T12:45:00Z"
task: TW-04-REPLAY-001
description: >-
  TW-04 Teaching Replay contract and BFF route family are live. The replay list
  (GET /api/v1/trainer/replay), replay detail (GET /api/v1/trainer/replay/{session_id}),
  commit route (POST /api/v1/trainer/sessions/{session_id}/commit), discard
  route (POST /api/v1/trainer/sessions/{session_id}/discard), BFF-resolved
  evidence links, replay-grade TeachingEvent schema, and frontend handoff
  bundle are aligned with the current Pantheon implementation. Lovable UI work
  may start. Depends on TW-01 for session_id and lifecycle semantics.
artifacts:
  bff_contract: docs/bff/TW-04-teaching-replay.md
  screen_spec: docs/screens/TW-04-teaching-replay.md
  example_payload: docs/examples/TW-04-teaching-replay.json
  lovable_ui_task: .coordination/responses/TW-04-teaching-replay-lovable-ui-task.yaml
  frontend_change_spec: docs/pantheon-handoffs/TW-04-teaching-replay/FRONTEND_CHANGE_SPEC.md
endpoints:
  - GET /api/v1/trainer/replay
  - GET /api/v1/trainer/replay/{session_id}
  - POST /api/v1/trainer/sessions/{session_id}/commit
  - POST /api/v1/trainer/sessions/{session_id}/discard
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
dependencies:
  - TW-01-FOUNDATION-001
  - TW-03-COMPARE-001
readiness_gate: >-
  BFF route family confirmed live at 2026-04-20T12:45:00Z. All four TW-04
  routes return the published replay field shape and the frontend handoff
  bundle is published. Frontend may begin production implementation.
  Commit/discard authority must come from the detail response allowedActions,
  and commit/discard submissions must go through the existing BFF client action
  layer.
front_actions_required:
  - list replayable sessions from GET /api/v1/trainer/replay with persona_id (required), status, pagination params
  - render replay detail from GET /api/v1/trainer/replay/{session_id}
  - render ordered TeachingEvent[] timeline from the detail response events[] array
  - submit commit through POST /api/v1/trainer/sessions/{session_id}/commit via the existing BFF client action layer only
  - submit discard through POST /api/v1/trainer/sessions/{session_id}/discard via the existing BFF client action layer only
  - surface commit/discard CTAs only when allowedActions.canCommit / allowedActions.canDiscard is true
  - resolve evidence links from BFF-provided `event.evidence_ref` objects — do not construct them client-side
  - render replay_resolution.state correctly (pending_decision / committed / discarded / not_applicable)
  - emit a bff-gap handoff if any required field is absent
acceptance_met:
  contract_ready_published: true
  bff_replay_list_route_live: true
  bff_replay_detail_route_live: true
  bff_commit_route_live: true
  bff_discard_route_live: true
  bff_route_live_at: "2026-04-20T12:45:00Z"
  lovable_ui_task_published: true
  frontend_change_spec_published: true
  front_repo_receives_handoff_bundle: true
