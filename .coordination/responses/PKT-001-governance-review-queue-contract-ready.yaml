feature_id: PKT-001-governance-review-queue
type: contract-ready
source_repo: ajoe734/pantheon
screen: governance-review-queue
screen_id: screen-governance-review-queue
workbench: governance-workbench
status: published
published_at: "2026-04-24T04:50:19Z"
description: >-
  PKT-001 Governance Review Queue handoff refreshed against the live Pantheon
  BFF route. `GET /api/v1/operator/governance/review-queue` and
  `POST /api/v1/operator/commands` remain the only approved surfaces, and the
  queue payload confirms `allowedActions.canForwardToApproval`,
  `allowedActions.canRequestChanges`, `allowedActions.canEscalate`,
  `meta.surfaces.review_queue`, and `meta.surfaces.allowedActions`.
artifacts:
  bff_contract: docs/bff/PKT-001-governance-review-queue.md
  screen_spec: docs/screens/PKT-001-governance-review-queue.md
  example_payload: docs/examples/PKT-001-governance-review-queue.json
  backend_delivery: .coordination/responses/PKT-001-governance-review-queue-backend-delivery.yaml
  lovable_ui_task: .coordination/responses/PKT-001-governance-review-queue-lovable-ui-task.yaml
  frontend_change_spec: docs/pantheon-handoffs/PKT-001-governance-review-queue/FRONTEND_CHANGE_SPEC.md
endpoints:
  - GET /api/v1/operator/governance/review-queue
  - POST /api/v1/operator/commands
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - backend_delivery
  - lovable_ui_task
  - example_payload
  - frontend_change_spec
readiness_gate: >-
  The governance review queue route family is live and returning the published
  field shape. If the current front branch still lacks fail-closed validation
  for the required `meta.surfaces.review_queue` and
  `meta.surfaces.allowedActions` keys, treat that as a front-owned refresh and
  republish the feedback bundle instead of reopening Pantheon BFF scope.
front_actions_required:
  - build or refresh the Governance Review Queue list and item detail drawer against the existing BFF client
  - use only the existing BFF client
  - do not add raw fetch calls in component files
  - do not invent fields beyond this handoff packet
  - render all routing CTAs from backend-shaped allowedActions only
  - pass filters to the BFF as query parameters; do not filter client-side
  - treat meta.surfaces.review_queue and meta.surfaces.allowedActions as required and fail closed if either is absent
  - display the degradation banner when any meta.surfaces entry is degraded or unavailable
acceptance_met:
  contract_ready_published: true
  bff_route_live: true
  lovable_ui_task_published: true
  front_repo_receives_handoff_bundle: true
bff_spec_path: docs/bff/PKT-001-governance-review-queue.md
ui_spec_path: docs/screens/PKT-001-governance-review-queue.md
frontend_change_spec_path: docs/pantheon-handoffs/PKT-001-governance-review-queue/FRONTEND_CHANGE_SPEC.md
examples:
  - docs/examples/PKT-001-governance-review-queue.json
