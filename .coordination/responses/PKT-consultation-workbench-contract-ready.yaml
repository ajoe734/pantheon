feature_id: PKT-consultation-workbench
type: contract-ready
source_repo: ajoe734/pantheon
screen: consultation-workbench-overview
status: published
published_at: "2026-04-18T12:00:00Z"
description: "Consultation Workbench overview packet is contract-ready. Pantheon now publishes a truthful overview route, screen spec, and example payload as an overview surface only; module-level truth remains with the per-module contracts and current backlog state."
artifacts:
  bff_contract: docs/bff/PKT-consultation-workbench.md
  screen_spec: docs/screens/PKT-consultation-workbench.md
  example_payload: docs/examples/PKT-consultation-workbench.json
  lovable_ui_task: .coordination/responses/PKT-consultation-workbench-lovable-ui-task.yaml
endpoints:
  - GET /api/v1/workbench/consultation
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
front_actions_required:
  - build the Consultation Workbench landing page from the single overview route
  - render module order, support refs, and next steps only from backend-owned payload fields
  - do not invent request forms, committee boards, or red-team memo UI in this packet
acceptance_met:
  - contract_ready_published: true
  - lovable_ui_task_published: true
  - front_repo_receives_handoff_bundle: true
bff_caveats:
  - this packet is an overview surface only
  - this packet must not flatten module truth: `CW-01` and `CW-03` are loop-complete for the current wave, while `CW-02` and `CW-04` both have live route families plus published frontend activation packets
