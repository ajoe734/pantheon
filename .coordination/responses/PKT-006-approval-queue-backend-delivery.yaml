feature_id: PKT-006-approval-queue
type: backend-delivery
target_repo: ajoe734/front-ai-trading-system
workbench: governance-workbench
screen_id: screen-governance-approval-queue
status: blocked
backend_commit: b774358263a02116887301a605d5d54b4b1aae77
bff_contract_version: pantheon-bff@b774358263a02116887301a605d5d54b4b1aae77
delivery_note_path: docs/pantheon-delivery/PKT-006-approval-queue/DELIVERY_NOTE.md
contract_lock_path: docs/pantheon-delivery/PKT-006-approval-queue/CONTRACT_LOCK.json
followup_expectation: >-
  Pantheon re-reviewed the returned PKT-006 ui-done handoff against the
  published contract, example payload, mirrored frontend-feedback request, the
  sibling front implementation, and the local Pantheon BFF app. The reviewed UI
  slice is statically aligned on shared-client reads, backend-shaped
  allowedActions, server-backed filters, degradation gating, and embedded
  drawer context, but the loop is blocked on two Pantheon-owned BFF gaps plus
  two front-owned follow-ups: GET /api/v1/operator/governance/approval-queue
  currently returns 404 from the local FastAPI app, POST
  /api/v1/operator/commands currently rejects ApproveDecision, RejectDecision,
  and RequestApprovalRevision with 422 enum validation errors, the advertised
  source_commit 0942961a7e31bdfa5adddddf2d31d72b41b141a9 resolves to request
  payloads that still say source_commit: pending, and the current UI still
  treats a pending decision with canApprove=false and canReject=false as a
  normal read-only state instead of surfacing the contract-gap condition
  required by docs/screens/PKT-006-approval-queue.md. The frontend-feedback
  request is mirrored into Pantheon, and the feedback docs are also present
  locally under docs/pantheon-feedback/PKT-006-approval-queue/. No new
  endpoints or shadow state are authorized. Pantheon must implement the
  published read route and command support on the existing surfaces first;
  after that, the front repo must republish a replay-clean
  frontend-feedback/ui-done pair from a truthful Git-visible commit that also
  closes the pending-decision guardrail before PKT-006 can move to
  loop-complete.
source_payload: .coordination/requests/PKT-006-approval-queue-ui-done.yaml
