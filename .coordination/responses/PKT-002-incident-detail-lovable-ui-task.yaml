feature_id: PKT-002-incident-detail
type: lovable-ui-task
project: ajoe734/front-ai-trading-system
status: closed
cycle: 2
pantheon_pr: null
base_url: null
workbench: operator-console
screen: incident-detail
screen_id: screen-operator-incident-detail
ui_spec_path: docs/screens/PKT-002-incident-detail.md
frontend_change_spec_path: docs/pantheon-handoffs/PKT-002-incident-detail/FRONTEND_CHANGE_SPEC.md
allowed_endpoints:
- GET /api/v1/operator/incident-response/{incident_id}
constraints:
- use existing bff client only
- do not add raw fetch in components
- do not import demo providers
- if any required field is missing, emit a bff-gap handoff instead of mocking
- publish ui-done from a transport-replayable front-repo commit that contains the
  returned handoff payload
- republish the mirrored feedback bundle from the same Git-visible front commit;
  do not leave the request or feedback files untracked
- wire the Open Action Drawer CTA into the delivered PKT-002 drawer surface, or
  narrow the published route/integration boundary so the handoff stays truthful
acceptance:
- build the Incident Detail composed view panel from GET /api/v1/operator/incident-response/{incident_id}
- use only the existing BFF client
- do not add raw fetch calls in component files
- do not invent fields beyond this handoff packet
- render all CTAs from backend-shaped allowedActions only
- render each degraded surface with explicit named copy; never show a generic empty
  state
- display the degradation banner when any meta.surfaces entry is degraded or unavailable
- render a non-dismissable staleness banner when meta.staleness is present
- render data.kill_switch.active_commands[] in the kill-switch surface
- republished ui-done and feedback artifacts truthfully document the current
  `/incidents/:incidentId` -> `/incident-action-drawer` integration boundary and
  touched file list
- if `HardRollback` remains disabled from Incident Detail, keep the
  target-artifact follow-up explicit in the mirrored API gap request instead of
  guessing the command parameter locally
required_feedback:
- docs/pantheon-feedback/PKT-002-incident-detail/LOVABLE_CHANGE_FEEDBACK.md
- docs/pantheon-feedback/PKT-002-incident-detail/API_GAP_REQUESTS.json
- docs/pantheon-feedback/PKT-002-incident-detail/UI_DECISIONS.md
- docs/pantheon-feedback/PKT-002-incident-detail/QA_STATUS.md
delivery_dependencies:
- .coordination/responses/PKT-002-incident-detail-contract-ready.yaml
links:
  lovable_project_url: null
  bff_spec_path: docs/bff/PKT-002-incident-detail.md
  ui_spec_path: docs/screens/PKT-002-incident-detail.md
  frontend_change_spec_path: docs/pantheon-handoffs/PKT-002-incident-detail/FRONTEND_CHANGE_SPEC.md
  example_payload_paths:
  - docs/examples/PKT-002-incident-detail.json
  handoff_bundle_dir: docs/pantheon-handoffs/PKT-002-incident-detail
gap_handoff_path: .coordination/requests/PKT-002-incident-detail-bff-gap.yaml
gap_handoff_template: .coordination/requests/PKT-002-incident-detail-bff-gap.example.yaml
completion_handoff_path: .coordination/requests/PKT-002-incident-detail-ui-done.yaml
completion_handoff_template: .coordination/requests/PKT-002-incident-detail-ui-done.example.yaml
review_findings_ref: .coordination/reviews/BP6-UI-REVIEW-001-review.md
