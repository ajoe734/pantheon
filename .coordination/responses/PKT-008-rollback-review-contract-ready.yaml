feature_id: PKT-008-rollback-review
type: contract-ready
source_repo: ajoe734/pantheon
screen: governance-rollback-review
status: published
published_at: "2026-04-16T15:35:00Z"
description: "PKT-008 Governance Rollback Review contract-ready handoff. BFF contract, example payload, screen spec, and Lovable task packet are all published."
artifacts:
  bff_contract: docs/bff/PKT-008-rollback-review.md
  screen_spec: docs/screens/PKT-008-rollback-review.md
  example_payload: docs/examples/PKT-008-rollback-review.json
  lovable_ui_task: .coordination/responses/PKT-008-rollback-review-lovable-ui-task.yaml
endpoints:
  - GET /api/v1/operator/rollback-review/{rollback_id}
  - POST /api/v1/operator/commands
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
front_actions_required:
  - build the Governance Rollback Review screen with rollback identity header, scope summary, position impact table, affected bindings panel, and trigger evidence drawer
  - use only the existing BFF client
  - do not add raw fetch calls in component files
  - do not derive position impact from raw binding or telemetry data; all position impact fields come from position_impact[] in the BFF response
  - render all CTAs from backend-shaped allowedActions only
  - disable the Approve CTA when meta.surfaces.position_data is degraded or unavailable regardless of allowedActions.canApproveRollback
  - show stale-data badge and unknown impact message for position_impact rows where position_data_stale is true
acceptance_met:
  - contract_ready_published: true
  - lovable_ui_task_published: true
  - front_repo_receives_handoff_bundle: true
