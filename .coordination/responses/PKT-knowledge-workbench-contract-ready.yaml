feature_id: PKT-knowledge-workbench
type: contract-ready
source_repo: ajoe734/pantheon
screen: knowledge-workbench-overview
status: published
published_at: "2026-04-18T12:00:00Z"
description: "Knowledge Workbench overview packet is contract-ready. Pantheon now publishes a truthful overview route, screen spec, and example payload as an overview surface only; module-level truth remains with the per-module contracts and current backlog state."
artifacts:
  bff_contract: docs/bff/PKT-knowledge-workbench.md
  screen_spec: docs/screens/PKT-knowledge-workbench.md
  example_payload: docs/examples/PKT-knowledge-workbench.json
  lovable_ui_task: .coordination/responses/PKT-knowledge-workbench-lovable-ui-task.yaml
endpoints:
  - GET /api/v1/workbench/knowledge
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
front_actions_required:
  - build the Knowledge Workbench landing page from the single overview route
  - render module order, support refs, and next steps only from backend-owned payload fields
  - do not invent registry, evidence-browser, or strategy-spec compare UI in this packet
acceptance_met:
  - contract_ready_published: true
  - lovable_ui_task_published: true
  - front_repo_receives_handoff_bundle: true
bff_caveats:
  - this packet is an overview surface only
  - this packet must not flatten module truth: `KW-01` remains hardening-gated, while `KW-02` to `KW-05` now have live BFF route families and published frontend handoff packets; remaining work is front-owned UI activation rather than net-new route implementation
