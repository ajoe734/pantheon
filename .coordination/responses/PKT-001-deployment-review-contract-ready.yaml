feature_id: PKT-001-deployment-review
type: contract-ready
source_repo: pantheon
screen: deployment-review-console
status: published
published_at: "2026-04-14T10:00:00Z"
description: "PKT-001 Deployment Review Console contract-ready handoff. BFF contract, example payload, screen spec, and Lovable task packet are all published."
artifacts:
  bff_contract: docs/bff/PKT-001-deployment-review-console.md
  screen_spec: docs/screens/PKT-001-deployment-review-console.md
  example_payload: docs/examples/PKT-001-deployment-review-console.json
  lovable_ui_task: .coordination/responses/PKT-001-deployment-review-lovable-ui-task.yaml
endpoints:
  - GET /api/v1/operator/deployment-plans
  - GET /api/v1/operator/deployment-review/{plan_id}
  - POST /api/v1/operator/commands
target_repo: ajoe734/front-ai-trading-system
handoff_bundle:
  - contract_ready
  - lovable_ui_task
  - example_payload
front_actions_required:
  - build the Deployment Review Console list and detail panels
  - use only the existing BFF client
  - do not add raw fetch calls in component files
  - do not invent fields beyond this handoff packet
  - render all CTAs from backend-shaped allowedActions only
  - display the degradation banner when any meta.surfaces entry is degraded or unavailable
acceptance_met:
  - contract_ready_published: true
  - lovable_ui_task_published: true
  - front_repo_receives_handoff_bundle: true
