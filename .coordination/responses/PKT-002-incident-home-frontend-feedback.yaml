feature_id: PKT-002-incident-home
type: frontend-feedback
source_repo: ajoe734/pantheon
reviewed_by: Codex
reviewed_at: '2026-04-24T11:06:34Z'
reviewed_commit: b1ee405d8c4e8096f73f1313092722a33ca3abff
reviewed_source_commit: 82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9
request_pair_commit: 1a1a42eebda033a1fbda4696df5b81271f5eed9b
ui_done_source_commit: 82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9
frontend_feedback_source_commit: 82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9
verified_front_workspace_head: 1a1a42eebda033a1fbda4696df5b81271f5eed9b
verified_remote_publish_commit: 1a1a42eebda033a1fbda4696df5b81271f5eed9b
feedback_bundle_transport: >-
  Reviewed source commit 82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 contains the
  refreshed PKT-002 request pair, feedback bundle, and reviewed Incident Home
  files. Current front publish commit
  1a1a42eebda033a1fbda4696df5b81271f5eed9b on
  origin/pkt-004-detail-fix republishes the canonical request pair and points
  both request bodies back to reviewed source commit
  82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9. Diffing the reviewed PKT-002 scope
  from source to the current publish commit changes only the two request
  files, so the returned UI slice and refreshed feedback bundle remain
  replay-clean from one truthful immutable source snapshot.
screen: incident-home
workbench: operator-console
screen_id: screen-operator-incident-home

status: loop-complete
disposition: close
review_result: replay-clean-route-fixed-and-contract-aligned
can_close: true

summary: >-
  Pantheon re-reviewed the current PKT-002 Incident Home ui-done and
  frontend-feedback return on origin/pkt-004-detail-fix against the published
  PKT-002 contract, canonical example payload, and the current Pantheon
  incident-home acceptance slice. The accepted reviewed source commit remains
  82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9: it refreshes the feedback bundle
  so its route narration and commit anchors are truthful again, and
  `IncidentHome.tsx` routes row clicks to the mounted
  `/operator/incidents/:incidentId` family. Current publish commit
  1a1a42eebda033a1fbda4696df5b81271f5eed9b keeps the canonical request pair
  replay-clean without changing the reviewed PKT-002 UI slice. Targeted
  TypeScript, eslint, shared banner regression, and production build
  verification all passed again in the sibling front workspace, and
  Pantheon's incident-home smoke slice returned `5 passed, 15 deselected`. No
  new Pantheon BFF endpoint, runtime escalation, or contract rebaseline is
  required for this loop.

acceptance_verified:
  - criterion: review the refreshed PKT-002 snapshot against the latest Pantheon contract and example payload
    status: pass
    note: >-
      `IncidentHome.tsx`, `src/pages/operator/types.ts`, and the shared
      `bffClient` in reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 still read only
      `GET /api/v1/incidents` and `GET /api/v1/kill-switch/status`, validate
      the required PKT-002 envelope fields, and stay aligned with the
      object-wrapped `meta.surfaces` examples in
      `docs/examples/PKT-002-incident-home.json`.
  - criterion: PKT-005 split-read banner derivation remains aligned
    status: pass
    note: >-
      Reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 still merges `incident_list`
      and `kill_switch` through `mergeBannerMeta()` and
      `deriveDegradationBannerState()`, and `src/lib/degradationBanner.ts`
      still accepts the object-wrapped surface envelopes emitted by the
      published examples and live Pantheon read store.
  - criterion: publish ui-done and frontend-feedback through a Git-visible request-pair transport
    status: pass
    note: >-
      `git ls-remote --heads origin pkt-004-detail-fix` resolves to
      1a1a42eebda033a1fbda4696df5b81271f5eed9b, and that publish commit
      contains `.coordination/requests/PKT-002-incident-home-ui-done.yaml`,
      `.coordination/requests/PKT-002-incident-home-frontend-feedback.yaml`,
      `docs/pantheon-feedback/PKT-002-incident-home/*`, and the reviewed UI
      files while both request bodies point back to reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9.
  - criterion: republish the request pair without changing the reviewed UI snapshot
    status: pass
    note: >-
      `git diff --name-only
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9
      1a1a42eebda033a1fbda4696df5b81271f5eed9b --` over the reviewed PKT-002
      scope lists only the two request files, so the reviewed UI slice remains
      pinned to source commit 82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9.
  - criterion: keep the feedback bundle aligned to the reviewed source commit, mounted route topology, and current API-gap state
    status: pass
    note: >-
      `API_GAP_REQUESTS.json` at reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 now reports
      `"status": "no_open_gaps"`, and `LOVABLE_CHANGE_FEEDBACK.md`,
      `UI_DECISIONS.md`, and `QA_STATUS.md` now narrate the mounted
      `/operator/incidents` route family truthfully.
  - criterion: rerun front static verification on the reviewed snapshot
    status: pass
    note: >-
      `./node_modules/.bin/tsc --noEmit --pretty false`, targeted
      `npx eslint src/pages/operator/IncidentHome.tsx
      src/pages/operator/types.ts src/lib/bffClient.ts
      src/lib/degradationBanner.ts src/App.tsx
      src/components/AppSidebar.tsx`, `npx --yes tsx
      src/components/GlobalDegradationBanner.test.tsx`, and `npm run build`
      all completed successfully in the sibling front workspace; the build
      emitted only the existing non-blocking Vite chunk-size warning.
  - criterion: Pantheon incident-home read slice still passes locally
    status: pass
    note: >-
      `python3 -m pytest services/control-plane/bff/smoke_test_incident.py -q -k 'test_in01_incident_list or test_in01_incident_list_filtered or test_in01_resolved_incident_list_includes_resolved_at or test_in05_kill_switch_status or test_in05_kill_switch_unavailable_disables_actions'`
      completed with `5 passed, 15 deselected`.
  - criterion: row selection reaches the mounted Incident Detail route
    status: pass
    note: >-
      `src/pages/operator/IncidentHome.tsx` at reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 now calls
      `navigate(`/operator/incidents/${incident.incident_id}`)`, which matches
      the mounted route family in `src/App.tsx`.
  - criterion: the active local runtime still advertises the incident-home route family
    status: pass
    note: >-
      `/openapi.json` on `http://127.0.0.1:18001` advertises both
      `/api/v1/incidents` and `/api/v1/kill-switch/status`, so the active
      operator-bff runtime still exposes the published PKT-002 read routes.

api_gaps: []

pantheon_actions_completed:
  - action: verify the current remote-visible PKT-002 request-pair publish commit
    path: ../front-ai-trading-system/.coordination/requests/PKT-002-incident-home-ui-done.yaml
    note: >-
      Pantheon verified that
      1a1a42eebda033a1fbda4696df5b81271f5eed9b on
      origin/pkt-004-detail-fix publishes both canonical PKT-002 request
      files, the refreshed feedback bundle, and the reviewed Incident Home UI
      files, and that both request bodies point to reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9.
  - action: inspect the metadata-only delta between the reviewed source and publish commit
    path: ../front-ai-trading-system
    note: >-
      `git diff --name-only
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9
      1a1a42eebda033a1fbda4696df5b81271f5eed9b --` over the reviewed PKT-002
      scope shows only the two request files.
  - action: inspect the reviewed source snapshot against the published PKT-002 routes
    path: ../front-ai-trading-system/src/pages/operator/IncidentHome.tsx
    note: >-
      Reviewed source commit
      82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9 still reads only
      `GET /api/v1/incidents` and `GET /api/v1/kill-switch/status` and keeps
      split-read degradation handling on the shared banner helpers while
      routing row selection through `/operator/incidents/${incident_id}`.
  - action: inspect feedback bundle truthfulness against the reviewed source snapshot
    path: ../front-ai-trading-system/docs/pantheon-feedback/PKT-002-incident-home
    note: >-
      Pantheon verified that `API_GAP_REQUESTS.json` now reports no open gaps
      and that `LOVABLE_CHANGE_FEEDBACK.md`, `UI_DECISIONS.md`, and
      `QA_STATUS.md` all match the mounted `/operator/incidents` route family
      and reviewed source commit.
  - action: rerun targeted front static verification on the reviewed snapshot
    path: ../front-ai-trading-system
    note: >-
      `./node_modules/.bin/tsc --noEmit --pretty false`, targeted
      `npx eslint` over the reviewed PKT-002 files,
      `npx --yes tsx src/components/GlobalDegradationBanner.test.tsx`, and
      `npm run build` all completed successfully in the sibling front
      workspace.
  - action: verify the Pantheon incident read slice
    path: services/control-plane/bff/smoke_test_incident.py
    note: >-
      `python3 -m pytest services/control-plane/bff/smoke_test_incident.py -q -k 'test_in01_incident_list or test_in01_incident_list_filtered or test_in01_resolved_incident_list_includes_resolved_at or test_in05_kill_switch_status or test_in05_kill_switch_unavailable_disables_actions'`
      passed with `5 passed, 15 deselected`.
  - action: inspect the active local runtime for the published incident-home route family
    path: http://127.0.0.1:18001
    note: >-
      `/openapi.json` on 18001 advertises `/api/v1/incidents` and
      `/api/v1/kill-switch/status`, matching the published PKT-002 read
      routes.

follow_up_items:
  - type: qa
    status: deferred_non_blocking
    note: >-
      Live browser QA against a running Pantheon BFF remains deferred after
      closeout.

required_front_repo_updates: []
required_pantheon_updates: []

loop_close_condition: >-
  Met. origin/pkt-004-detail-fix now points at
  1a1a42eebda033a1fbda4696df5b81271f5eed9b, which republishes the canonical
  PKT-002 request pair pinned to reviewed source commit
  82b1ceb76a7de4f8e49b5f08c0b9fcb865f52bd9; the reviewed UI remains aligned
  with the published incident-home split-read contract, the feedback bundle is
  truthful again, and row navigation now stays on the mounted
  `/operator/incidents` route family.

risk_note: >-
  Remaining risk is non-blocking live browser QA and future live payload drift,
  not request-pair replayability, route truth, or PKT-002 contract alignment.
  The current incident-home loop is closed on the published Pantheon contract.

lovable_ui_task_status: closed
next_action: none
delivery_artifact_ref: .coordination/responses/PKT-002-incident-home-backend-delivery.yaml
review_findings_ref: .coordination/reviews/PKT-002-incident-home-review.md
