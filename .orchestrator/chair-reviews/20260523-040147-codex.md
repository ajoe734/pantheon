# Operational Review - 20260523-040147 - Chair: Codex

**Review time:** 2026-05-23T04:05:10Z
**Chair agent:** Codex
**Scope:** Operational review only; no task-board mutation.

## Summary

System execution lanes are idle and clean. The task board has 5 current tasks:
3 `done`, 2 `blocked` human gates, and 0 `in_progress`, `review`, or
`review_approved` tasks. The only live worker in `.orchestrator/state.json` is
this chair review run (`codex-20260523T040147Z-0df9c9dd`, `task_id=null`),
correctly tied to queue event `evt-20260523T040147Z-0157edea`.

No dispatch backlog, approval backlog, provider dispatch pause, owner/reviewer
mismatch, or push-status/ahead closeout item is blocking mainline execution.
Sidecars can be approved as a standing permission, but the only non-terminal
parents are human-gate tasks and should not receive sidecars.

The important operational debt this cycle is outside normal dispatch:
stale blocker metadata for already-done tasks, incomplete closeout delivery
metadata that is breaking nightly publish discipline, a failing promote
workflow, and 145 remote `task/*` branches with no open PRs.

## Findings

### F1 - No fake `in_progress`

- Current board status counts: `done=3`, `blocked=2`.
- `in_progress`, `review`, and `review_approved` counts are all 0.
- The only live worker is the current chair-review run with `task_id=null`;
  this is expected and does not correspond to a task-board entry.

### F2 - Worker / owner / reviewer routing is clean

- `PROD-WRITES-001-V2`: owner `Claude`, reviewer `Codex2`, status `blocked`,
  `task_class=human_gate`.
- `LIVE-SCALE-001-V2`: owner `Claude2`, reviewer `Codex`, status `blocked`,
  `task_class=human_gate`.
- No owner equals reviewer. No worker is running against the wrong task owner or
  reviewer slot.
- The queue contains only the current chair review event, already started and
  leased by the matching Codex worker run.

### F3 - Dispatch / approval queues are not stuck

- Prompt queue depth: 0.
- State queue: only current chair review event, status `started`.
- Pending approvals: none. `.orchestrator/state.json` has no approval items
  beyond `last_reconciled_at`.
- No approval action is safe or necessary this cycle.

### F4 - Provider guardrails are not blocking mainline

- `provider_guardrails.dispatch_pauses` is `{}`.
- `provider_capabilities.json` shows the configured providers available for
  local dispatch.
- Historical failure streaks remain in state, but repeated-failure details for
  this cycle are none and there is no current provider-wide auth blocker.

### F5 - No stale review pipeline

- `review` count: 0.
- `review_approved` count: 0.
- No reviewer reassignment is warranted.

### F6 - Sidecar hygiene is acceptable, with human-gate exclusions

- Active sidecars: 0.
- Terminal sidecar on the board: `OSS-STAT-001-SIDECAR-ACCEPTANCE`, status
  `done`.
- No duplicate or over-saturating sidecars are present.
- The two non-terminal tasks are human gates and should be excluded from sidecar
  generation:
  `PROD-WRITES-001-V2`, `LIVE-SCALE-001-V2`.

### F7 - Closeout push status is clean, but delivery metadata is incomplete

- Done tasks have no `push_status` or `ahead` value indicating unpushed commits:
  `OSS-STAT-001-SIDECAR-ACCEPTANCE`, `LOVABLE-STRICT-PUBLISH`,
  `OSS-QUANTLIB-V2-001`.
- However, `scripts/git/nightly_publish.sh check` fails release discipline
  because done-task delivery metadata is incomplete:
  `OSS-STAT-001-SIDECAR-ACCEPTANCE` has no delivery metadata, and
  `LOVABLE-STRICT-PUBLISH` / `OSS-QUANTLIB-V2-001` are missing commit and
  commit-trailer metadata.
- This is now an operational blocker for publish cuts, even though normal
  closeout push state is clean.

### F8 - Stale blocker records remain for done tasks

`ai-status.json` still lists open blockers for:

- `LOVABLE-STRICT-PUBLISH`
- `OSS-QUANTLIB-V2-001`

Both tasks are currently `done` in the board, and their `next` fields record
merged PR closeout. The stale blocker rows should be cleared by a status refresh
or explicit OPS metadata repair, not by directly marking tasks from this chair
review.

### F9 - Human-gate dependency notes are stale

`PROD-WRITES-001-V2` and `LIVE-SCALE-001-V2` are correctly blocked as human
gates, but their `next` text still references older task IDs such as
`HA-PROD-001-V2`, `CBL-LIVE-001-V2`, `LSP-006-V2`, and `BLA-007-V2`. These
references should be audited so the gates state only the live human blockers:
risk-owner signoff, operator signoff, and any current observation/report
requirement.

### F10 - Publish pipeline needs OPS triage

- Latest release tag remains `release/v2026.05.19.0`; latest prod tag remains
  `prod/v2026.05.17.2`.
- `nightly-publish-cut.yml` failed on 2026-05-20, 2026-05-21, and 2026-05-22.
  Local reproduction shows `nightly_publish.sh check` fails release discipline
  on wave state and missing done-task delivery metadata.
- `publish-promote.yml` is also failing repeatedly. Discovery currently finds
  eligible candidates `v2026.05.18.0` and `v2026.05.19.0`, but the latest run
  fails in `Open promote PRs` at
  `git merge --no-ff --no-edit -m promote: v2026.05.18.0 origin/publish/v2026.05.18.0`
  with exit 128.
- `git merge-tree --write-tree` succeeds for both publish branches, so the
  promote failure is not an obvious content conflict. The workflow also lacks
  an explicit git identity setup before creating the merge commit; stderr is
  currently hidden by the helper's captured subprocess output.

### F11 - Remote task-branch cleanup is overdue

- Open PRs to `dev`: 0.
- Open PRs to `master`: 0.
- Remote `origin/task/*` branches: 145.
- Remote task branches already merged into `origin/dev`: 143.
- Not merged into `origin/dev`: `origin/task/EP5-002-V2`,
  `origin/task/OSS-FINRL-V2-001`.

These are zombie-branch candidates. Do not delete from chair review; open or
dispatch an OPS cleanup that verifies reachability and deletes only confirmed
merged/abandoned branches.

## Suggested Repairs

1. Create `OPS-CLOSEOUT-METADATA-REPAIR` to reconstruct or explicitly exempt
   delivery metadata for the three done tasks blocking release discipline:
   `OSS-STAT-001-SIDECAR-ACCEPTANCE`, `LOVABLE-STRICT-PUBLISH`,
   `OSS-QUANTLIB-V2-001`.
2. Create `OPS-PUBLISH-PIPELINE-TRIAGE` to fix `nightly-publish-cut.yml` and
   `publish-promote.yml`: preserve the release-discipline failure details,
   expose stderr from `publish_promote.py`, and add/verify git identity before
   promote merge commits if confirmed.
3. Create `OPS-HUMAN-GATE-METADATA-AUDIT` to clean stale blocker rows and stale
   human-gate dependency text without changing terminal task statuses.
4. Create `OPS-ZOMBIE-BRANCH-CLEANUP` to batch-check the 145 remote task
   branches, delete the 143 confirmed merged branches, and separately triage
   `EP5-002-V2` and `OSS-FINRL-V2-001`.
5. Trigger Sprint 9 planning once publish/metadata hygiene has an owner, since
   there is no runnable execution backlog on the current board.

## Sidecar Recommendation

Decision: `approve_sidecars`.

Rationale: all execution workers are idle, queues and approvals are clear, and
there is no current provider guardrail. Keep `max_sidecars` as `null`. Block
sidecars only for the two human-gate parents:
`PROD-WRITES-001-V2` and `LIVE-SCALE-001-V2`.
