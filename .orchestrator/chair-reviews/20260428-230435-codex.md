# Chair Review - 2026-04-28T23:04:35Z (Codex)

## Summary

Operational state is active, but the execution lane is being consumed by a repeated sidecar review dispatch. `ai-status.json` currently has no `in_progress` tasks, so there is no fake in-progress board entry. Queue depth is effectively one execution worker plus this chair review: `evt-20260428T230435Z-3184cec6` is dispatching `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` to reviewer `Codex2`.

The main issue is not queue staleness; it is worker identity / reviewer behavior. The Codex2-targeted worker log shows the process acting as `Codex`, rerunning owner-side validation, and re-handing the packet to `Codex2` instead of issuing a reviewer approve/reject decision. `.orchestrator/state.json` now records 34 generic exits for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE:codex2`.

There are no pending approvals. Claude remains paused for quota until `2026-04-29T12:20:00Z`, which blocks Claude-reviewed support work but is not a global provider outage.

## Findings

1. No fake `in_progress`: `ai-status.json` has zero tasks with `status=in_progress`. `SVC-HEALTH-OBSERVABILITY-UNIFICATION` is still `todo`, and `Gemini` is waiting on assignment state rather than a live in-progress claim.

2. Queue target is structurally correct, but the worker is not completing reviewer work. `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` is `review` with reviewer `Codex2`, and the queue targets `Codex2`; the active log shows `AI_NAME=Codex` handoff behavior, leaving the task in `review`.

3. Dispatch queue is current, not stale. `.orchestrator/event-queue.jsonl` contains two current events from `2026-04-28T23:04:35Z`: the Codex2 review dispatch and this chair review. Both are marked `started` in state.

4. Approval queue is clear. `.orchestrator/approval-queue.json` has `pending: []`, matching the prompt's `Pending Approvals: 0`; no `approval_actions` are needed.

5. Provider guardrail is partial. Claude is paused for quota until `2026-04-29T12:20:00Z`. This blocks `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE`, whose reviewer is Claude, but Codex/Codex2/Gemini lanes still exist.

6. Review / review_approved backlog needs clearing. `SVC-RESEARCH-WORKER-GATEWAY` has been `review_approved` since `2026-04-28T20:05:05Z` and should be finalized by owner `Codex2`. `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE` has been in `review` since `2026-04-28T19:44:21Z` but its reviewer lane is paused. `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` is cycling through repeated non-terminal Codex2 dispatches.

7. Sidecar count is not excessive, but pressure is concentrated. The two active sidecars have clear `helper_parent`, `helper_kind=acceptance_packet`, and `mutates_canonical=false`. Do not create more support work for the same parents while one sidecar loops and the other waits on a paused reviewer.

8. Repeated GitHub review PR creation failures are visible for both sidecars: `GraphQL: The codex/2026-04-21-exec-sync branch has no history in common with master`. This is not blocking local queue dispatch, but it should be tracked as an OPS repair.

## Suggested Repairs

- Fix Codex2 reviewer dispatch identity so reviewer workers run with `AI_NAME=Codex2` or an equivalent adapter identity override.
- Add a retry circuit breaker for repeated reviewer non-terminal exits. After a small threshold, stop requeueing and create an OPS follow-up instead of continuing the wake loop.
- Prioritize `SVC-RESEARCH-WORKER-GATEWAY` owner finalization. That should unblock `SVC-HEALTH-OBSERVABILITY-UNIFICATION`.
- Keep the Claude-reviewed sidecar parked or reroute it by explicit supervisor policy while Claude is paused until `2026-04-29T12:20:00Z`.
- Open an OPS follow-up for GitHub PR branch ancestry failures on the `codex/2026-04-21-exec-sync` branch.
- If `.orchestrator/provider-capabilities.json` is intentionally retired, update the chair review skill to name `.orchestrator/state.json` as the authoritative provider guardrail source.

## Sidecar Recommendation

Approve sidecars globally for 45 minutes with `max_sidecars=2`, because utilization is low and there is no global approval or provider outage. Block sidecars for these parents until current review/finalization debt clears:

- `SVC-RESEARCH-ORCHESTRATOR-SERVICE`: existing sidecar is in a Codex2 reviewer wake loop.
- `SVC-RESEARCH-WORKER-GATEWAY`: parent is already `review_approved` and has an open acceptance sidecar.
- `SVC-HEALTH-OBSERVABILITY-UNIFICATION`: dependency-gated on `SVC-RESEARCH-WORKER-GATEWAY` finalization.

Recommended focus: finalize `SVC-RESEARCH-WORKER-GATEWAY`, repair the Codex2 review identity loop, then resolve or reroute the Claude-paused sidecar review.
