# Operational Review - 20260430-133259 - Codex

## Summary

Review timestamp: 2026-04-30T13:34:55Z.

Main execution has two valid live workers: Claude on `SVC-SOURCE-SEARCH-TEST-CLOSURE` and Codex2 on `TEST-FULLSUITE-HARNESS-ISOLATION`. Claude is suspended on a fresh approval request; Codex2 is running. I did not find fake `in_progress` board rows, owner/reviewer mismatches, or stale `review` / `review_approved` tasks.

Sidecars should be approved with limits. Underutilization is `0.2857` and there is runnable execution backlog, but provider guardrails mean sidecar dispatch should avoid paused providers and should not duplicate parents that already have live work or an existing support sidecar.

## Findings

1. **Task board / live workers:** no fake `in_progress`. `SVC-SOURCE-SEARCH-TEST-CLOSURE` has live worker `claude-20260430T133006Z-e5c7d2ae`; `TEST-FULLSUITE-HARNESS-ISOLATION` has live worker `codex-20260430T133018Z-6f41fa74`.

2. **Worker / queue alignment:** clean. The Claude worker targets owner `Claude` for `SVC-SOURCE-SEARCH-TEST-CLOSURE`; the Codex2 worker targets owner `Codex2` for `TEST-FULLSUITE-HARNESS-ISOLATION`; the current Codex worker is a `chair_review` run with `task_id: null`, which is expected.

3. **Dispatch / approval queues:** no old queue item is stuck. The only pending approval, `apr-20260430T133231Z-bb6b1ad9`, was created at 2026-04-30T13:32:31Z for Claude and is recent. The command is read-only compose validation: `docker compose config -q 2>&1; echo "exit: $?"`.

4. **Provider guardrails:** guardrails are provider-scoped, not global. `claude2` is quota-paused until 2026-05-01T04:30:00Z; `copilot` and `gemini` have quota/capacity pauses; `gemini2` is blocked by non-interactive auth. Claude/Codex/Codex2 still provide a viable main path once the approval is handled.

5. **Review / closeout:** no current task is in `review` or `review_approved`. Recent done tasks generally record task-scoped commits and verification. One closeout hygiene item remains: `SVC-OPENCLAW-ACTIVATION-READY-E2E-SIDECAR-BFF-HANDOFF` records `push_status: ahead`; treat it as publish-pending only if remote publication is required.

6. **Sidecars:** one active sidecar exists, `SVC-BFF-INCIDENT-SMOKE-FIXTURE-SIDECAR-BFF-HANDOFF`, with clear parent `SVC-BFF-INCIDENT-SMOKE-FIXTURE`, but it is owned by auth-blocked `Gemini2`. Do not create another sidecar for that parent until the existing sidecar is recovered or reassigned.

## Suggested Repairs

1. Allow approval `apr-20260430T133231Z-bb6b1ad9`; it is scoped validation and should unblock Claude's current source/search task.

2. Keep paused providers out of new dispatches until their guardrails clear. In particular, do not send urgent work to `claude2` before 2026-05-01T04:30:00Z, and do not target `gemini2` until non-interactive auth is repaired.

3. Avoid duplicate sidecars for `SVC-SOURCE-SEARCH-TEST-CLOSURE`, `TEST-FULLSUITE-HARNESS-ISOLATION`, and `SVC-BFF-INCIDENT-SMOKE-FIXTURE`: the first two already have live main workers, and the BFF parent already has a stuck support sidecar.

4. If sidecar capacity is used this cycle, prefer support-only packets for independent todo backlog such as `SVC-RESEARCH-REPLICATION-SMOKE-FIX` or `SVC-OSS-WANDB-DORMANT-MATRIX-ALIGN`; keep sidecars non-canonical and acceptance/dependency focused.

## Sidecar Recommendation

Approve up to 2 sidecars for 45 minutes.

Rationale: the system is underutilized and has runnable execution backlog, while current blockers are provider-scoped rather than global. Use `blocked_sidecar_parents` to prevent duplicate support on active or already-sidecarred parents.
