# Operational Review - Claude2 - 2026-04-28 14:01Z

## Summary

Decision: approve sidecars, capped at 2 for 45 minutes.

The execution plane is mostly healthy. Three live workers are running and aligned with their owner/reviewer roles. There is no global blocker: the only paused lane (copilot, quota) and the stuck Qwen auth lane are both provider-scoped. Backlog still has safely parallelizable support work behind SVC-COMPOSE and SVC-RUNTIME-CONTROL-CLOSEOUT. Two specific parents should not get new sidecars this cycle (SVC-SURFACES already has one in `review`; SVC-RUNTIME-CONTROL is mid-finalize with a stuck approval).

## Findings

1. **Same fake `in_progress` repeats from prior chair review.** `SVC-RUNTIME-CONTROL-CLOSEOUT` is still `in_progress` in `ai-status.json` (since 2026-04-28T13:25:32Z, ~36 minutes), and Gemini's agent record still says `status: working` on it. There is no Gemini live worker in `.orchestrator/state.json` bound to this task — the only Gemini-owned activity remains the prior `SVC-RUNTIME-CONTROL` review handoff. The supervisor has not yet reconciled the closeout binding noted by the previous chair (Codex2, 13:29Z).

2. **Worker / queue alignment is otherwise correct.**
   - `claude-20260428T140143Z-ef379563` running — task `SVC-GOVERNANCE-API`, reason `review_ready_dispatch`. Task reviewer is Claude. ✓
   - `codex-20260428T140143Z-dbdf45f3` running — task `SVC-SURFACES-SIDECAR-BFF-HANDOFF`, reason `review_ready_dispatch`. Task reviewer is Codex. ✓
   - `claude2-20260428T140145Z-4c453a01` running — chair review (this run). ✓
   - Three older workers (`claude-…-63933f13`, `codex-…-b15a305b`, `codex-…-90c05ac4`) are in `superseded` status; that is the expected state after re-prioritization, not a leak.

3. **Stuck approval blocking SVC-RUNTIME-CONTROL finalize.** `apr-20260428T135027Z-16517bcb` is `pending` in `.orchestrator/approval-queue.json`. It was created at 2026-04-28T13:50:27Z (~11 minutes old) by `claude-20260428T134444Z-63933f13` for a finalize-stage `git add …` while staging SVC-RUNTIME-CONTROL artifacts. The worker that requested it has since been `superseded` ("Worker superseded to prioritize higher-priority review/finalize work"). The approval is now orphaned: its worker cannot consume the decision. This is the same shape that historic `auto-pruned orphaned approval` history entries cleared, so the auto-prune should fire, but it has not yet.

4. **Provider guardrails are provider-scoped only.** `provider_guardrails.dispatch_pauses` contains only `copilot` (paused until 2026-04-28T14:11:00Z, reason "402 You have no quota"). Qwen is `auth_ready: false` because the OAuth free tier was discontinued on 2026-04-15. Both are documented and do not block other providers. No global pause is active.

5. **Review / review_approved dwell.**
   - `SVC-RUNTIME-CONTROL` is `review_approved` since 13:41:44Z (~20 minutes). Finalize is blocked on the orphaned approval in finding 3.
   - `SVC-GOVERNANCE-API` entered `review` at 14:01:19Z (under 1 minute) — fresh, not stale.
   - `SVC-SURFACES-SIDECAR-BFF-HANDOFF` entered `review` at 13:59:20Z — fresh, not stale.
   - No long-stale review_approved tasks beyond SVC-RUNTIME-CONTROL.

6. **Sidecar load is moderate, not saturating.** Three sidecar-class tasks present:
   - `SVC-RUNTIME-CONTROL-CLOSEOUT-SIDECAR-ACCEPTANCE` (todo, Codex; reassigned after Copilot quota terminal)
   - `SVC-SURFACES-SIDECAR-BFF-HANDOFF` (review, Codex2 owner / Codex reviewer; live worker exists)
   - `SVC-RUNTIME-CONTROL-SIDECAR-BFF-HANDOFF` (todo, Codex; reassigned from Qwen after OAuth discontinuation)

   Each has a distinct parent and helper kind; no duplicate-per-parent. Two have ownership reassigned away from blocked providers, which is correct but means they are new untouched work. SVC-SURFACES already has a sidecar in active review, so that parent does not need another sidecar wave this cycle.

## Suggested Repairs

- **Auto-prune orphaned approval `apr-20260428T135027Z-16517bcb`.** Its `worker_run_id` is in `superseded` status and cannot consume a decision; the supervisor's existing orphan-prune path should reject it. If manual intervention is required, deny it with note "Auto-pruned approval because the worker exited before approval could be applied" (matching the standing pattern for `apr-20260428T133358Z-3979a818` etc). Until pruned, Claude finalize on SVC-RUNTIME-CONTROL stays gated.

- **Reconcile SVC-RUNTIME-CONTROL-CLOSEOUT binding (carry-over from prior chair review).** Either bind a Gemini worker explicitly to `SVC-RUNTIME-CONTROL-CLOSEOUT` or record in the closeout narrative that the parent-review worker on `SVC-RUNTIME-CONTROL` covers it. Right now Gemini is reported `working` against a task with no live worker, which makes `in_progress` ambiguous on the board. Do not flip the task to `done` until a worker actually reports completion.

- **Open OPS / SUP follow-ups (do not directly mutate ownership):**
  - `OPS-APPROVAL-ORPHAN-AUTOPRUNE`: confirm orphan-pruner sweeps `pending` approvals when their worker enters `superseded` (not only when it disappears entirely). Today's stuck approval shows the path is currently keyed on missing worker state, not on terminal worker statuses.
  - `OPS-CLOSEOUT-WORKER-BINDING`: have supervisor either auto-clear `in_progress` when no live worker is bound for ≥ N minutes, or auto-create a worker dispatch event for the recorded owner instead of leaving the task ambiguous.

- **Resume Claude2's own assignment after this cycle.** Claude2 is `waiting` on `SVC-POLICY-LEARNING-BOUNDARY` (todo, owner Claude2, reviewer Codex). The chair review run does not advance that task; once chair review exits, normal dispatch should pick it up.

## Sidecar Recommendation

Approve up to 2 sidecars. Preferred parents:

- `SVC-COMPOSE`: acceptance/dependency-map sidecar that documents which upstream service families (governance, runtime-control, evidence, surfaces, service-disposition) must be in `done` before compose smoke can run, plus a pre-staged smoke command list. Mutates only support artifacts.
- `SVC-RUNTIME-CONTROL-CLOSEOUT`: support packet that consolidates the Claude handoff review and structures the auth/JWT, legacy idempotency, and placeholder-deployment-approval gaps as forward references for SVC-RUNTIME-HARDENING. Existing `SVC-RUNTIME-CONTROL-CLOSEOUT-SIDECAR-ACCEPTANCE` already covers the acceptance packet; if the supervisor intends a second helper, prefer a *review/evidence* helper kind, not another acceptance packet.

Avoid generating sidecars against:

- `SVC-SURFACES` — already has `SVC-SURFACES-SIDECAR-BFF-HANDOFF` in `review` this cycle. Adding another would saturate the same parent.
- `SVC-RUNTIME-CONTROL` — finalize is already mid-flight and gated on a stuck approval (finding 3). A new sidecar on this parent would not unblock anything and risks duplicating `SVC-RUNTIME-CONTROL-SIDECAR-BFF-HANDOFF` which is already in `todo`.
