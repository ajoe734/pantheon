# Chair Operational Review - 2026-05-31 20:57 UTC - Codex

## Summary

Decision: approve sidecars with no global cap.

There is idle worker capacity and no pending approval queue blocker. The main execution path is blocked only by `ASST-OCGW-001`, which should be quarantined from sidecar generation until Gemini quota handling and its dirty task worktree are recovered.

## Findings

1. `ASST-OCGW-001` is a fake `in_progress` item. Live state still records worker `gemini-20260531T205217Z-2cb7ed3d` as `running`, but the worker status file is `failed`, exit code 1, finished at `2026-05-31T20:52:25Z`; the runner PID is defunct and the child PID is gone.

2. The queue event is correctly targeted. Event `evt-20260531T205215Z-6256b989` targets Gemini for `ASST-OCGW-001` with reason `owned_in_progress_dispatch`, matching owner `Gemini` and reviewer `Codex`. This is not an owner/reviewer mismatch.

3. Provider guardrail is not stopping a known quota loop. `.orchestrator/state.json` has empty `provider_guardrails.dispatch_pauses`, while the latest Gemini log reports `TerminalQuotaError` / 429 with quota reset after about `19h3m48s`. Recent activity shows repeated `ASST-OCGW-001` redispatches every supervisor cycle followed by missing-process reconciliation.

4. `ASST-OCGW-001` has non-durable partial work. Its task worktree `task/ASST-OCGW-001` is behind `origin/dev` by 55 commits and has uncommitted edits in `.env.example`, `docker-compose.yml`, `docker-compose.staging-full.yml`, `services/openclaw-gateway-adapter/main.py`, plus untracked credential-mount code, tests, task brief, and evidence.

5. Dispatch and approval queues are not broadly stale. The live queue contains the current failed Gemini dispatch and this chair review dispatch, both created at `2026-05-31T20:52Z` with leases until `21:22Z`. The approval queue has `pending: []`.

6. No long-lived `review` or `review_approved` task is present. The live board has 3 `done`, 2 human-gate `blocked`, 1 `in_progress`, and 12 `todo` tasks; no task is waiting in review or finalization.

7. Sidecar pressure is low. There are no active sidecars on the live board, and only the old completed `OSS-STAT-001-SIDECAR-ACCEPTANCE` sidecar is present. Sidecars remain reasonable for independent support work, but not for parent `ASST-OCGW-001`.

8. Closeout push hygiene is clean for current board records. Done tasks do not show `push_status: ahead`; `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` record merged PRs and manual sync after the older Gemini2 push-auth problem.

9. PR oversight: open dev PR #682 is under 24h old with required checks green, though merge state is `BEHIND`. Open master PRs #688 and #695 are `task/chair-review-*` branches, not `promote/*`, both behind and failing master checks. They should be closed or retargeted away from `master`.

10. Publish pipeline health is degraded. The latest `publish-promote.yml` run at `2026-05-31T20:38Z` failed in `publish_promote.py open-prs` when `git merge --no-ff ... origin/publish/v2026.05.18.0` exited 128. No remote `promote/*` branches are present. `nightly-publish-cut.yml` has failed daily from `2026-05-24` through `2026-05-31`.

## Suggested Repairs

- Record a Gemini quota/capacity dispatch pause from the latest `TerminalQuotaError`, or create `OPS-GEMINI-QUOTA-GUARD-20260531` to make this automatic before the next redispatch cycle.

- Preserve `ASST-OCGW-001` partial work before any recovery. Prefer an owner anchor commit or explicit salvage task; do not refresh, rebase, or overwrite the dirty worktree first.

- Re-dispatch `ASST-OCGW-001` only after Gemini quota reset or after a deliberate owner recovery plan that accounts for the dirty worktree. Do not change owner via chair reassignment while the task is `in_progress`.

- Create `OPS-PROMOTE-PIPELINE-20260531` to debug the `publish_promote.py` merge exit 128 and restore promote PR creation for `v2026.05.18.0` / `v2026.05.19.0`.

- Create `OPS-NIGHTLY-PUBLISH-CUT-20260531` to triage repeated nightly publish failures and restore scheduled release tag creation.

- Close or retarget PR #688 and PR #695 away from `master`; task/chair-review branches should not target `master` outside the promote or hotfix path.

## Sidecar Recommendation

Approve sidecars. Do not set `max_sidecars`. Block sidecar generation for parent `ASST-OCGW-001` until Gemini quota handling and dirty-worktree recovery are complete. Prefer sidecars only for independent backlog with a clear parent support need and a healthy non-Gemini provider.
