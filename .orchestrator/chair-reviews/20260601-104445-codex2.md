# Chair Review - 2026-06-01 10:44 UTC - Codex2

## Summary

Decision: deny sidecars for this wave.

The execution queue and approval queue are clear, but the active board still has a fake `in_progress` on `ASST-OCGW-004`. Its task PR #752 is already merged to `dev` at merge commit `c21a63b93390232246f340ce2d85395cc91ada34`, and the last worker runtime is completed, but supervisor guardrails classified two Codex owner attempts as auth failures. This needs lifecycle reconciliation, not more sidecar pressure.

This review used the live orchestrator root at `/home/lupin/code/pantheon` for runtime state, provider guardrails, queues, and PR status. Output files are written in this isolated chair-review worktree.

## Findings

1. `ASST-OCGW-004` is a fake `in_progress`.
   - Board state: `in_progress`, owner `Codex`, reviewer `Claude`, last update `2026-06-01T10:24:24Z`.
   - Runtime state: `codex-20260601T102424Z-f4328499` finished at `2026-06-01T10:31:46Z` with status `completed`; no live ASST-OCGW-004 process remains.
   - Delivery state: PR #752 merged to `dev` at `2026-06-01T10:30:31Z`, merge commit `c21a63b93390232246f340ce2d85395cc91ada34`, with Branch CI Gate checks passing.
   - The worktree `/tmp/pantheon-worker-worktrees/pantheon/asst-ocgw-004` is on `task/ASST-OCGW-004...origin/task/ASST-OCGW-004`; only `.orchestrator/task-briefs/asst_ocgw_004.md` is untracked.

2. Repeated Codex auth failures are real, but reassignment is not the right repair.
   - Provider guardrail shows `ASST-OCGW-004:codex1_1` failure count 2, last failure at `2026-06-01T10:34:38Z`, reason `completed.stderr = b"Error: not authenticated, please login first"`.
   - Because the task is already delivered through merged PR #752 and still marked `in_progress`, emitting an owner reassignment would violate the chair safety rule against owner changes on `in_progress` tasks and would risk duplicate implementation.
   - Recommended repair is supervisor lifecycle reconciliation from merged PR/runtime truth.

3. Queue and approval lanes are not jammed.
   - `.orchestrator/event-queue.jsonl` contains only this chair review event.
   - `.orchestrator/approval-queue.json` has `pending: []`.
   - No low-risk approval action is available.

4. Provider guardrails constrain progress, but this is not a provider-wide outage.
   - `codex1` is auth-paused until `2026-06-01T10:49:38Z` because of ASST-OCGW-004 failures.
   - `claude2` is manually paused until `2026-06-04T00:00:00Z`.
   - `claude`, `codex2`, `gemini`, `gemini2`, and `copilot` report auth-ready in `provider_capabilities.json`.

5. No long-stale review or review_approved task was found.
   - `ASST-OCGW-003` entered `review` at `2026-06-01T10:31:29Z`; PR #754 is open as a draft against `dev`, and listed checks are successful.
   - There are no active `review_approved` tasks.

6. Sidecars are not safe for this wave.
   - Remaining todo work depends directly or transitively on `ASST-OCGW-003` and/or `ASST-OCGW-004`.
   - `ASST-OCGW-004` needs status/closeout repair, while `ASST-OCGW-003` needs ordinary review completion.
   - Existing active sidecar pressure is low; the only active-board sidecar is historical and `done`.

7. Closeout/push-status hygiene has historical debt, but no approval to process now.
   - Active done tasks do not present a pending safe normal push approval.
   - Archive scan still finds many historical `delivery.push_status: ahead` records, including merged tasks whose `head_merged_to_target` is true, so this is metadata/branch-retirement debt rather than a blind push action.
   - No chair push approval should be emitted this cycle.

8. PR watch.
   - Dev PR #754 is fresh and green but intentionally draft for ASST-OCGW-003 review.
   - Dev PR #682 is green and under the 24-hour stuck-task threshold.
   - Six non-promote chair-review PRs targeting `master` (#688, #695, #698, #705, #726, #729) are open with failing checks. They should not be merged to `master`; create an OPS cleanup to close, retarget, or retire those branches.

## Suggested Repairs

1. Reconcile `ASST-OCGW-004` through supervisor/status tooling:
   - Confirm PR #752 merge commit `c21a63b93390232246f340ce2d85395cc91ada34`.
   - Record that worker `codex-20260601T102424Z-f4328499` is terminal and no live worker remains.
   - Move the task through the required review/closeout lifecycle without re-running implementation.

2. Refresh Codex auth state before any new Codex1 owner dispatch.
   - The repeated `not authenticated` failures show the lane is not safe for fresh work until the guardrail expires and auth is revalidated.

3. Let `ASST-OCGW-003` continue normal review.
   - PR #754 is fresh, draft, and green. Revisit reviewer reassignment only if Claude cannot pick it up after the next scan window.

4. Create `OPS-CHAIR-PR-BASE-HYGIENE-20260601`.
   - Scope: close or retarget non-promote chair-review PRs aimed at `master`, and keep chair output PRs on the normal task-to-`dev` path.

5. Create `OPS-ARCHIVE-PUSH-STATUS-AUDIT-20260601`.
   - Scope: reconcile historical archive `push_status: ahead` records against branch ancestry and retire only verified stale task branches.

## Sidecar Recommendation

Deny sidecars for 45 minutes.

Reason: idle capacity exists, but the runnable surface is dominated by `ASST-OCGW-004` lifecycle drift and `ASST-OCGW-003` active review. Downstream assistant work is dependency-gated, and speculative sidecars would duplicate the same OpenClaw gateway surface without a clear parent support need.
