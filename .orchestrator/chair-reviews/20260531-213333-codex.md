# Chair Operational Review - 2026-05-31 21:33 UTC - Codex

## Summary

Decision: approve sidecars with no global cap.

There is runnable execution backlog and idle/underused non-Gemini capacity, with no pending approvals and no global provider outage. The current mainline operational fault is still `ASST-OCGW-001`: the board/runtime state marks it `in_progress`, but the latest Gemini dispatch has already failed on quota and left the task worktree dirty. Sidecars are acceptable only for independent support work; do not generate sidecars for `ASST-OCGW-001` until its quota loop and partial-work recovery are handled.

## Findings

1. `ASST-OCGW-001` is a fake `in_progress` item at inspection time. The canonical status root shows owner `Gemini`, reviewer `Codex`, status `in_progress`, last update `2026-05-31T21:33:34Z`; runtime state still leases `evt-20260531T213331Z-4ef498da` to `gemini-20260531T213334Z-a6cf731a` until `22:03:38Z`, but the runner status file is `failed`, PID `533383` is defunct, and the child PID is gone.
2. The Gemini failure is quota, not owner/reviewer mismatch. The queue event targeted `Gemini` for `ASST-OCGW-001` with reason `owned_in_progress_dispatch`, matching the board owner/reviewer assignment. The Gemini log reports 429 `TerminalQuotaError` with quota reset after about `18h22m32s`.
3. Provider guardrails are not preventing the repeat loop. `provider_guardrails.dispatch_pauses` is empty even though recent activity shows repeated `ASST-OCGW-001` Gemini redispatches followed by `worker_failed` boot reconciliation and `skipped_dirty_worktree` reuse.
4. `ASST-OCGW-001` has non-durable partial work. Its isolated worktree is on `task/ASST-OCGW-001`, behind `origin/dev` by 59 commits, with modified `.env.example`, compose files, `services/openclaw-gateway-adapter/main.py`, and untracked credential-mount code, tests, task brief materialization, and evidence.
5. Dispatch/approval queues are not broadly stale. The only live queue events are the failed Gemini dispatch and this chair review. Approval queue is empty (`pending_count=0`), so there is no low-risk approval to process.
6. No task is in `review` or `review_approved`, so there is no long-lived review or finalization lane to unblock this cycle.
7. Sidecar pressure is low. The board has one historical completed sidecar (`OSS-STAT-001-SIDECAR-ACCEPTANCE`) and no active sidecar saturation. The backlog still has runnable `todo` work outside the Gemini-failed parent.
8. Closeout push hygiene is clean for current done records. `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` record merged PRs with manual sync after the older Gemini2 push-auth issue; no done task currently exposes `delivery.push_status: ahead`.
9. PR oversight: dev PR #682 is under 24h old and all listed checks are green, but it is `BEHIND` and should be refreshed if it remains open. Master PRs #688, #695, and #698 are `task/chair-review-*` branches, not `promote/*` or hotfix PRs; they are behind and failing master checks, so they should be closed or retargeted away from `master`.
10. Publish pipeline health remains degraded. `publish-promote.yml` failed again at `2026-05-31T21:36Z` in `publish_promote.py open-prs`: `git merge --no-ff ... origin/publish/v2026.05.18.0` exited 128. No remote `promote/*` branches exist. `nightly-publish-cut.yml` has failed daily from `2026-05-24` through `2026-05-31`.

## Suggested Repairs

- Add or repair a Gemini quota dispatch pause for `ASST-OCGW-001` so 429 `TerminalQuotaError` stops immediate redispatch until quota reset.
- Preserve `ASST-OCGW-001` partial work before any recovery. Prefer an owner anchor commit or explicit salvage follow-up; do not refresh, rebase, or overwrite the dirty task worktree first.
- Re-dispatch `ASST-OCGW-001` only after Gemini quota reset or after a deliberate owner recovery plan. Do not chair-reassign owner while the task is `in_progress`.
- Create or continue `OPS-GEMINI-QUOTA-GUARD-20260531`, `OPS-ASST-OCGW-001-SALVAGE`, `OPS-PROMOTE-PIPELINE-20260531`, and `OPS-NIGHTLY-PUBLISH-CUT-20260531`.
- Close or retarget PR #688, #695, and #698 away from `master`; chair-review task branches should flow to `dev` unless they are a promote/hotfix branch.
- Refresh PR #682 if it remains open and behind after the next cycle; it is not yet a >24h stuck PR.

## Sidecar Recommendation

Approve sidecars with `max_sidecars: null`. Block only parent `ASST-OCGW-001`. Prefer independent support work on healthy non-Gemini providers until the Gemini quota loop and dirty-worktree recovery are complete.
