# Operational Review - Codex Chair - 2026-04-29 16:32 UTC

## Summary

Decision: approve sidecars, capped at 2 for 45 minutes, but do not spawn new sidecars for the current review/finalization parents (`SVC-BFF-OIDC-JWKS-AUTH-FACADE`, `SVC-QLIB-GATED-PREACTIVATION-PREFLIGHT`, `SVC-RLLIB-RAYTUNE-DORMANT-SCAFFOLD-CLOSEOUT`).

Canonical `ai-status.json` has no `in_progress` tasks. Runtime `.orchestrator/state.json` has one stale task-cache entry: `SVC-FINRL-DORMANT-SCAFFOLD-CLOSEOUT` still appears as `in_progress` under Copilot, while canonical status is `todo` under Codex2 after the 16:32:08 auto-reassignment. No live Copilot worker remains.

Queue and approval queues are not blocked. The only event in `event-queue.jsonl` is this chair review. `.orchestrator/approval-queue.json` has no pending approvals.

## Findings

1. Fake `in_progress`: canonical board is clean, but runtime state cache is stale for `SVC-FINRL-DORMANT-SCAFFOLD-CLOSEOUT`. The Copilot worker was superseded after `402 You have no quota`, and the task was auto-reassigned from Copilot to Codex2. This is a state reconciliation issue, not a task-board mutation request.

2. Reviewer failure loop: `SVC-BFF-OIDC-JWKS-AUTH-FACADE` is in `review` with reviewer Codex2. Codex2 review workers failed twice before terminal status, most recently at 16:29:06. The pending handoff to Codex2 remains, but the loop is not producing a review. Reassign reviewer to Claude.

3. Owner/reviewer dispatch consistency: recent dispatches otherwise matched the then-current owner/reviewer. Claude reviewed and approved `SVC-QLIB-GATED-PREACTIVATION-PREFLIGHT`; Codex handed off `SVC-RLLIB-RAYTUNE-DORMANT-SCAFFOLD-CLOSEOUT` for Claude2 review. Superseded worker records are expected after responsibility/status movement.

4. Provider guardrails: `claude2` is paused for auth until 16:33:39, `gemini` for capacity until 16:37:01, and `copilot` for quota until 16:46:59. These reduce capacity but are not global blockers because Codex, Codex2, and Claude remain usable. Claude2 pause directly affects `SVC-RLLIB-RAYTUNE-DORMANT-SCAFFOLD-CLOSEOUT` review and Claude2-owned backlog.

5. Review and `review_approved` dwell: no long-stale review items were found. Current items are recent: BFF review at 16:27:54, RLlib/Ray Tune review at 16:30:57, and Qlib `review_approved` at 16:31:41. The actionable issue is not age; it is BFF reviewer failure and Qlib needing Codex2 finalization.

6. Sidecars: no active sidecar overload was found. The prior BFF handoff sidecar is done, and new BFF sidecars would duplicate support instead of solving the reviewer loop. Underutilization remains low and there is runnable backlog, so sidecars are safe if limited and aimed at clear support needs outside the blocked parents.

7. Review PR bus noise: repeated `github_review_pr_failed` events report `backend-dev-publish-20260429` has no history in common with `master`. This is not currently blocking local dispatch, but it will keep creating noisy review-bus failures until branch lineage or PR creation policy is repaired.

## Suggested Repairs

- Reassign `SVC-BFF-OIDC-JWKS-AUTH-FACADE` reviewer from Codex2 to Claude, then dispatch a fresh review. Do not create another sidecar for this parent.

- Re-dispatch `SVC-QLIB-GATED-PREACTIVATION-PREFLIGHT` to owner Codex2 for closeout finalization: task-scoped commit, required evidence, and `done` via the closeout skill. Do not treat `review_approved` as terminal.

- Let the next supervisor reconciliation clear the stale `SVC-FINRL-DORMANT-SCAFFOLD-CLOSEOUT` runtime cache entry. If it persists for another loop, create a SUP follow-up to refresh `.orchestrator/state.json.tasks` from canonical `ai-status.json` after `task_reassigned` events.

- If Claude2 remains auth-paused after `blocked_until`, reassign `SVC-RLLIB-RAYTUNE-DORMANT-SCAFFOLD-CLOSEOUT` review to Claude or Codex2 and move Claude2-owned todo work to an available provider until credentials are repaired.

- Create an OPS/SUP follow-up for the GitHub review PR branch-lineage issue: pause repeated PR creation or fix the `backend-dev-publish-20260429` head/base relationship before further review-bus retries.

## Sidecar Recommendation

Approve sidecars with TTL 45 minutes and max 2. Safe candidates should be support-only packets for runnable todo work with explicit parent needs, such as data ownership mapping or autonomous source/search smoke support. Do not spawn sidecars for `SVC-BFF-OIDC-JWKS-AUTH-FACADE`, `SVC-QLIB-GATED-PREACTIVATION-PREFLIGHT`, or `SVC-RLLIB-RAYTUNE-DORMANT-SCAFFOLD-CLOSEOUT`; those need reassignment, finalization, or reviewer recovery rather than extra support material.
