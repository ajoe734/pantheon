# Chair Operational Review - 2026-05-25 19:09:09Z (Codex2)

Chair: Codex2
Review written: 2026-05-25T19:13:35Z
Queue depth from prompt: 0
Pending approvals from prompt: 0
Paused lanes from prompt: none

## Summary

Decision: approve_sidecars, max_sidecars: null.

Execution dispatch is clear: no `in_progress`, `todo`, `review`, or `review_approved` work is present on the active board. The only live worker is this chair review run (`codex-20260525T190909Z-3f3243c2`) and its queue event matches (`evt-20260525T190909Z-f53aa841`). No pending approvals, no dispatch pauses, and all configured providers report `auth_ready: true`.

The active board still has two legitimate human-gate blockers: `PROD-WRITES-001-V2` and `LIVE-SCALE-001-V2`. These should not be reassigned or dispatched until their listed human signoffs / prerequisites arrive.

Important separate issue: publish/promote hygiene is unhealthy. The execution queue is not blocked by it, but release automation is. `nightly-publish-cut.yml` has repeated failures, local `nightly_publish.sh check` fails release discipline, and `publish-promote.yml` is failing hourly while trying to promote old publish snapshots.

## Findings

1. Fake `in_progress`: none found. `ai-status.json` and `.orchestrator/state.json` have no active implementation tasks. Only this chair review worker is live.

2. Owner/reviewer or queue mismatch: none found on active work. No task has `owner == reviewer`; the only queue event is this chair review event and it is correctly leased to Codex2.

3. Dispatch / approval queue: no stuck execution queue item. `.orchestrator/state.json` has only `evt-20260525T190909Z-f53aa841` in `started` state for this review. Approval queue has no actionable pending approval.

4. Provider guardrails: not blocking. `dispatch_pauses` is empty and providers are auth-ready (`claude`, `claude2`, `gemini`, `gemini2`, `codex`, `codex2`, slot variants, `copilot`, `grok`).

5. Review / review_approved linger: none found on current board.

6. Sidecars: no active sidecar pressure. Current board only shows `OSS-STAT-001-SIDECAR-ACCEPTANCE` as `done`; there is no duplicate active sidecar wave and no unsafe parent saturation.

7. Closeout hygiene: active board has no safe normal push approval to process. However release discipline flags closeout metadata gaps:
   - `OSS-STAT-001-SIDECAR-ACCEPTANCE` is `done` with no delivery metadata.
   - `LOVABLE-STRICT-PUBLISH` is `done` but still has an open phantom blocker and delivery metadata missing commit / trailers.
   - `OSS-QUANTLIB-V2-001` is `done` but still has an open phantom blocker and delivery metadata missing commit / trailers.

8. Open task PR: PR #556 (`task/OPS-BFF-DELTA-V3-DISPATCH-20260525`) is open against `dev`, checks are green, but `mergeable` is `CONFLICTING`. It is about 16 hours old at this review, below the 24 hour default but well past the configured 120 minute drift alarm. It needs rebase/conflict repair by the owning path.

9. Publish/promote automation:
   - `nightly-publish-cut.yml` has repeated scheduled failures. Local `bash scripts/git/nightly_publish.sh check` fails release discipline for W25: missing W23/W24 wave open events, W25 not frozen/closed, and the metadata gaps listed above.
   - `publish-promote.yml` is failing hourly while candidates `v2026.05.18.0` and `v2026.05.19.0` remain eligible. The failed step throws `CalledProcessError` on `git merge --no-ff ... origin/publish/v2026.05.18.0`. The workflow does not configure git committer identity before creating the promote merge commit, and `publish_promote.py` captures but does not print git stderr, so the immediate root cause is hidden in logs.
   - No open `promote/*` PR currently exists.

10. Branch hygiene: 255 remote `task/*` heads exist while only one open PR uses a `task/*` branch. Treat the rest as potential zombie branches, not automatically deletable. They need a reachability audit before deletion.

## Suggested Repairs

1. Re-dispatch or create an OPS repair for PR #556: rebase `task/OPS-BFF-DELTA-V3-DISPATCH-20260525` on current `dev`, resolve conflicts, and push the task branch.

2. Create `OPS-PUBLISH-PIPELINE-REPAIR-20260525`:
   - Repair W25 release discipline or explicitly close/freeze the wave before the next cut.
   - Repair delivery metadata / blocker records for `OSS-STAT-001-SIDECAR-ACCEPTANCE`, `LOVABLE-STRICT-PUBLISH`, and `OSS-QUANTLIB-V2-001`.
   - Add git identity setup to `publish-promote.yml` or `publish_promote.py` before merge-commit creation.
   - Make `run_git` print captured stderr on failure so the next promote failure is diagnosable.
   - After repair, run `nightly-publish-cut.yml` and `publish-promote.yml` via `workflow_dispatch`.

3. Create or continue `OPS-BRANCH-CLEANUP`: audit all 255 remote `task/*` branches, then delete only heads that are proven merged into `dev` or explicitly abandoned.

4. Keep `PROD-WRITES-001-V2` and `LIVE-SCALE-001-V2` blocked. They require human signoffs / prerequisite evidence, not worker reassignment.

## Sidecar Recommendation

Approve sidecars for 45 minutes with no cap. There is no dispatch, approval, provider, or sidecar-saturation blocker. Current board has no eligible parent task, so approval mainly permits safe repair/support work if the supervisor creates it; it should not generate sidecars for the two human-gate blockers.
