# Chair Review - 2026-04-29T00:08:44Z (Codex)

## Summary

Supervisor lifecycle is healthy and the queue is current: the 2026-04-29T00:08:44Z cycle started one execution dispatch and this chair review. `ai-status.json` has no `in_progress` tasks, so there is no fake in-progress board entry.

The main operational risk is the repeated Codex2 reviewer loop on `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE`. The queue targets reviewer `Codex2` correctly, but the worker continues to behave like owner-side `Codex`, revalidating and re-handing off instead of issuing a review decision. The relevant Codex2 failure streak is now 46 non-terminal exits, with the last recorded failure at `2026-04-29T00:08:43Z`; the newly started worker emitted another Codex handoff at `2026-04-29T00:10:08Z`.

There are no pending approvals. Claude is paused for quota until `2026-04-29T12:20:00Z`, which blocks Claude-reviewed support work but is not a global execution blocker.

## Findings

1. No fake `in_progress`: all active board items are `todo`, `review`, or `review_approved`; no task claims `in_progress` without a live worker.

2. Queue owner/reviewer mapping is structurally correct for the active execution item. `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` is `review` with reviewer `Codex2`, and event `evt-20260429T000844Z-924df6c5` targets `Codex2`.

3. The Codex2 worker behavior is wrong or identity-confused. Repeated worker logs show `Codex` owner-style validation and handoff back to `Codex2`, leaving the task in `review` instead of producing approve/reject. This is now the dominant loop.

4. Dispatch queue is not stale. `.orchestrator/state.json` marks the current sidecar review dispatch and chair review as `started` at `2026-04-29T00:08:44Z`; queue depth is not accumulating.

5. Approval queue is clear. `.orchestrator/approval-queue.json` has `pending: []`, matching the prompt's `Pending Approvals: 0`; no `approval_actions` are appropriate.

6. Provider guardrail is partial. Claude has a quota pause from `2026-04-28T20:02:17Z` until `2026-04-29T12:20:00Z`. This stalls `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE`, whose reviewer is Claude, but Codex/Codex2/Gemini/Copilot lanes are not globally blocked.

7. Review and finalization debt is holding the mainline. `SVC-RESEARCH-WORKER-GATEWAY` has been `review_approved` since `2026-04-28T20:05:05Z` and needs owner `Codex2` finalization. `SVC-HEALTH-OBSERVABILITY-UNIFICATION` remains `todo` and depends on that gateway task, so the next mainline item should not be treated as free to run until finalization clears.

8. Sidecar pressure is concentrated but not excessive. There are two active sidecar review items, both with clear `helper_parent`, `helper_kind=acceptance_packet`, and support-only artifact scope. The problem is duplication risk on the same parents, not a lack of parent support need.

9. GitHub review PR creation is still failing for both active sidecars with `GraphQL: The codex/2026-04-21-exec-sync branch has no history in common with master`. This is not blocking local dispatch, but it is persistent OPS debt.

## Suggested Repairs

- Fix the Codex2 reviewer dispatch identity so a Codex2-targeted review run acts as reviewer `Codex2`, not owner `Codex`.
- Add a retry circuit breaker for reviewer non-terminal exits. After a small threshold, stop requeueing the same review dispatch and create an OPS follow-up.
- Prioritize owner finalization of `SVC-RESEARCH-WORKER-GATEWAY`; that should unblock the dependency chain toward `SVC-HEALTH-OBSERVABILITY-UNIFICATION`.
- Keep the Claude-reviewed sidecar parked until the Claude quota pause expires, or reroute by explicit supervisor policy rather than silent redispatch.
- Create an OPS follow-up for the GitHub review PR branch ancestry failure on `codex/2026-04-21-exec-sync`.

## Sidecar Recommendation

Approve sidecars globally for 45 minutes with `max_sidecars=2`, because utilization remains low and there is no global approval or provider outage. Block new sidecars for parents already carrying review/finalization debt:

- `SVC-RESEARCH-ORCHESTRATOR-SERVICE`: existing acceptance sidecar is in a Codex2 reviewer loop.
- `SVC-RESEARCH-WORKER-GATEWAY`: parent is already `review_approved` and has an open acceptance sidecar stalled on Claude.
- `SVC-HEALTH-OBSERVABILITY-UNIFICATION`: dependency-gated until `SVC-RESEARCH-WORKER-GATEWAY` is finalized.

Recommended focus: repair the Codex2 review identity loop, finalize `SVC-RESEARCH-WORKER-GATEWAY`, then resolve or explicitly reroute the Claude-paused sidecar review.
