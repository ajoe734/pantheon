# Chair Operational Review - 2026-05-22T21:51:32Z

Chair Agent: Codex

## Summary

本輪 operational review 檢查 `ai-status.json`、`.orchestrator/state.json`、approval queue、provider capabilities、recent activity、open PRs、release/promote workflows 與遠端 task branches。

主執行面目前是 idle / sprint-transition 狀態：queue depth 0、pending approvals 0、dispatch pauses `{}`、provider guardrails 無 active pause，mode occupancy 只有本 chair review 正在跑。task board 沒有 `in_progress`、`review`、`review_approved`；只有 2 個 human-gate `blocked` task 與 3 個 `done` task。

Sidecar 決策：approve sidecars, no cap。現在沒有可立即產生 sidecar 的 active parent backlog，但也沒有 global sidecar blocker；保持 approval token 開放，讓下一批 BFF-P0 / sprint task materialize 後可直接啟動。

## Findings

1. No fake `in_progress`.
   `ai-status.json` 與 `.orchestrator/state.json` 都顯示 0 個 `in_progress` / `review` / `review_approved`。live worker 只有本次 chair review worker `codex-20260522T215132Z-caf86b7b`，`task_id: null`，queue event `evt-20260522T215132Z-e70aae75` 對得上本輪 chair dispatch。

2. Worker routing / queue alignment is clean.
   `.orchestrator/state.json.queue.events` 只有本 chair event，status `started`，lease 未過期。沒有 task worker 跑錯 owner/reviewer，也沒有 stale dispatch queue item。

3. Approval queue is empty.
   `.orchestrator/approval-queue.json.pending` length = 0。沒有 safe normal push approval 可由主席處理，也沒有需要 deny 的 stale approval。

4. Provider guardrails are not blocking mainline.
   `provider_guardrails.dispatch_pauses` 為 `{}`；`provider_capabilities.json` 顯示 Claude/Claude2/Gemini/Gemini2/Codex/Codex2/Copilot adapters 可用且 auth ready。沒有 provider-wide auth failure 或 quota pause。

5. Human gates are correctly blocked.
   `PROD-WRITES-001-V2` 與 `LIVE-SCALE-001-V2` 都是 `task_class: human_gate` / `non_dispatchable: true`，等待 risk owner / operator signoff 與前置條件。這不是 fake stuck work，也不應 reassign reviewer。

6. Closeout hygiene has stale metadata that now blocks publish.
   `bash scripts/git/nightly_publish.sh check` fails release discipline because wave `2026-W25` is not frozen/closed, wave open events for `2026-W23` and `2026-W24` are missing, and current board done tasks have incomplete delivery metadata:
   - `OSS-STAT-001-SIDECAR-ACCEPTANCE` is done with no delivery metadata.
   - `LOVABLE-STRICT-PUBLISH` delivery metadata is missing commit and commit trailers.
   - `OSS-QUANTLIB-V2-001` delivery metadata is missing commit and commit trailers.
   There is no current `push_status: ahead` on the active board, but the closeout metadata gap is now a release workflow blocker.

7. Publish/promote pipeline is failing.
   `nightly-publish-cut.yml` failed on 2026-05-20, 2026-05-21, and 2026-05-22 at "Check if dev advanced"; local reproduction shows release discipline failures above. `publish-promote.yml` has 10 recent failures; latest run `26313328465` found eligible candidates `v2026.05.18.0` and `v2026.05.19.0` but failed opening promote PRs on `git merge --no-ff ... origin/publish/v2026.05.18.0` with exit 128. Since `.github/workflows/publish-promote.yml` does not configure git identity before creating a merge commit, the likely immediate cause is missing CI committer identity. No `promote/*` PR is open.

8. Remote task branch hygiene is degraded.
   `git ls-remote --heads origin 'refs/heads/task/*' | wc -l` returns 143, while `gh pr list --state open` returns 0. These are zombie task branches unless individually proven active or intentionally retained.

9. Sidecar pressure is safe.
   Current active board has only one sidecar record and it is `done` (`OSS-STAT-001-SIDECAR-ACCEPTANCE`). No duplicate live sidecars, no parent saturation, and no unclear active parent support need. Current backlog simply has no materialized runnable parent candidates.

## Suggested Repairs

1. Create `OPS-RELEASE-DISCIPLINE-REPAIR`.
   Scope: repair wave lifecycle metadata and current done-task delivery metadata so `scripts/git/nightly_publish.sh check` passes. Do not mark tasks done manually; update the durable metadata path through the normal closeout/sync mechanism.

2. Create `OPS-PUBLISH-PROMOTE-FIX`.
   Scope: add git identity configuration to `publish-promote.yml` or make `scripts/git/publish_promote.py` configure `user.name` / `user.email` before merge commits, then rerun `publish-promote.yml` for eligible releases.

3. Create `OPS-ZOMBIE-BRANCH-CLEANUP`.
   Scope: batch verify `origin/task/*` heads are reachable from `origin/dev` or intentionally abandoned, then delete safe zombie task branches. Do not delete branches still ahead of `dev` without explicit signoff.

4. Trigger or materialize the next execution wave.
   The board is empty except human gates. If BFF-P0 is the intended next lane, dispatch the first materialized tasks rather than generating sidecars from no parent.

## Sidecar Recommendation

Approve sidecars with `max_sidecars: null`.

Reason: idle auto workers exist, there are no queue/approval/provider guardrail blockers, and there is no unsafe sidecar saturation. The current runnable backlog is empty, so the approval mostly prevents an unnecessary extra chair cycle once the next parent tasks are materialized.
