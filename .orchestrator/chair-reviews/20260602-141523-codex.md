# Chair Operational Review - 2026-06-02 14:15 UTC - Codex

## Summary

- Decision: approve sidecars with TTL 45 minutes and no max-sidecar cap, but do not generate sidecars from stale ASST parent rows until state reconciliation completes.
- No `in_progress`, `review`, or `review_approved` rows exist in `ai-status.json`, so there is no fake in-progress task or long-lived review dwell visible on the board.
- No pending approvals were supplied; no low-risk approval action is available this cycle.
- Main risk is state drift: multiple ASST tasks are still listed as `todo` in `ai-status.json` even though their task branches or PRs have already merged, and `ASST-USER-001` has an ahead task branch with no open PR.

## Findings

1. **Task board drift can create false dispatch/backlog.**
   `ai-status.json` still lists all active ASST rows as `todo`. At least `ASST-KERNEL-001` and `ASST-KERNEL-002` already have archive records marked completed, while many other ASST task branches have merged PRs on `dev`. Examples: `ASST-FE-002` PR #790 merged at `2026-06-02T13:30:52Z`; `ASST-USER-001` PR #787 merged at `2026-06-02T04:58:33Z`; `ASST-FE-001` PR #784 and `ASST-BFF-001` PR #782 are also merged. This is not a fake `in_progress`, but it is a false `todo` backlog risk.

2. **`ASST-USER-001` has a task branch ahead of `dev` with no open PR.**
   `origin/task/ASST-USER-001` is ahead by commit `38613b4a` (`ASST-USER-001: enforce kernel gate on context; fix env pollution`). The commit has task trailers and verification text, but there is no open PR for the branch. Do not delete this branch; it needs a task PR or an explicit supersede/archive decision.

3. **Merged task branches remain on origin.**
   Verified heads for `ASST-KERNEL-001`, `ASST-KERNEL-002`, `ASST-KERNEL-003`, `ASST-OCGW-001`, `ASST-OCGW-002`, `ASST-OCGW-005`, `ASST-KERNEL-006`, `ASST-KERNEL-007`, `ASST-BFF-001`, `ASST-FE-001`, `ASST-FE-002`, and `ASST-SEC-001` are ancestors of `origin/dev`. These are zombie branch cleanup candidates after state reconciliation confirms no open PR still needs them.

4. **Stuck task PR exists.**
   PR #682 (`task/OPS-ARCHIVE-AUTO-COMMIT-20260531T165903Z`) has been open since `2026-05-31T16:59:11Z`, checks are green, but merge state is `DIRTY`. It is over the 24h task PR threshold and needs owner refresh/rebase or closure if superseded.

5. **Old non-promote PRs are open against `master`.**
   `gh pr list --base master` shows old `task/chair-review-*` PRs (#688, #695, #698, #705, #726, #729), all older than 24h and failing multiple checks. No open `promote/*` or `hotfix/*` PR was found. These look like routing/hygiene leftovers, not promote-path work.

6. **Residual blockers remain open for already-done work.**
   `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` are `done` with manual sync notes, but their blocker entries still show `open` from Gemini2 push-auth failures on `2026-05-19`. They should be cleared through the normal state repair path after verifying the manual sync records.

7. **Provider and queue source limitations.**
   This worktree does not contain current `.orchestrator/state.json` or `.orchestrator/provider_capabilities.json`; the local `ai-activity-log.jsonl` tail is stale at `2026-05-15`. The supervisor prompt reports queue depth `1`, pending approvals `0`, paused lane `copilot`, and underutilization `0.1429`. Based on available source, Copilot pause is local to FE ownership and is not a global blocker.

## Suggested Repairs

- Create an `OPS-STATE-RECON-20260602-ASST` follow-up to reconcile `ai-status.json`, archives, task PR merges, blockers, and current-work mirrors using the sanctioned status/archive tooling. Do not hand-edit task terminal states.
- Re-dispatch or explicitly wake the owner for `ASST-USER-001` to open/merge a PR for commit `38613b4a`, or record a clear supersede decision if the ahead commit should not land.
- Re-dispatch PR #682 to its owner for rebase/refresh; if it is an obsolete auto-archive backfill, close it with a note.
- Clear stale blockers for `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` only after confirming their manual sync records and merged PR references.
- After state reconciliation, delete merged zombie task branches whose heads are reachable from `origin/dev`; do not delete `task/ASST-USER-001` while it is ahead.
- Close, retarget, or otherwise clean old `task/chair-review-*` PRs against `master`; promote/hotfix lanes should use `promote/*` or `hotfix/*` heads.
- Ensure future chair worktrees include current `.orchestrator/state.json`, provider capability data, and queue event metadata so queue age and owner/reviewer event matching can be verified directly.

## Sidecar Recommendation

Approve sidecars for non-stale support work: idle capacity exists, execution has a queued item, pending approvals are zero, and there is no global blocker. However, block sidecar generation for stale ASST parents until the state reconciliation follow-up lands, because those rows may represent already-merged work rather than runnable backlog.

No approval actions or reassignment actions are recommended this cycle.
