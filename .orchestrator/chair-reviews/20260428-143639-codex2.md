# Operational Review - Codex2 - 2026-04-28 14:36 UTC

## Summary

Decision: approve sidecars, capped at 2, but do not generate additional sidecars for `SVC-RUNTIME-CONTROL` or `SVC-RUNTIME-CONTROL-CLOSEOUT` until the existing review backlog drains.

No fake board-level `in_progress` was found. The only `in_progress` task is `SVC-CONSULTATION-SERVICE-ACTIVATION-SIDECAR-BFF-HANDOFF`, and it has a live Codex worker (`codex-20260428T143639Z-93e108d6`, pid 509816) with matching queue event `evt-20260428T143639Z-b76d8581`.

## Findings

1. `in_progress` integrity: OK. There is no board task marked `in_progress` without a live worker.

2. Worker and queue matching: mostly OK. The two active queue events are both `started`: one consultation sidecar worker and this chair review. The chair review worker has `task_id: null`, which is expected for a chair event, not a fake task worker.

3. Approval queue: needs reconciliation. `.orchestrator/approval-queue.json` has pending approval `apr-20260428T143356Z-7fd5719e`, but `.orchestrator/state.json` reports only `approvals.last_reconciled_at: null` and no pending approval summary. The approval itself is recent, not stale, but the state-plane summary is out of sync.

4. Provider guardrails: not a global blocker. `gemini` is paused until 14:41 UTC and `copilot` until 14:43 UTC due quota/capacity failures. `qwen` remains effectively unavailable by capability metadata. Codex/Claude lanes still allow progress, so mainline dispatch should continue with provider-aware routing.

5. Review and review_approved dwell: not long-stale yet. `SVC-GOVERNANCE-API` is in `review` after reviewer reassignment churn; `SVC-RUNTIME-CONTROL` is `review_approved` and waiting finalize. Existing sidecar reviews are recent: runtime closeout acceptance at 14:29, runtime BFF handoff at 14:36.

6. Sidecar pressure: moderate. Existing support work is useful but concentrated around runtime-control:
   - `SVC-RUNTIME-CONTROL-CLOSEOUT-SIDECAR-ACCEPTANCE` in review
   - `SVC-RUNTIME-CONTROL-SIDECAR-BFF-HANDOFF` in review
   - `SVC-CONSULTATION-SERVICE-ACTIVATION-SIDECAR-BFF-HANDOFF` running
   - `SVC-RUNTIME-CONTROL-CLOSEOUT-SIDECAR-REVIEW` still todo
   - `SVC-SEARCH-SERVICE-SIDECAR-BFF-HANDOFF` still todo

## Suggested Repairs

- Run approval reconciliation or supervisor approval refresh so `.orchestrator/state.json` reflects the pending item in `.orchestrator/approval-queue.json`.
- Drain `SVC-GOVERNANCE-API` review before starting more governance-family implementation, because the reviewer has already bounced between Claude and Codex2.
- Let `SVC-RUNTIME-CONTROL` finalize after the scoped staging approval is resolved; avoid spawning more runtime-control support packets until current review packets are accepted or closed.
- Create an OPS/SUP follow-up for state hygiene: `SVC-GOVERNANCE-API.next` contains raw provider payload fragments from reviewer reassignment, which should be represented as structured `failure_kind` plus `raw_ref`.

## Sidecar Recommendation

Approve sidecars with `max_sidecars: 2` and TTL 45 minutes. There is execution backlog and no global blocker, but sidecar generation should skip `SVC-RUNTIME-CONTROL` and `SVC-RUNTIME-CONTROL-CLOSEOUT` for now because those parents already have active/review support coverage.

Recommended focus:

- `SVC-GOVERNANCE-API`
- `SVC-RUNTIME-CONTROL`
- `SVC-RUNTIME-CONTROL-SIDECAR-BFF-HANDOFF`
- `SVC-RUNTIME-CONTROL-CLOSEOUT-SIDECAR-ACCEPTANCE`
