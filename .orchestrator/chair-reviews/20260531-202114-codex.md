# Chair Operational Review - 2026-05-31 20:21 UTC - Codex

## Summary

Decision: approve sidecars with no global cap. There is idle worker capacity and a runnable assistant execution backlog, with no pending approvals and no global provider-wide pause.

Exception: do not generate sidecars for `ASST-OCGW-001` until its owner recovery is clean. That task has partial dirty worktree state and the assigned Gemini worker is repeatedly exiting on quota.

## Findings

1. `ASST-OCGW-001` is a fake live `in_progress` item at inspection time. Live board shows `ASST-OCGW-001` as `in_progress`, owner `Gemini`, reviewer `Codex`; the last worker state file for `gemini-20260531T202115Z-dbb61b27` is `failed`, and `ps` shows the recorded PID as defunct. The latest Gemini log reports `TerminalQuotaError` / 429 with quota reset after about 19h34m.

2. The queue event itself is not misassigned. The live event `evt-20260531T202112Z-02dcf127` targets `Gemini` for `ASST-OCGW-001`, matching the task owner and `owned_in_progress_dispatch`. The failure mode is provider capacity plus stale live-worker accounting, not owner/reviewer mismatch.

3. Provider guardrail is not stopping the failing loop. `.orchestrator/state.json` has empty `provider_guardrails.dispatch_pauses`, but the activity log shows repeated `ASST-OCGW-001` redispatches about every supervisor cycle, followed by `worker_failed` boot reconciliation. This keeps the main execution lane busy without progress.

4. Dispatch and approval queues are not generally stuck. Live queue contained the current `ASST-OCGW-001` dispatch plus this chair review dispatch during inspection; approval queue has `pending: []`.

5. No long-lived `review` or `review_approved` task is present in the live board. The only non-terminal executable task is `ASST-OCGW-001` in `in_progress`.

6. Sidecar pressure is low. The live board has no active sidecars and only one old completed sidecar (`OSS-STAT-001-SIDECAR-ACCEPTANCE`). Sidecars are operationally reasonable, but not for `ASST-OCGW-001` until its dirty worktree and owner handoff are handled.

7. Closeout push hygiene is clean for current board records. Done tasks do not show `push_status: ahead`; `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` record merged PRs via manual sync after old Gemini2 push-auth failure.

8. PR oversight: open dev PR #682 (`task/OPS-ARCHIVE-AUTO-COMMIT-20260531T165903Z`) is under 24h old, checks are green, but merge state is `BEHIND`; monitor or rebase if it remains open. Open master PR #688 is a `task/chair-review-*` branch targeting `master`, failing many master checks, and should not be promoted as-is.

9. Publish pipeline health is degraded. `publish-promote.yml` is failing hourly; the latest inspected run fails in `publish_promote.py open-prs` when `git merge --no-ff ... origin/publish/v2026.05.18.0` exits 128. No open `promote/*` PRs or remote `promote/*` branches were found. `nightly-publish-cut.yml` has failed daily from 2026-05-27 through 2026-05-31; latest release tag remains `release/v2026.05.19.0` despite `origin/dev` advancing today.

## Suggested Repairs

- Create or dispatch an ops repair for Gemini quota handling: classify the latest Gemini `TerminalQuotaError` as a quota/capacity pause and stop redispatching `ASST-OCGW-001` to Gemini until the reset window or a healthy provider handoff is available.

- Preserve `ASST-OCGW-001` worktree state before any owner change. Its worktree has uncommitted changes in compose files, gateway adapter files, tests, task brief, and evidence. Do not overwrite or refresh it until an anchor commit or explicit salvage decision is made.

- Add a follow-up such as `OPS-GEMINI-QUOTA-GUARD-20260531` to ensure worker-runtime `failed` status plus Gemini 429 logs update `provider_guardrails.dispatch_pauses` before the next redispatch cycle.

- Add `OPS-PROMOTE-PIPELINE-20260531` to debug `publish_promote.py` merge exit 128 for `v2026.05.18.0` / `v2026.05.19.0`, then rerun `publish-promote.yml` by workflow dispatch after the merge base issue is fixed.

- Add `OPS-NIGHTLY-PUBLISH-CUT-20260531` to triage repeated nightly publish failures and restore the daily release tag path.

- Close or retarget PR #688 away from `master`; task/chair-review branches should not enter master directly outside the promote/hotfix path.

## Sidecar Recommendation

Approve sidecars. Do not set a max sidecar cap. Block sidecar generation for parent `ASST-OCGW-001` until the Gemini quota loop and dirty task worktree are recovered. Prefer sidecars for independent `todo` backlog items with clear parent support needs and healthy non-Gemini providers.
