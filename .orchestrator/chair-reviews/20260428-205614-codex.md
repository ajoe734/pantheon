# Chair Review - 2026-04-28T20:56:14Z (Codex)

## Summary

Operational state is usable but has one review-loop issue. `ai-status.json` has no `in_progress` tasks, so there is no fake in-progress board entry. The live execution worker is `codex-20260428T205614Z-c5e96f2b` on `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE`; the chair worker is separate. Approval queue has `pending_count=0`.

Mainline should focus on `SVC-RESEARCH-WORKER-GATEWAY`, which has been `review_approved` since `2026-04-28T20:05:05Z` and needs owner `Codex2` finalization. `SVC-HEALTH-OBSERVABILITY-UNIFICATION` remains `todo` and is dependency-gated on `SVC-RESEARCH-WORKER-GATEWAY`, so it should not be pulled forward until that finalization lands.

Claude is paused by provider guardrail until `2026-04-29T12:20:00Z`; Claude2 is installed but not authenticated. This is not a global blocker because Codex/Codex2/Gemini/Copilot remain available, but it blocks the Claude-reviewed sidecar path.

## Findings

1. No fake `in_progress`: active board states are `review_approved` (`SVC-RESEARCH-WORKER-GATEWAY`), `todo` (`SVC-HEALTH-OBSERVABILITY-UNIFICATION`), and two `review` sidecars.

2. Current queue event `evt-20260428T205614Z-924d7a66` correctly targets reviewer `Codex2` for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE`. However, the worker log shows the Codex2 review worker running `AI_NAME=Codex scripts/ai-status.sh handoff ... Codex2`, which re-hands the sidecar back to the same reviewer instead of producing a reviewer decision. This has repeated at roughly `20:24`, `20:29`, `20:34`, `20:40`, `20:45`, `20:50`, and `20:56`, causing a review-ready dispatch loop.

3. Dispatch queue is current, not stale: both state queue events from `20:56:14Z` are `started`. The recurring Codex2 sidecar review dispatch is the operational problem, not an old stuck queue item.

4. Approval queue is clear: `.orchestrator/approval-queue.json` has no pending approvals. No `approval_actions` are recommended.

5. Review backlog is small but needs ordering: `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE` is in `review` with reviewer `Claude` since `19:44:21Z`, but Claude is quota-paused. `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` is in `review` with live Codex2 dispatch but looping.

6. Sidecar count is at the configured cap of two open sidecars. Both have clear `helper_parent`, `helper_kind=acceptance_packet`, `mutates_canonical=false`, and support-only artifacts. Do not spawn duplicate sidecars for the same parents until the current review/finalization backlog clears.

## Suggested Repairs

- Fix the Codex2 reviewer identity path for `review_ready_dispatch`: Codex2 review workers should run as `AI_NAME=Codex2` or otherwise call the review/approve path, not owner `handoff` as `Codex`.
- Add a retry guard for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` so repeated non-terminal reviewer exits create an OPS follow-up instead of indefinite requeue.
- Prioritize owner finalization of `SVC-RESEARCH-WORKER-GATEWAY`; this unblocks `SVC-HEALTH-OBSERVABILITY-UNIFICATION`.
- Avoid Claude/Claude2 dispatch for new work while Claude is quota-paused and Claude2 remains unauthenticated.
- Treat repeated GitHub review PR creation failures (`branch has no history in common with master`) as a separate non-blocking OPS follow-up; they are noisy but not the current execution blocker.

## Sidecar Recommendation

Approve sidecars globally for 45 minutes with `max_sidecars=2`, but block duplicate sidecars for active or dependency-gated parents:

- `SVC-RESEARCH-ORCHESTRATOR-SERVICE`: parent already completed and the existing acceptance sidecar is in a reviewer loop.
- `SVC-RESEARCH-WORKER-GATEWAY`: parent is already `review_approved` and has an open acceptance sidecar.
- `SVC-HEALTH-OBSERVABILITY-UNIFICATION`: still dependency-gated on `SVC-RESEARCH-WORKER-GATEWAY`.

Recommended focus: clear `SVC-RESEARCH-WORKER-GATEWAY` finalization first, then resolve the Codex2 sidecar review loop, then revisit Claude-paused sidecar review.
