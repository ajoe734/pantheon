# Chair Operational Review - 2026-05-31 20:31 UTC - Codex2

## Summary

Decision: approve sidecars with no global cap.

There is idle capacity and a runnable assistant backlog, pending approvals are empty, and there is no provider-wide global pause. One parent must be quarantined: do not generate sidecars for `ASST-OCGW-001` until its Gemini quota failure and dirty task worktree are recovered.

## Findings

1. `ASST-OCGW-001` is a fake live `in_progress` item at inspection time. Live state shows the task `in_progress`, owner `Gemini`, reviewer `Codex`, but the current runner status file for `gemini-20260531T203135Z-5e9f0996` is `failed`, the recorded runner PID is defunct, and the child PID is gone.

2. The latest Gemini worker failed on provider quota, not a task-board owner/reviewer mismatch. The log `20260531T203135928951Z-gemini-gemini-19ee3c.log` reports Gemini API 429 `TerminalQuotaError` with reset after about 19h24m. The queue event `evt-20260531T203133Z-9ec7d4f7` correctly targets `Gemini` for `owned_in_progress_dispatch`.

3. Provider guardrail is not preventing a failing redispatch loop. Live `provider_guardrails.dispatch_pauses` is empty even though recent activity shows repeated `ASST-OCGW-001` worker starts followed by `worker process missing during supervisor boot reconciliation`. This is now blocking the main execution path for that task.

4. `ASST-OCGW-001` has non-durable partial work that should not be overwritten. Its task worktree is `task/ASST-OCGW-001`, behind `origin/dev` by 53 commits, and contains uncommitted edits in compose files, `.env.example`, `services/openclaw-gateway-adapter/main.py`, new credential mount/test files, task brief materialization, and evidence. The supervisor refresh is already skipping this worktree as dirty.

5. Dispatch and approval queues are not generally stale. The live queue contains the current `ASST-OCGW-001` dispatch and this chair review dispatch, both started this cycle with leases expiring at `2026-05-31T21:01:39Z`. The approval queue has `pending: []`.

6. No long-lived `review` or `review_approved` task is present. The live board has no tasks in `review` or `review_approved`.

7. Sidecar pressure is low. There are no active sidecars and only one completed sidecar on the live board, `OSS-STAT-001-SIDECAR-ACCEPTANCE`. Sidecars are operationally reasonable for independent backlog, but not for `ASST-OCGW-001` until recovery is clean.

8. Closeout push hygiene is clean for current board records. Done tasks do not show `push_status: ahead`; `LOVABLE-STRICT-PUBLISH` and `OSS-QUANTLIB-V2-001` record merged closeout PRs and manual sync after the older Gemini2 push-auth issue.

9. PR oversight: open dev PR #682 is a task archive PR under 24h old with green checks, so it is not stuck yet. Open master PR #688 is a `task/chair-review-*` branch targeting `master`, not a `promote/*` branch, and it is behind with failing checks. It should be closed or retargeted away from `master`; it should not be treated as a promote PR.

10. Publish pipeline health is still degraded. The last five `publish-promote.yml` runs on 2026-05-31 failed. The latest failure is in `publish_promote.py open-prs` when `git merge --no-ff ... origin/publish/v2026.05.18.0` exits 128. There are no remote `promote/*` branches and no open `promote/*` PRs. The last five `nightly-publish-cut.yml` runs from 2026-05-27 through 2026-05-31 also failed.

## Suggested Repairs

- Stop redispatching `ASST-OCGW-001` to Gemini until quota resets or a deliberate recovery handoff is made. Record a provider guardrail pause for Gemini quota failure from the latest 429 log.

- Preserve `ASST-OCGW-001` partial work before any owner change. Prefer an owner anchor commit or explicit salvage decision; do not refresh, rebase, or overwrite the dirty worktree first.

- Create `OPS-GEMINI-QUOTA-GUARD-20260531` to make Gemini `TerminalQuotaError` update `provider_guardrails.dispatch_pauses` before the next redispatch loop.

- Create `OPS-PROMOTE-PIPELINE-20260531` for `publish_promote.py` merge exit 128 and rerun `publish-promote.yml` only after the merge-base issue is fixed.

- Create `OPS-NIGHTLY-PUBLISH-CUT-20260531` to triage the repeated `nightly-publish-cut.yml` failure at "Check if dev advanced".

- Close or retarget PR #688 away from `master`; only `promote/*` and hotfix PRs should target `master`.

## Sidecar Recommendation

Approve sidecars. Do not set `max_sidecars`. Block sidecar generation for parent `ASST-OCGW-001` until the Gemini quota loop and dirty task worktree are recovered. Prefer sidecars on independent `todo` backlog with clear parent support needs and healthy non-Gemini providers.
