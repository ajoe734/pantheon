# Chair Review - 2026-04-28T22:00:25Z (Codex)

## Summary

Operational state is usable, but the active execution lane is stuck in a reviewer wake loop. `ai-status.json` has no `in_progress` tasks, so there is no fake in-progress board entry. The only live execution queue event is for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE`, and the queue event correctly targets reviewer `Codex2`.

The main operational problem is that Codex2 review workers are behaving as `Codex`: the current log shows the worker using the default actor path and re-handing the sidecar from `Codex` to `Codex2` instead of issuing a reviewer decision. This has repeated through many non-terminal exits; `provider_guardrails` records 22 generic exits for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE:codex2`.

There are no pending approvals. Claude is provider-paused for rate limit until `2026-04-29T12:20:00Z`, which blocks the Claude-reviewed sidecar path but is not a global blocker because Codex/Codex2/Gemini capacity still exists.

## Findings

1. No fake `in_progress`: `ai-status.json` currently has zero tasks with `status=in_progress`. Gemini is assigned to `SVC-HEALTH-OBSERVABILITY-UNIFICATION`, but that task is still `todo`, not fake in-progress.

2. Queue event target is structurally correct but worker identity is wrong. `evt-20260428T220025Z-c932296b` targets `Codex2` for `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` because the task is in `review` with reviewer `Codex2`. The active log shows the worker revalidating and calling `scripts/ai-status.sh handoff ... Codex2` as `Codex`, leaving the task in `review`.

3. Dispatch queue is current, not stale. State queue events from `2026-04-28T22:00:25Z` are `started`; the problem is repeated fresh dispatch of the same sidecar review, not an old queue item stuck unprocessed.

4. Approval queue is clear. `.orchestrator/approval-queue.json` has `pending: []`, and the supervisor prompt also reports `Pending Approvals: 0`; no `approval_actions` are recommended.

5. Provider guardrail is partial. Claude is paused for quota until `2026-04-29T12:20:00Z`, blocking `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE` because its reviewer is `Claude`. `.orchestrator/provider-capabilities.json` is absent, so provider availability evidence comes from `.orchestrator/state.json` only.

6. Review backlog needs clearing before new duplicate support work. `SVC-RESEARCH-WORKER-GATEWAY` has been `review_approved` since `2026-04-28T20:05:05Z` and should be finalized by owner `Codex2`. `SVC-RESEARCH-WORKER-GATEWAY-SIDECAR-ACCEPTANCE` has been in `review` since `2026-04-28T19:44:21Z` but reviewer `Claude` is paused. `SVC-RESEARCH-ORCHESTRATOR-SERVICE-SIDECAR-ACCEPTANCE` is in `review` but cycling.

7. Sidecar pressure is at the cap. Two active sidecars exist, both with clear `helper_parent`, `helper_kind=acceptance_packet`, and `mutates_canonical=false`. Do not create duplicate sidecars for these parents or for `SVC-HEALTH-OBSERVABILITY-UNIFICATION` while its dependency `SVC-RESEARCH-WORKER-GATEWAY` is not finalized.

## Suggested Repairs

- Fix Codex2 reviewer identity for `review_ready_dispatch`: set the review worker environment so `current_actor()` resolves to `Codex2`, or route reviewer wakes to a review/approve path instead of a handoff path.
- Add a retry circuit breaker for repeated non-terminal reviewer exits. After a small threshold, create an OPS follow-up and stop requeueing the same `review_ready_dispatch`.
- Prioritize owner finalization of `SVC-RESEARCH-WORKER-GATEWAY`; this should unblock `SVC-HEALTH-OBSERVABILITY-UNIFICATION`.
- Keep the Claude-reviewed sidecar parked or formally reroute it through supervisor policy while Claude is paused until `2026-04-29T12:20:00Z`.
- Open a separate OPS follow-up for repeated GitHub review PR failures: `GraphQL: The codex/2026-04-21-exec-sync branch has no history in common with master`.
- Either regenerate `.orchestrator/provider-capabilities.json` or update the chairman-review skill if `.orchestrator/state.json` is now the sole provider guardrail source.

## Sidecar Recommendation

Approve sidecars globally for 45 minutes with `max_sidecars=2`, because utilization is low and there is no global approval or provider blocker. Block sidecars for these specific parents until the current backlog clears:

- `SVC-RESEARCH-ORCHESTRATOR-SERVICE`: parent is done, and the existing acceptance sidecar is looping in reviewer dispatch.
- `SVC-RESEARCH-WORKER-GATEWAY`: parent is already `review_approved` and has an open acceptance sidecar.
- `SVC-HEALTH-OBSERVABILITY-UNIFICATION`: task is still dependency-gated on `SVC-RESEARCH-WORKER-GATEWAY` finalization and already has prior support material.

Recommended focus: finalize `SVC-RESEARCH-WORKER-GATEWAY`, repair the Codex2 sidecar review loop, then resolve or reroute the Claude-paused sidecar review.
