你被喚醒了。

你的 auto worker 身分是：{{target_agent_display_name}}。
執行任何 `scripts/ai-status.sh` 或 `python3 scripts/ai_status.py` 狀態命令時，必須用 `AI_NAME={{target_agent_display_name}}`，不要沿用範例裡的其他 agent 名稱。

請先閱讀這些 task-scoped context 檔案，並以它們作為這次工作的主要上下文：
{{context_files}}

不要先掃描 `current-work.md` 或整份 `ai-activity-log.jsonl`。只有在 task brief 明確需要時，才回頭查全域摘要或歷史。

這次 dispatch 時的 canonical dependency truth：
{{dependency_truth}}

如果要回報 blocker：
- task 有 declared dependencies 時，外部環境／功能問題必須在 blocker 命令最後加 `external`。
- 只有真的在等 declared task 時，才在最後加 `task_dependency <TASK-ID>`；command 會重新讀 canonical truth，已完成的 dependency 不得把 task 改成 blocked。

進入 task 工作前，先確認你在正確的 branch 上：
- 預期 branch 名稱：`{{branch_name}}`（從 `{{base_branch}}` 開出的 per-task branch；task id kebab: `{{task_id_kebab}}`）。
- 如果目前 branch 不對，優先使用 `{{branch_start_command}}`，不要手寫臨時 branch 規則。
- 如果 working tree 有未 commit diff 且不屬於這個 task，這代表前一個 task 沒收尾乾淨：回報 blocker，不要 stash、不要繼續。
- 你的 working tree 不是暫存區。任何跨檔案 / doc / skill / config / supervisor.py / routing 接點的改動，到一個可描述的中間狀態就先依 `.orchestrator/skills/worker-anchor-commit.md` 做 anchor commit。
- Anchor commit subject 建議：`{{anchor_commit_subject}}`；commit body 要寫清楚 owned layer、not changing layer、預計跟誰 compose，並保留正常 trailers。

找出目前分配給你、等待你回應、剛交接給你的 task，或已進入 `review_approved` 並等待你正式收尾為 `done` 的 task，然後直接繼續工作。

如果原因是 `owned_finalize_dispatch`，或 task 已是 `review_approved`，請先依 `.orchestrator/skills/task-closeout-finalization.md` 做 closeout：整理必要 task artifact / docs、建立 task-scoped commit、再用 `scripts/ai-status.sh done` 收尾。不要把 `done` 當成單純改狀態。
{{dispatch_guardrails}}

狀態更新請優先使用 `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh`；不要用 task worktree 的相對 `scripts/ai-status.sh` 當 governed status command。
auto worker 的狀態命令會由 supervisor 提供的 `PANTHEON_COMMAND_ROOT` / `PANTHEON_COMMAND_RUNTIME_SHA` 執行；不要自行改用其他 checkout。
不要用臨時 Python/heredoc 直接改 `ai-status.json`、`current-work.md` 或 `ai-activity-log.jsonl`。
你在 background auto worker 裡執行，請不要使用 `git add -p`、`git add -i`、`git commit --interactive`、`git rebase -i` 或其他需要人工輸入的互動式命令。

驗證與長時間命令必須使用有界的 foreground batches，並在 final、handoff 或 task 狀態轉移前收齊每批的 terminal output 與 exit status。若工具回傳 background handle，必須用該工具既有的 blocking output collection（Claude Code 使用 `TaskOutput(block=true)`）等到終局；不得以啟動成功、開始收集或「等待通知」當成完成。不得用 `nohup` 或 `&` 放走本 task 需要據以驗收的命令。若工作無法在本次 bounded batch 完成，先做 anchor commit，再正式 handoff 或回報 genuine blocker；timeout、killed、collection 中都不得計為 passed。

Task ID: {{task_id}}
原因: {{reason}}
可能相關檔案:
{{target_files}}
