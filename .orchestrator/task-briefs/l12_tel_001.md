# Task Brief: L12-TEL-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Make telemetry acknowledgement durable and preserve lifecycle identity
- Status: ready_for_review
- Owner: Codex2
- Reviewer: Codex
- Next: Independent `Codex` review of follow-up anchor `064366e8f` and the refreshed checksummed evidence, covering all three prior rejection paths.

## Summary
移除 202-before-durable-write 遺失窗，修復 heartbeat 抹除 trace identity，加入 auth/tenant 與可重播 durable buffer。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.

## Reviewer follow-up resolution

1. Batch ingest returns 400 for empty or wholly rejected batches. Mixed batches
   return 202 only when at least one event has a durable receipt and report
   `status=partially_accepted` with both counts.
2. `RuntimeSummaryProjectionStore` persists by `(tenant_id, runtime_id)`.
   Cross-tenant lifecycle/heartbeat, restart, scoped list/get, and ambiguous
   unscoped-get regressions prove identities cannot overwrite one another.
3. All five telemetry lineage GET routes pass the authenticated tenant into a
   tenant-scoped graph. Cross-tenant and unscoped legacy records return 404.

## Owner handoff

- Prior merged delivery: PRs `#4148` and `#4155`, merge commits
  `809c286e456cea262b0b272b34c5e16c87792090` and
  `4f905c5a563483ff15eee9221c47eb720b8cecce`.
- Reviewer rejection anchor: `4ee451348`.
- Follow-up implementation anchor: `064366e8f`.
- Evidence: `docs/deployment/evidence/twelve-loop-gap/L12-TEL-001`.
- Validation: 167 focused tests passed, including the real NATS child-process
  crash recovery; TEL-002 smoke, compile, schema, and refreshed checksums
  passed.
- Discovery residual: 266 telemetry tests ran with one skip and the same
  unrelated legacy `RuntimeManagerClient(allow_local=True)` setup error.
