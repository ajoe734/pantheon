# Task Brief: L12-TEL-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Make telemetry acknowledgement durable and preserve lifecycle identity
- Status: ready_for_review
- Owner: Codex2
- Reviewer: Codex
- Next: Independent `Codex` review of the schema-valid ProductEvidenceManifest, exact repo-relative review_file binding, positive owner-evidence admission, merged PR #4148/#4155/#4170 delivery, refreshed README/checksums, and deliberately absent formal approval verdict.

## Summary
移除 202-before-durable-write 遺失窗，修復 heartbeat 抹除 trace identity，加入 auth/tenant 與可重播 durable buffer。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.

## Reviewer reopen retained

Implementation and merge evidence were accepted independently: PR `#4170`
merged as `c762af14d56d141c59efc31b255c3814ef4378b3`; 167 focused tests,
including real NATS crash recovery, TEL-002 smoke, compileall, trade-journal
schema, checksums, and diff-check passed. The remaining rejection was limited
to the old `evidence.json` shape, which could not pass
`schemas/product-evidence.schema.json` or the product-level done guardrail.

## Owner schema remediation

- `evidence.json` now carries every required `ProductEvidenceManifest` block
  and uses string schema version `loop_product_evidence.v1`.
- `task.review_file` is the exact repository-relative
  `docs/deployment/evidence/twelve-loop-gap/L12-TEL-001/evidence.json`.
- `overall_admission=pass_owner_evidence_ready` is positive owner admission,
  not a reviewer approval claim.
- PR `#4148`, `#4155`, and `#4170` head/merge/time/CI identities, existing
  acceptance and security results, real restart recovery, bounded runtime
  readback, non-blocking residuals, and checksum integrity are normalized.
- `record_log` retains both `Codex` changes-requested decisions and has no
  approved formal verdict. After a later approval, owner closeout must append
  the actual `Codex` verdict before `done`.
