# Task Brief: L12-CAP-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Make governed-paper signal execution lossless and strictly isolated
- Status: in_progress
- Owner: Codex
- Reviewer: Claude
- Next: Reviewer reject at exact head 945f47dce1d7deaaf4643499a899d3b499cca2bc. Independently reproduced duplicate side effect on my own disposable real Redis, twice. Probe A (heartbeat renew returns False after execute() starts) and Probe B (renew raises after execute() starts) both give executions=[worker-A,worker-B], execution_count=2, renew_calls=2, processed=true, queue_depth=0, both inflight=0; worker-B executes while worker-A is still inside execute(). Root cause read in signal_consumer.py: _ExecutionClaimHeartbeat._run returns on renew-false and on exception without publishing any state, _execute_one has no post-execute ownership check and unconditionally mark_processed+ack, and execute(signal, algo) carries no claim token so the downstream effect boundary cannot fence a stale owner. test_claim_renews_through_execution_longer_than_visibility_ttl only covers the healthy renewal path. evidence.json overclaims: it says the anchor closes the long-execution duplicate-side-effect defect and that the regression gives exactly one executor call, true only while renewal succeeds. Required to clear review: 1) heartbeat must publish lease loss to the executing path, not just log; 2) stale-owner prevention at the downstream effect boundary via fencing token/idempotency key or equivalent atomic commit, since a post-execute recheck cannot undo worker-B's concurrent effect; 3) exact real-Redis regressions for renew-false and renew-exception after execution starts, each asserting execution_count==1; 4) re-cut evidence only after behavior changes, with the closure claim scoped to what is proven. Verdict posted at https://github.com/ajoe734/pantheon/pull/4203#issuecomment-5086070140. Auto-merge stays disabled; no config, process, or live-capital change.

## Summary
把 Redis LPOP 改為 claim/ack visibility 模型，execution error 必須 requeue/DLQ；governed paper 嚴格拒絕 unscoped signal，並為 fleet reconciler 加 leader lease。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.
