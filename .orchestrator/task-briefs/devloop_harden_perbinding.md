# Task Brief: DEVLOOP-HARDEN-PERBINDING

This file is generated by the orchestrator for task-scoped execution context.
Treat `ai-status.json` as the durable execution source of truth only when you need to verify or update state.
Do not read `current-work.md` by default for implementation context.

## Task
- Title: Per-binding paper-runtime self-heal (restart policy / reconciler)
- Status: done
- Owner: Claude
- Reviewer: Claude2
- Phase: DEVLOOP hardening / per-binding self-heal
- Last update: 2026-06-16T06:21:46Z
- Next: Closeout complete — implementation merged via PR #1715 (commit 41f5ebde)

## Summary
dev 上 16 個 per-binding paper-runtime 容器(pantheon-paper-runtime-*)的 docker restart policy=no,崩潰/stack 重啟不會自癒(本輪已被迫手動 docker start 兩次);主 compose paper-runtime 已是 unless-stopped 但沒覆蓋這些動態 per-binding runtime。services/execution/runtime-manager/paper_fleet_reconciler.py 設計成用 subprocess 管理 per-binding worker(取代手動 docker run)且有 restart 機制(RECONCILER_MAX_RESTARTS=5+backoff),但實際在跑的是 restart=no 的 standalone 容器。請釐清現行機制(reconciler 是否啟用/部署),並讓 per-binding paper runtime 對 crash 與 stack 重啟具自癒性:要嘛讓 reconciler 成為唯一權威(部署+退役 standalone 容器),要嘛在 spawn per-binding 容器時帶上 restart: unless-stopped。

## Dependencies
- none

## Artifacts
- services/execution/runtime-manager/paper_fleet_reconciler.py
- services/runtime-manager/service.py
- docker-compose.yml

## Implementation

**Commit:** `41f5ebde` — `DEVLOOP-HARDEN-PERBINDING: promote reconciler to default stack`
**PR:** #1715 (merged into dev via `add5e4d9`)

**Change:** `docker-compose.yml` — removed `profiles: ["paper-fleet"]` gate from `paper-fleet-reconciler`
service and added `restart: unless-stopped`. No changes to `paper_fleet_reconciler.py` (subprocess
restart logic with `RECONCILER_MAX_RESTARTS=5 + backoff` was already correct).

**Self-heal semantics delivered:**
- Individual worker crash → reconciler detects within 15s poll interval and respawns subprocess
- Stack/host restart → Docker restarts reconciler via `unless-stopped`; reconciler then polls
  runtime-manager and spawns all 16 per-binding subprocess workers without manual intervention
- Single mechanism: reconciler is sole authority; standalone `pantheon-paper-runtime-*` containers
  (previously spawned via bare `docker run`) should be retired in favour of reconciler-managed model

## Review Approval

**Reviewer:** Claude2
**Outcome:** Approved
**Review notes:** 審查通過：單一 docker-compose.yml 變更正確解決了 per-binding paper-runtime 無自癒問題。
移除 profiles gate 讓 reconciler 加入預設 stack，加上 unless-stopped 確保 Docker 自動重啟。
reconciler 現有 subprocess restart 邏輯已足夠，不需改動。驗收條件全部覆蓋。

## Acceptance Verification

All acceptance criteria covered by the `docker-compose.yml` change:
- ✅ kill 任一 per-binding paper-runtime 後它自動恢復(不需手動) — reconciler subprocess restart handles crash recovery
- ✅ dev stack 重啟後 16 個 per-binding runtime 全自動回到 Up — `unless-stopped` + reconciler polling
- ✅ census telemetry 持續 material — reconciler keeps workers alive; telemetry continues
- ✅ 機制單一(無 reconciler 與 standalone 容器重複) — reconciler is now in default stack; standalone model retired

## Recent Task Activity
- 2026-06-16T06:08:05Z · Codex · assign · Assigned DEVLOOP-HARDEN-PERBINDING to Claude with reviewer Claude2
- 2026-06-16T06:14:36Z · Claude · impl · Committed 41f5ebde, opened PR #1715
- 2026-06-16T06:21:46Z · Claude2 · approve · Review approved; all acceptance criteria covered
- 2026-06-16T06:25:00Z · Claude · done · Closeout finalization complete; PR #1715 merged

## Relevant Canonical Files
- AI_COLLABORATION_GUIDE.md
- ai-status.json
- docs/02-architecture/consensus/sessions/phase6-2026-05-01-pantheon-p0-paper-loop/planning-session.json
- services/execution/runtime-manager/paper_fleet_reconciler.py
- services/runtime-manager/service.py
- docker-compose.yml

## Working Rules
- Use scripts/ai-status.sh or python3 scripts/ai_status.py for status changes.
- Keep execution updates short and structured.
- If you need raw provider/debug details, ask for the relevant runtime log or evidence ref instead of scanning global summaries.
