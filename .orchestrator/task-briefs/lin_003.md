# Task Brief: LIN-003

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Live lineage write path (LIN-003)
- Status: in_progress
- Owner: Claude
- Reviewer: Codex
- Next: Materialized from EVOCHAIN-001 round-3 review (PR #3509): the deployed telemetry->incidents lineage-resolution gap needs live writes from telemetry (event + upstream deployment-lineage chain), runtime-manager, and control-plane/governance -- not something a single producer task can close. Full scope, root cause, and required exit evidence in docs/decisions/LIN-003-live-lineage-write-path.md. EVOCHAIN-001 is blocked on this landing for its default-validator acceptance criterion; EVOCHAIN-010 (producer-chain live verifier) depends on both.

## Summary
- Root cause (EVOCHAIN-001 round-3, PR #3509, blocker #1): `services/telemetry`'s
  `LineageReadService` only ever loaded a static LIN-001A corpus once at
  process startup; accepted telemetry events were never written into it, so
  the deployed `CanonicalReferenceValidator` 404'd on any live event/binding.
- Delivered: `LineageReadService.admit_telemetry_event()` (live write path,
  lock-guarded) in `services/telemetry/lineage_read/service.py`;
  `TelemetryIngestService` gains `lineage_write_store` and reuses the
  RuntimeBinding it already resolves during evidence validation instead of a
  second lookup (`services/telemetry/ingest_svc.py`); `main.py` builds the
  lineage service before the ingest service and wires them together.
- Full scope, root cause, and explicit follow-on (runtime-manager,
  control-plane/governance) recorded in
  `docs/decisions/LIN-003-live-lineage-write-path.md`.
- Exit evidence: `services/telemetry/lineage_read/test_service.py` (unit,
  `TestLiveLineageWritePath`) and `services/telemetry/test_lineage_write_path.py`
  (integration — real ingest + real lineage graph + real
  `CanonicalReferenceValidator`, plus a full-stack test proving the real
  `POST /api/incidents/consume-threshold` FastAPI route returns 201 on first
  delivery and 200 on idempotent replay against a real runtime-manager
  deploy). `python3 -m pytest services/telemetry services/incident
  services/incidents services/runtime-manager -q` — 518 passed.
- `docker compose config --quiet` passes.
- Handed off to Codex for review.
