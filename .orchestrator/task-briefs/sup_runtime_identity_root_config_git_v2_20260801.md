# Task Brief: SUP-RUNTIME-IDENTITY-ROOT-CONFIG-GIT-V2-20260801

This file is generated by the orchestrator for task-scoped execution context.
Treat `ai-status.json` as the durable execution source of truth only when you need to verify or update state.
Do not read `current-work.md` by default for implementation context.

## Task
- Title: Build immutable runtime root, config, and Git identity guards
- Status: in_progress
- Owner: Antigravity
- Reviewer: Human/Ops
- Phase: Supervisor Runtime Identity V2 / 1 Root Config Git
- Last update: 2026-08-01T04:04:01Z
- Next: Exact head 4cd85c7a8087bebc38713131c45a52e703f06918 on PR #4443 is rejected. Although current GitHub checks are green, independent git diff --check fails at scripts/promote_supervisor_runtime.py:123 (trailing whitespace) and scripts/test_promote_supervisor_runtime.py:930 (new blank line at EOF), contradicting the handoff claim. The PR adds an undeclared generated .orchestrator/task-briefs file and omits the declared review_pending evidence directory; acceptance explicitly requires task briefs out of the implementation commit. The identity design still binds the wrong config: it hard-codes <candidate-root>/.orchestrator/config.json instead of the exact live /home/lupin/pantheon-ci-deploy/runtime/live-supervisor-mainroot-config.json. verify_against_live_config resolves before is_symlink, so a symlink alias to the real file is accepted; config parent-component symlinks and path-swap races are untested. EXPLICIT_GENERATED_UNTRACKED_PATHS wrongly admits .orchestrator/config.json and its path predicate also allows rel_path in allowed.parents; replacement config/parent entries must fail closed. Remote handling is still an exact string set, not structural GitHub HTTPS/SSH parsing, and git fetch origin dev followed by mutable origin/dev does not create one freshly fetched immutable accepted-dev identity bound into the candidate snapshot. Tests mock fetch without proving any accepted remote ref update, so a forged local origin/dev fixture still passes. The five broad new tests omit required separate cases for ssh/https structural forms and spoofing, relative/nested/symlink-component escape, deleted-root race, basename-to-HEAD mismatch, commit absent from freshly fetched accepted dev, tree/index/staged mismatch, every forbidden/allowed untracked path, exact external live config path, parent/leaf symlink alias, and independent byte/path/length/SHA drift. Rewrite cleanly from current origin/dev with 4cd/853/#4437 rejected ancestry absent. Build one immutable snapshot around the exact external live config bytes/path/length/SHA and a freshly fetched trusted dev ref, remove config from untracked allowances, use race-resistant component checks/comparison, add the complete separate deny matrix, remove the generated task brief, add truthful review_pending evidence in the declared directory, run focused tests, local trailer gate and git diff --check, push #4443, wait CI, and hand off exact head. Do not inspect /proc, launch/signal processes, edit config, change services, or materialize product tasks.

## Summary
Replace only the rejected candidate-root, config-byte, and Git identity layer with immutable fail-closed values and deterministic tests.

## Dependencies
- SUP-RUNTIME-PROMOTION-SNAPSHOT-INVARIANTS-20260801: done · Build live-schema supervisor promotion snapshots and invariants

## Artifacts
- scripts/promote_supervisor_runtime.py
- scripts/test_promote_supervisor_runtime.py
- docs/deployment/evidence/supervisor/SUP-RUNTIME-IDENTITY-ROOT-CONFIG-GIT-V2-20260801/

## Recent Task Activity
- Omitted from automatic dispatch context. The canonical task row above is the bounded handoff context; query validated activity history only for targeted forensic work.

## Relevant Canonical Files
- AI_COLLABORATION_GUIDE.md
- ai-status.json
- docs/02-architecture/consensus/sessions/phase6-2026-05-01-pantheon-p0-paper-loop/planning-session.json
- scripts/promote_supervisor_runtime.py
- scripts/test_promote_supervisor_runtime.py
- docs/deployment/evidence/supervisor/SUP-RUNTIME-IDENTITY-ROOT-CONFIG-GIT-V2-20260801/

## Working Rules
- Use scripts/ai-status.sh or python3 scripts/ai_status.py for status changes.
- Keep execution updates short and structured.
- If you need raw provider/debug details, ask for the relevant runtime log or evidence ref instead of scanning global summaries.
