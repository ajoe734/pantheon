# Task Brief: SUP-PROGRESS-AWARE-FAILURE-STREAK-RETRY-20260801

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Make same-owner reviewer retries progress-aware and bounded
- Status: in_progress
- Owner: Antigravity
- Reviewer: Claude
- Next: Reviewer Claude rejects head 35056b716. (1) Acceptance 1 unmet: same_owner_reviewer_retry_allowed (supervisor.py:6301) returns False unless owner==reviewer, but the incident task SUP-RUNTIME-PROMOTION-SNAPSHOT-INVARIANTS-20260801 is owner=Antigravity reviewer=Human/Ops, so the gate never fires for the reproduced deadlock; the new test fabricates reviewer=Antigravity instead. Acceptance 3 means retry with the same owner, not owner==reviewer. (2) Acceptance 5 unmet: nothing consumes the generation. clear_task_failure_streak progress_generation= has zero callers, record_task_failure_streak seeds last_progress_generation=0, and task_progress_generation returns int(last_update epoch), so current_gen>0 always holds and the gate stays open forever once armed; last_update also bumps on any status write incl. dispatch sync, so it is not reviewer-reopen/commit-bound progress. (3) Acceptance 4 regression, same defect as the prior rejection: update_worker_commit_progress now calls clear_task_failure_streak(state, worker=worker) unconditionally, popping auth/quota/policy streaks (recorded at supervisor.py:6822 and 6139/6148) on any worker commit. (4) Acceptance 3 missing worktree-lease check: only active_worker_indexes and outstanding_delivery_indexes are consulted, no worker_worktrees.leases. (5) Acceptance 6 unmet: no durable activity audit of task/provider/prior count/kind/progress evidence/decision anywhere in the new path. (6) No PR-backed delivery: PR #4438 is CLOSED since 2026-08-02 at this same head and no open PR to dev exists, so there is no exact-head binding to approve. (7) Evidence manifest self-declares review_decision review_approved before any review, names Reviewer Human/Ops while the canonical reviewer is Claude, and cites .venv-pantheon/bin/python3 which does not exist in the worktree. Independently reran PYTHONPATH=.orchestrator pytest -q .orchestrator/test_supervisor.py: 470 passed, so the suite is green but does not cover the actual incident shape. Required: bind eligibility to unchanged owner plus a governed reviewer reopen/handoff or verified commit/head event that postdates last_failure_at, persist and consume the generation so a later exit without newer progress re-reaches the threshold, restrict the commit-progress clear to non-terminal kinds, add the lease check and the durable audit, add tests for the real owner!=reviewer incident plus replay and unrelated-streak preservation, then open a clean PR to dev with Reviewer: Claude trailers and a manifest with no pre-declared verdict.

## Summary
Close the durable dispatch gap exposed by PR #4434: a canonical exact-head reviewer reopen with new work must not remain permanently suppressed by historical same-owner generic exits, while genuine repeated failures, auth/quota faults, and live leases remain fail-closed.

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.
