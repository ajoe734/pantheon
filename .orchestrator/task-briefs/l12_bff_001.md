# Task Brief: L12-BFF-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Implement durable infrastructure health telemetry and incident recovery
- Status: in_progress
- Owner: Codex2
- Reviewer: Codex
- Next: Exact-head PR #4274 at caef48af71178e22ff38e8afca7445ffa91b5d77 cannot be approved. AC5 durable recovery fails after retention: prune_retained_history deletes delivered incident-open rows, _dependencies_delivered treats missing dependency rows as undelivered, and recovery still queues incident-resolve against incident_mappings.create_delivery_id; independently reproduced an old delivered open being pruned followed by resolve remaining pending with claim_due returning no work. Preserve dependency completion durably or prevent pruning referenced rows, and add a recovery-after-retention regression. Also recut the task evidence: validate_twelve_loop_gap_evidence.py rejects the current manifest (head_binding, current_delivery_checks, current_cut_consistency, bound_document_consistency, and related fields), while evidence.json still names owner Claude2/reviewer Claude, validated head f2be990d, and pending approval instead of canonical Codex2/Codex exact head caef48af. Existing 167 focused tests, py_compile, checksum, diff/trailer gates, and all PR CI checks pass but do not cover these blockers.

## Summary
建立非交易 infrastructure telemetry authority，補 error-rate spike、完整 downstream registry、durable probe state、delivery retry/DLQ及 incident resolve，移除會被 binding validation 拒絕的 sentinel。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.
