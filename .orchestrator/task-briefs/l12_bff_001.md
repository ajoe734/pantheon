# Task Brief: L12-BFF-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Implement durable infrastructure health telemetry and incident recovery
- Status: in_progress
- Owner: Antigravity
- Reviewer: Claude
- Next: Human/Ops confirms Claude reviewer rejection after the reviewer command was lost during journal sequences 1646-1650. The branch changes only sentinel formatting and a claim-only evidence file. A UUID-shaped sentinel remains a fabricated RuntimeBinding; the module itself states binding validation will DLQ it. Probe/failure/open-incident state remains process memory; no complete authoritative downstream registry or error-rate trigger exists; telemetry and incident delivery failures are swallowed without retry/DLQ/replay; incident telemetry_event_ids is empty; recovery merely pops local state and explicitly leaves the incident open. Required before review: authoritative infrastructure telemetry schema/path without fake trading identity, complete configured registry and interval/error-rate triggers, durable replica-safe state, shared event IDs with retry/DLQ/replay, incident update/resolve on recovery, and executed real-admission, spike, two-replica restart, and target stop/recovery drills with schema/checksum/PR checks.

## Summary
建立非交易 infrastructure telemetry authority，補 error-rate spike、完整 downstream registry、durable probe state、delivery retry/DLQ及 incident resolve，移除會被 binding validation 拒絕的 sentinel。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.
