# Task Brief: L12-SRC-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Implement safe supervised source due-state reconciliation
- Status: in_progress
- Owner: Codex2
- Reviewer: Codex
- Next: Review failed: the explicit two-worker idempotency acceptance is not met. The added tests use one TestClient/module and are serialized by module-level threading.RLock, so they prove same-process threads only. JsonlConnectorScheduleStore and JsonlIngestScheduleStore enqueue/claim operate on per-process in-memory maps without a cross-process transaction/lease. Independent two-instance reproduction against shared JSONL produced schedule_jsonl_records=2, two distinct frontiers, and both workers claimed one. Implement cross-process atomic reconcile plus enqueue/claim/dedupe (or a durable singleton lease that makes the second worker fail closed), add a genuine multi-process test proving one connector, schedule, run, and SourceRecord, update the evidence packet to remove the current false pass, then rerun focused and full source-ingestion suites.

## Summary
讓 Persona required_data_sources 自動形成 connector/schedule desired state；排程常駐但外部 pull 仍受 allowlist、rate limit 與 egress policy 限制。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.
