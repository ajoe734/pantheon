# Task Brief: L12-REC-001

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Harden reconciliation scheduling, storage, and recovery
- Status: in_progress
- Owner: Codex
- Reviewer: Codex2
- Next: Review rejected at 46ee200fa / PR #4150. Required fixes: (1) bind ConsumerWorkerState checkpoint save/release to the acquired lease_token with CAS and renewal or fail-closed ownership checks; regression must prove that after lease expiry a successor acquires and the stale worker cannot overwrite the successor token/checkpoints (current reproduction: successor_lease_preserved=False). (2) tenant-scope consumer pending/dead_letters/completed identity by tenant+event, or strictly constrain the worker to one configured tenant; equal external event IDs for tenant-a and tenant-b must both enqueue/deliver (current second enqueue=False). (3) tenant-aware JSON and Postgres get_* legacy/raw-key fallback must verify exact tenant before returning; current tenant-b get_drift_report returns a tenant-a record. Existing 94 reconciliation tests and 26 affected tests pass, as do py_compile, evidence checksum and PR required checks, so add regression coverage for these acceptance failures before re-handoff.

## Summary
修正 scheduled-reconcile timeout，將 record/report 與 worker state 轉為 durable authority，加入 scheduler/consumer lease、corruption fail-closed 與 tenant auth。

## Coordination Root
- Auto workers inherit `PANTHEON_STATUS_ROOT`, `PANTHEON_COMMAND_ROOT`, and `PANTHEON_COMMAND_RUNTIME_SHA` from the supervisor.
- Run `$PANTHEON_COMMAND_ROOT/scripts/ai-status.sh` for governed status changes; git, tests, and product edits continue in this task worktree while canonical status, activity, archive and lock writes are routed to the validated central root.

## Owner repair handoff

At `2026-07-26T10:42:08Z`, `Codex` completed the three repairs required by
the rejected review:

- Consumer checkpoints and release now compare the private token acquired by
  that worker with the canonical token under the state-file lock. Matching
  checkpoints renew the lease; after expiry and successor acquisition, stale
  save/release attempts fail without changing the successor token or
  checkpoint, including when both processes use the same worker ID.
- Consumer pending, dead-letter, and completed identities now include tenant
  plus external event ID. Equal `shared-event-id` values for tenant A and
  tenant B both enqueue, deliver, and retain separate completed receipts.
- Tenant-aware JSON and Postgres legacy/raw-key lookup now verifies the exact
  tenant for evaluations, alert handoffs, reconciliation records, drift
  reports, and worker states before returning a record.

Repair lineage:

- Rejected-acceptance regression anchor:
  `23959c7ae2816067fa2a605397cd936f7e2e8eeb`.
- Consumer/store repair anchor:
  `f4d9a84d5c79b1669f260f25fd428054e65d45d2`.

Owner verification:

- `pytest -q services/reconciliation-drift/tests` — `98 passed, 1 warning in
  30.45s`.
- Affected foundation/Postgres/lifecycle tests — `26 passed in 4.60s`.
- `py_compile`, Ruff `F,E9`, `git diff --check`, JSON parse, and evidence
  checksum — passed.
