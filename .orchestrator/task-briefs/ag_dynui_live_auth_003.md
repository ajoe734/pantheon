# Task Brief: AG-DYNUI-LIVE-AUTH-003

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Agora Trading Room frontend BFF auth headers
- Status: review_approved
- Owner: Claude
- Reviewer: Codex
- Next: Re-verified 2026-07-03 (owned_finalize_dispatch, 5th pass): execute-plans PR #148 is now MERGED (mergedAt=2026-07-03T13:18:16Z, mergeCommit=63437558318f9e2610c867f55f4b6d3f3ce7053e) and dev FE has redeployed from that exact commit (`/deployment.json` on https://pantheon-lupin-dev-fe.35.201.239.38.sslip.io confirms `commit=6343755...` deployed 20260703T131946Z). A naive unauthenticated live probe still showed 401 on `/bff/agora/trading-room` and `/bff/agora/trading-room/decision-events` (see /tmp/agora-live-after-auth003.json) — root cause: the probe browser session carried no cookie and no `pantheon.bff.bearerToken`, so `buildHeaders()` correctly omitted Authorization (nothing to send). Re-ran the probe seeded with the same dev bearer-token login the e2e suite uses (`execute-plans/e2e/helpers/auth.ts` `installOidcDevLogin`, tenant `pantheon-dev`) so a real Authorization header is sent as designed. Result: `/bff/agora/trading-room/decision-events` now returns 200 (fix works for that route), but `/bff/agora/trading-room` (the aggregate GET) returns HTTP 500 `INTERNAL_ERROR` from the BFF once a bearer token + correct `X-Tenant-Id: pantheon-dev` are present (confirmed via direct curl, reproducible; `/bff/me` with the same bearer returns 200 fine). The browser reports this as a CORS failure only because the BFF's 500 response omits CORS headers (unlike its 200/401/403 paths, which all carry `access-control-allow-origin`) — a secondary symptom, not the root cause. This is a BFF backend bug outside this task's FE-only scope (tradingRoom.ts / headers.ts), not something fixable by further edits to the artifacts owned here. Acceptance criteria "live browser probe shows /bff/agora/trading-room returns 200" and "does not show Failed to load Trading Room" are still NOT met. Recorded via `ai-status.py note` (not `blocker`/`progress`, to avoid downgrading `review_approved` per [[feedback_review_approved_done_flow]]); this needs a new backend task/handoff to fix the 500 on the bearer-authenticated aggregate trading-room BFF route before this task can close `done`. Evidence: /tmp/agora-live-after-auth003.json (unauth 401 baseline), /tmp/agora-live-after-auth003-authed.json + .png (authenticated probe showing decision-events 200 / trading-room CORS-surfaced 500).

## Summary
修 execute-plans Agora Trading Room frontend client: 所有 tradingRoom.ts read/write fetch 必須使用 shared BFF auth headers, 保留動態 BFF data flow, 補 Authorization 測試, PR merge 後等待 dev FE deploy 並用 live browser probe 證明 /bff/agora/trading-room 與 decision-events 都回 200。不得重做靜態 UI; 設計/合約不明時先開 blocker。
