# Task Brief: MGMT-PERF-IA-005-SIDECAR-BFF-HANDOFF-FOLLOWUP-4

Generated in the worker workspace because the supervisor root did not have a task brief file.

## Task
- Title: Prepare MGMT-PERF-IA-005 BFF and frontend handoff packet
- Status: review
- Owner: Codex2
- Reviewer: Claude
- Next: Independent reviewer verification complete (commit 4be66b7de touched only the sidecar packet and its task brief; `git show --stat` confirms no canonical/runtime/registry/governance/frontend file changed). Cross-checked all 7 route/behavior claims in the packet against `services/control-plane/bff/main.py` and confirmed each: `GET .../recommendations` (line 44208, built by `_pm12_quarterly_recommendation_item` at 42005) carries `recommendation_id`, evidence refs, `human_review_state`, governance destinations, pagination, `meta.surfaces`; `POST .../recommendations/{id}/submit` (42621) sets `live_capital_mutation: false` / `requires_human_gate_decision: true`; `GET/GET{id} .../promotion-reviews` (42729/42817, via `_promotion_review_item_from_recommendation` at 42283) expose stable review/promotion-review/Human Inbox ids, allowed decisions, submission and decision projections; `POST .../decisions` (42860) injects `live_capital_mutation: false` into `data` and `meta`; `GET .../formula` (43872) returns backend-authored weights/version_history/evidence_refs plus `meta.surfaces` health. The capital-join gap claim is also confirmed: neither recommendation nor promotion-review item builders emit a `rebalance_id`, proposal link, precondition-result link, or apply-receipt link; `ranking_snapshot_id`/`approval_ref` appear only on the rebalance-proposal side. `POST /bff/rebalances/{id}/apply` (24515) is an accepted governed command ("execution remains a separate command" per its own docstring), not a synchronous applied receipt. Content is independently fact-checked and accurate. However, formal `review_approved` for this task is expected to be classifier-blocked as self-approval for the Claude-reviewer + Codex2-owner sidecar lane on parent MGMT-PERF-IA-005 -- this exact pattern was confirmed repeatedly on sibling tasks FOLLOWUP-2 (3 denials before the chair reassigned reviewer to Antigravity, see `.orchestrator/reviews/MGMT-PERF-IA-005-SIDECAR-BFF-HANDOFF-FOLLOWUP-2-review-antigravity.md`) and FOLLOWUP-3 (2 denials, still stuck in `review`). Recommend the same fix here: reassign this task's reviewer to a non-Claude identity (e.g. Antigravity) so an independent `approve` can be issued; task remains `review` pending that reassignment or human action.

## Summary
平行支援 MGMT-PERF-IA-005，先整理 BFF query gap、operator journey 與前端 handoff materials，不改 canonical truth。
